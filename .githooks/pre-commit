#!/bin/bash
#
# Git pre-commit hook for baseline version freshness validation
# ==============================================================
#
# This hook warns when performance baselines are stale (version mismatch
# with package.json). Non-blocking — always exits 0.
#

set -u

# Run baseline version check (non-blocking)
node server/scripts/check-baseline-version.js --format=stderr || true

# Always exit 0 (non-blocking)
exit 0
