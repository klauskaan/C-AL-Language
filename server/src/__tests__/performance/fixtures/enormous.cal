// Auto-generated CAL file for performance testing
// Target: 10000 lines
// Complexity: complex
// Seed: 42

codeunit 50000 "Performance Test Large File"
{

table 50000 "Custom Table 0"
{
    DataClassification = CustomerContent;

    fields
    {
        field(1; "Entry No."; Integer)
        {
            DataClassification = CustomerContent;
            AutoIncrement = true;
        }
        field(2; "Code"; Code[20])
        {
            DataClassification = CustomerContent;
        }
        field(3; "Description"; Text[100])
        {
            DataClassification = CustomerContent;
        }
        field(4; "Amount"; Decimal)
        {
            DataClassification = CustomerContent;
        }
        field(5; "Date"; Date)
        {
            DataClassification = CustomerContent;
        }
    }

    keys
    {
        key(PK; "Entry No.")
        {
            Clustered = true;
        }
    }
}

procedure ComplexBusinessLogic0(
    DocumentNo: Code[20];
    PostingDate: Date;
    AccountNo: Code[20];
    Amount: Decimal;
    DimensionSetID: Integer
): Boolean
var
    GenJournalLine: Record "Gen. Journal Line";
    GLEntry: Record "G/L Entry";
    DimensionValue: Record "Dimension Value";
    TempDimSetEntry: Record "Dimension Set Entry" temporary;
    LastEntryNo: Integer;
    VATAmount: Decimal;
    BaseAmount: Decimal;
    TotalAmount: Decimal;
    i: Integer;
begin
    // Initialize variables
    VATAmount := 0;
    BaseAmount := Amount;
    TotalAmount := 0;

    // Validate account
    if AccountNo = '' then
        Error('Account number cannot be empty');

    // Check if document already posted
    GLEntry.SetRange("Document No.", DocumentNo);
    if not GLEntry.IsEmpty() then
        Error('Document %1 already posted', DocumentNo);

    // Calculate VAT
    if PostingDate >= DMY2Date(1, 1, 2024) then
        VATAmount := BaseAmount * 0.25
    else
        VATAmount := BaseAmount * 0.20;

    TotalAmount := BaseAmount + VATAmount;

    // Process dimension set
    if DimensionSetID <> 0 then begin
        TempDimSetEntry.SetRange("Dimension Set ID", DimensionSetID);
        if TempDimSetEntry.FindSet() then
            repeat
                if not DimensionValue.Get(
                    TempDimSetEntry."Dimension Code",
                    TempDimSetEntry."Dimension Value Code"
                ) then
                    Error(
                        'Dimension Value %1 %2 not found',
                        TempDimSetEntry."Dimension Code",
                        TempDimSetEntry."Dimension Value Code"
                    );
            until TempDimSetEntry.Next() = 0;
    end;

    // Create journal line
    GenJournalLine.Init();
    GenJournalLine."Line No." := GetNextLineNo0();
    GenJournalLine."Document No." := DocumentNo;
    GenJournalLine."Posting Date" := PostingDate;
    GenJournalLine."Account Type" := GenJournalLine."Account Type"::"G/L Account";
    GenJournalLine."Account No." := AccountNo;
    GenJournalLine.Amount := TotalAmount;
    GenJournalLine."VAT Amount" := VATAmount;
    GenJournalLine."Dimension Set ID" := DimensionSetID;
    GenJournalLine.Insert();

    // Post the entry
    GLEntry.Init();
    if GLEntry.FindLast() then
        LastEntryNo := GLEntry."Entry No."
    else
        LastEntryNo := 0;

    GLEntry."Entry No." := LastEntryNo + 1;
    GLEntry."G/L Account No." := AccountNo;
    GLEntry."Posting Date" := PostingDate;
    GLEntry."Document Type" := GLEntry."Document Type"::Invoice;
    GLEntry."Document No." := DocumentNo;
    GLEntry.Amount := TotalAmount;
    GLEntry."VAT Amount" := VATAmount;
    GLEntry."Dimension Set ID" := DimensionSetID;
    GLEntry.Insert();

    // Perform validation loops
    for i := 1 to 10 do begin
        if GLEntry.Amount <> TotalAmount then
            Error('Amount mismatch in iteration %1', i);
    end;

    exit(true);
end;

procedure GetNextLineNo0(): Integer
var
    GenJnlLine: Record "Gen. Journal Line";
begin
    if GenJnlLine.FindLast() then
        exit(GenJnlLine."Line No." + 10000)
    else
        exit(10000);
end;

page 50000 "Custom List 0"
{
    PageType = List;
    SourceTable = "Custom Table 0";
    UsageCategory = Lists;
    ApplicationArea = All;

    layout
    {
        area(Content)
        {
            repeater(Group)
            {
                field("Entry No."; "Entry No.")
                {
                    ApplicationArea = All;
                }
                field("Code"; "Code")
                {
                    ApplicationArea = All;
                }
                field("Description"; "Description")
                {
                    ApplicationArea = All;
                }
                field("Amount"; "Amount")
                {
                    ApplicationArea = All;
                }
            }
        }
    }
}

table 50001 "Custom Table 1"
{
    DataClassification = CustomerContent;

    fields
    {
        field(1; "Entry No."; Integer)
        {
            DataClassification = CustomerContent;
            AutoIncrement = true;
        }
        field(2; "Code"; Code[20])
        {
            DataClassification = CustomerContent;
        }
        field(3; "Description"; Text[100])
        {
            DataClassification = CustomerContent;
        }
        field(4; "Amount"; Decimal)
        {
            DataClassification = CustomerContent;
        }
        field(5; "Date"; Date)
        {
            DataClassification = CustomerContent;
        }
    }

    keys
    {
        key(PK; "Entry No.")
        {
            Clustered = true;
        }
    }
}

procedure ComplexBusinessLogic1(
    DocumentNo: Code[20];
    PostingDate: Date;
    AccountNo: Code[20];
    Amount: Decimal;
    DimensionSetID: Integer
): Boolean
var
    GenJournalLine: Record "Gen. Journal Line";
    GLEntry: Record "G/L Entry";
    DimensionValue: Record "Dimension Value";
    TempDimSetEntry: Record "Dimension Set Entry" temporary;
    LastEntryNo: Integer;
    VATAmount: Decimal;
    BaseAmount: Decimal;
    TotalAmount: Decimal;
    i: Integer;
begin
    // Initialize variables
    VATAmount := 0;
    BaseAmount := Amount;
    TotalAmount := 0;

    // Validate account
    if AccountNo = '' then
        Error('Account number cannot be empty');

    // Check if document already posted
    GLEntry.SetRange("Document No.", DocumentNo);
    if not GLEntry.IsEmpty() then
        Error('Document %1 already posted', DocumentNo);

    // Calculate VAT
    if PostingDate >= DMY2Date(1, 1, 2024) then
        VATAmount := BaseAmount * 0.25
    else
        VATAmount := BaseAmount * 0.20;

    TotalAmount := BaseAmount + VATAmount;

    // Process dimension set
    if DimensionSetID <> 0 then begin
        TempDimSetEntry.SetRange("Dimension Set ID", DimensionSetID);
        if TempDimSetEntry.FindSet() then
            repeat
                if not DimensionValue.Get(
                    TempDimSetEntry."Dimension Code",
                    TempDimSetEntry."Dimension Value Code"
                ) then
                    Error(
                        'Dimension Value %1 %2 not found',
                        TempDimSetEntry."Dimension Code",
                        TempDimSetEntry."Dimension Value Code"
                    );
            until TempDimSetEntry.Next() = 0;
    end;

    // Create journal line
    GenJournalLine.Init();
    GenJournalLine."Line No." := GetNextLineNo1();
    GenJournalLine."Document No." := DocumentNo;
    GenJournalLine."Posting Date" := PostingDate;
    GenJournalLine."Account Type" := GenJournalLine."Account Type"::"G/L Account";
    GenJournalLine."Account No." := AccountNo;
    GenJournalLine.Amount := TotalAmount;
    GenJournalLine."VAT Amount" := VATAmount;
    GenJournalLine."Dimension Set ID" := DimensionSetID;
    GenJournalLine.Insert();

    // Post the entry
    GLEntry.Init();
    if GLEntry.FindLast() then
        LastEntryNo := GLEntry."Entry No."
    else
        LastEntryNo := 0;

    GLEntry."Entry No." := LastEntryNo + 1;
    GLEntry."G/L Account No." := AccountNo;
    GLEntry."Posting Date" := PostingDate;
    GLEntry."Document Type" := GLEntry."Document Type"::Invoice;
    GLEntry."Document No." := DocumentNo;
    GLEntry.Amount := TotalAmount;
    GLEntry."VAT Amount" := VATAmount;
    GLEntry."Dimension Set ID" := DimensionSetID;
    GLEntry.Insert();

    // Perform validation loops
    for i := 1 to 10 do begin
        if GLEntry.Amount <> TotalAmount then
            Error('Amount mismatch in iteration %1', i);
    end;

    exit(true);
end;

procedure GetNextLineNo1(): Integer
var
    GenJnlLine: Record "Gen. Journal Line";
begin
    if GenJnlLine.FindLast() then
        exit(GenJnlLine."Line No." + 10000)
    else
        exit(10000);
end;

table 50002 "Custom Table 2"
{
    DataClassification = CustomerContent;

    fields
    {
        field(1; "Entry No."; Integer)
        {
            DataClassification = CustomerContent;
            AutoIncrement = true;
        }
        field(2; "Code"; Code[20])
        {
            DataClassification = CustomerContent;
        }
        field(3; "Description"; Text[100])
        {
            DataClassification = CustomerContent;
        }
        field(4; "Amount"; Decimal)
        {
            DataClassification = CustomerContent;
        }
        field(5; "Date"; Date)
        {
            DataClassification = CustomerContent;
        }
    }

    keys
    {
        key(PK; "Entry No.")
        {
            Clustered = true;
        }
    }
}

procedure ComplexBusinessLogic2(
    DocumentNo: Code[20];
    PostingDate: Date;
    AccountNo: Code[20];
    Amount: Decimal;
    DimensionSetID: Integer
): Boolean
var
    GenJournalLine: Record "Gen. Journal Line";
    GLEntry: Record "G/L Entry";
    DimensionValue: Record "Dimension Value";
    TempDimSetEntry: Record "Dimension Set Entry" temporary;
    LastEntryNo: Integer;
    VATAmount: Decimal;
    BaseAmount: Decimal;
    TotalAmount: Decimal;
    i: Integer;
begin
    // Initialize variables
    VATAmount := 0;
    BaseAmount := Amount;
    TotalAmount := 0;

    // Validate account
    if AccountNo = '' then
        Error('Account number cannot be empty');

    // Check if document already posted
    GLEntry.SetRange("Document No.", DocumentNo);
    if not GLEntry.IsEmpty() then
        Error('Document %1 already posted', DocumentNo);

    // Calculate VAT
    if PostingDate >= DMY2Date(1, 1, 2024) then
        VATAmount := BaseAmount * 0.25
    else
        VATAmount := BaseAmount * 0.20;

    TotalAmount := BaseAmount + VATAmount;

    // Process dimension set
    if DimensionSetID <> 0 then begin
        TempDimSetEntry.SetRange("Dimension Set ID", DimensionSetID);
        if TempDimSetEntry.FindSet() then
            repeat
                if not DimensionValue.Get(
                    TempDimSetEntry."Dimension Code",
                    TempDimSetEntry."Dimension Value Code"
                ) then
                    Error(
                        'Dimension Value %1 %2 not found',
                        TempDimSetEntry."Dimension Code",
                        TempDimSetEntry."Dimension Value Code"
                    );
            until TempDimSetEntry.Next() = 0;
    end;

    // Create journal line
    GenJournalLine.Init();
    GenJournalLine."Line No." := GetNextLineNo2();
    GenJournalLine."Document No." := DocumentNo;
    GenJournalLine."Posting Date" := PostingDate;
    GenJournalLine."Account Type" := GenJournalLine."Account Type"::"G/L Account";
    GenJournalLine."Account No." := AccountNo;
    GenJournalLine.Amount := TotalAmount;
    GenJournalLine."VAT Amount" := VATAmount;
    GenJournalLine."Dimension Set ID" := DimensionSetID;
    GenJournalLine.Insert();

    // Post the entry
    GLEntry.Init();
    if GLEntry.FindLast() then
        LastEntryNo := GLEntry."Entry No."
    else
        LastEntryNo := 0;

    GLEntry."Entry No." := LastEntryNo + 1;
    GLEntry."G/L Account No." := AccountNo;
    GLEntry."Posting Date" := PostingDate;
    GLEntry."Document Type" := GLEntry."Document Type"::Invoice;
    GLEntry."Document No." := DocumentNo;
    GLEntry.Amount := TotalAmount;
    GLEntry."VAT Amount" := VATAmount;
    GLEntry."Dimension Set ID" := DimensionSetID;
    GLEntry.Insert();

    // Perform validation loops
    for i := 1 to 10 do begin
        if GLEntry.Amount <> TotalAmount then
            Error('Amount mismatch in iteration %1', i);
    end;

    exit(true);
end;

procedure GetNextLineNo2(): Integer
var
    GenJnlLine: Record "Gen. Journal Line";
begin
    if GenJnlLine.FindLast() then
        exit(GenJnlLine."Line No." + 10000)
    else
        exit(10000);
end;

table 50003 "Custom Table 3"
{
    DataClassification = CustomerContent;

    fields
    {
        field(1; "Entry No."; Integer)
        {
            DataClassification = CustomerContent;
            AutoIncrement = true;
        }
        field(2; "Code"; Code[20])
        {
            DataClassification = CustomerContent;
        }
        field(3; "Description"; Text[100])
        {
            DataClassification = CustomerContent;
        }
        field(4; "Amount"; Decimal)
        {
            DataClassification = CustomerContent;
        }
        field(5; "Date"; Date)
        {
            DataClassification = CustomerContent;
        }
    }

    keys
    {
        key(PK; "Entry No.")
        {
            Clustered = true;
        }
    }
}

page 50001 "Custom List 1"
{
    PageType = List;
    SourceTable = "Custom Table 1";
    UsageCategory = Lists;
    ApplicationArea = All;

    layout
    {
        area(Content)
        {
            repeater(Group)
            {
                field("Entry No."; "Entry No.")
                {
                    ApplicationArea = All;
                }
                field("Code"; "Code")
                {
                    ApplicationArea = All;
                }
                field("Description"; "Description")
                {
                    ApplicationArea = All;
                }
                field("Amount"; "Amount")
                {
                    ApplicationArea = All;
                }
            }
        }
    }
}

procedure ComplexBusinessLogic3(
    DocumentNo: Code[20];
    PostingDate: Date;
    AccountNo: Code[20];
    Amount: Decimal;
    DimensionSetID: Integer
): Boolean
var
    GenJournalLine: Record "Gen. Journal Line";
    GLEntry: Record "G/L Entry";
    DimensionValue: Record "Dimension Value";
    TempDimSetEntry: Record "Dimension Set Entry" temporary;
    LastEntryNo: Integer;
    VATAmount: Decimal;
    BaseAmount: Decimal;
    TotalAmount: Decimal;
    i: Integer;
begin
    // Initialize variables
    VATAmount := 0;
    BaseAmount := Amount;
    TotalAmount := 0;

    // Validate account
    if AccountNo = '' then
        Error('Account number cannot be empty');

    // Check if document already posted
    GLEntry.SetRange("Document No.", DocumentNo);
    if not GLEntry.IsEmpty() then
        Error('Document %1 already posted', DocumentNo);

    // Calculate VAT
    if PostingDate >= DMY2Date(1, 1, 2024) then
        VATAmount := BaseAmount * 0.25
    else
        VATAmount := BaseAmount * 0.20;

    TotalAmount := BaseAmount + VATAmount;

    // Process dimension set
    if DimensionSetID <> 0 then begin
        TempDimSetEntry.SetRange("Dimension Set ID", DimensionSetID);
        if TempDimSetEntry.FindSet() then
            repeat
                if not DimensionValue.Get(
                    TempDimSetEntry."Dimension Code",
                    TempDimSetEntry."Dimension Value Code"
                ) then
                    Error(
                        'Dimension Value %1 %2 not found',
                        TempDimSetEntry."Dimension Code",
                        TempDimSetEntry."Dimension Value Code"
                    );
            until TempDimSetEntry.Next() = 0;
    end;

    // Create journal line
    GenJournalLine.Init();
    GenJournalLine."Line No." := GetNextLineNo3();
    GenJournalLine."Document No." := DocumentNo;
    GenJournalLine."Posting Date" := PostingDate;
    GenJournalLine."Account Type" := GenJournalLine."Account Type"::"G/L Account";
    GenJournalLine."Account No." := AccountNo;
    GenJournalLine.Amount := TotalAmount;
    GenJournalLine."VAT Amount" := VATAmount;
    GenJournalLine."Dimension Set ID" := DimensionSetID;
    GenJournalLine.Insert();

    // Post the entry
    GLEntry.Init();
    if GLEntry.FindLast() then
        LastEntryNo := GLEntry."Entry No."
    else
        LastEntryNo := 0;

    GLEntry."Entry No." := LastEntryNo + 1;
    GLEntry."G/L Account No." := AccountNo;
    GLEntry."Posting Date" := PostingDate;
    GLEntry."Document Type" := GLEntry."Document Type"::Invoice;
    GLEntry."Document No." := DocumentNo;
    GLEntry.Amount := TotalAmount;
    GLEntry."VAT Amount" := VATAmount;
    GLEntry."Dimension Set ID" := DimensionSetID;
    GLEntry.Insert();

    // Perform validation loops
    for i := 1 to 10 do begin
        if GLEntry.Amount <> TotalAmount then
            Error('Amount mismatch in iteration %1', i);
    end;

    exit(true);
end;

procedure GetNextLineNo3(): Integer
var
    GenJnlLine: Record "Gen. Journal Line";
begin
    if GenJnlLine.FindLast() then
        exit(GenJnlLine."Line No." + 10000)
    else
        exit(10000);
end;

procedure ComplexBusinessLogic4(
    DocumentNo: Code[20];
    PostingDate: Date;
    AccountNo: Code[20];
    Amount: Decimal;
    DimensionSetID: Integer
): Boolean
var
    GenJournalLine: Record "Gen. Journal Line";
    GLEntry: Record "G/L Entry";
    DimensionValue: Record "Dimension Value";
    TempDimSetEntry: Record "Dimension Set Entry" temporary;
    LastEntryNo: Integer;
    VATAmount: Decimal;
    BaseAmount: Decimal;
    TotalAmount: Decimal;
    i: Integer;
begin
    // Initialize variables
    VATAmount := 0;
    BaseAmount := Amount;
    TotalAmount := 0;

    // Validate account
    if AccountNo = '' then
        Error('Account number cannot be empty');

    // Check if document already posted
    GLEntry.SetRange("Document No.", DocumentNo);
    if not GLEntry.IsEmpty() then
        Error('Document %1 already posted', DocumentNo);

    // Calculate VAT
    if PostingDate >= DMY2Date(1, 1, 2024) then
        VATAmount := BaseAmount * 0.25
    else
        VATAmount := BaseAmount * 0.20;

    TotalAmount := BaseAmount + VATAmount;

    // Process dimension set
    if DimensionSetID <> 0 then begin
        TempDimSetEntry.SetRange("Dimension Set ID", DimensionSetID);
        if TempDimSetEntry.FindSet() then
            repeat
                if not DimensionValue.Get(
                    TempDimSetEntry."Dimension Code",
                    TempDimSetEntry."Dimension Value Code"
                ) then
                    Error(
                        'Dimension Value %1 %2 not found',
                        TempDimSetEntry."Dimension Code",
                        TempDimSetEntry."Dimension Value Code"
                    );
            until TempDimSetEntry.Next() = 0;
    end;

    // Create journal line
    GenJournalLine.Init();
    GenJournalLine."Line No." := GetNextLineNo4();
    GenJournalLine."Document No." := DocumentNo;
    GenJournalLine."Posting Date" := PostingDate;
    GenJournalLine."Account Type" := GenJournalLine."Account Type"::"G/L Account";
    GenJournalLine."Account No." := AccountNo;
    GenJournalLine.Amount := TotalAmount;
    GenJournalLine."VAT Amount" := VATAmount;
    GenJournalLine."Dimension Set ID" := DimensionSetID;
    GenJournalLine.Insert();

    // Post the entry
    GLEntry.Init();
    if GLEntry.FindLast() then
        LastEntryNo := GLEntry."Entry No."
    else
        LastEntryNo := 0;

    GLEntry."Entry No." := LastEntryNo + 1;
    GLEntry."G/L Account No." := AccountNo;
    GLEntry."Posting Date" := PostingDate;
    GLEntry."Document Type" := GLEntry."Document Type"::Invoice;
    GLEntry."Document No." := DocumentNo;
    GLEntry.Amount := TotalAmount;
    GLEntry."VAT Amount" := VATAmount;
    GLEntry."Dimension Set ID" := DimensionSetID;
    GLEntry.Insert();

    // Perform validation loops
    for i := 1 to 10 do begin
        if GLEntry.Amount <> TotalAmount then
            Error('Amount mismatch in iteration %1', i);
    end;

    exit(true);
end;

procedure GetNextLineNo4(): Integer
var
    GenJnlLine: Record "Gen. Journal Line";
begin
    if GenJnlLine.FindLast() then
        exit(GenJnlLine."Line No." + 10000)
    else
        exit(10000);
end;

page 50002 "Custom List 2"
{
    PageType = List;
    SourceTable = "Custom Table 2";
    UsageCategory = Lists;
    ApplicationArea = All;

    layout
    {
        area(Content)
        {
            repeater(Group)
            {
                field("Entry No."; "Entry No.")
                {
                    ApplicationArea = All;
                }
                field("Code"; "Code")
                {
                    ApplicationArea = All;
                }
                field("Description"; "Description")
                {
                    ApplicationArea = All;
                }
                field("Amount"; "Amount")
                {
                    ApplicationArea = All;
                }
            }
        }
    }
}

table 50004 "Custom Table 4"
{
    DataClassification = CustomerContent;

    fields
    {
        field(1; "Entry No."; Integer)
        {
            DataClassification = CustomerContent;
            AutoIncrement = true;
        }
        field(2; "Code"; Code[20])
        {
            DataClassification = CustomerContent;
        }
        field(3; "Description"; Text[100])
        {
            DataClassification = CustomerContent;
        }
        field(4; "Amount"; Decimal)
        {
            DataClassification = CustomerContent;
        }
        field(5; "Date"; Date)
        {
            DataClassification = CustomerContent;
        }
    }

    keys
    {
        key(PK; "Entry No.")
        {
            Clustered = true;
        }
    }
}

procedure ComplexBusinessLogic5(
    DocumentNo: Code[20];
    PostingDate: Date;
    AccountNo: Code[20];
    Amount: Decimal;
    DimensionSetID: Integer
): Boolean
var
    GenJournalLine: Record "Gen. Journal Line";
    GLEntry: Record "G/L Entry";
    DimensionValue: Record "Dimension Value";
    TempDimSetEntry: Record "Dimension Set Entry" temporary;
    LastEntryNo: Integer;
    VATAmount: Decimal;
    BaseAmount: Decimal;
    TotalAmount: Decimal;
    i: Integer;
begin
    // Initialize variables
    VATAmount := 0;
    BaseAmount := Amount;
    TotalAmount := 0;

    // Validate account
    if AccountNo = '' then
        Error('Account number cannot be empty');

    // Check if document already posted
    GLEntry.SetRange("Document No.", DocumentNo);
    if not GLEntry.IsEmpty() then
        Error('Document %1 already posted', DocumentNo);

    // Calculate VAT
    if PostingDate >= DMY2Date(1, 1, 2024) then
        VATAmount := BaseAmount * 0.25
    else
        VATAmount := BaseAmount * 0.20;

    TotalAmount := BaseAmount + VATAmount;

    // Process dimension set
    if DimensionSetID <> 0 then begin
        TempDimSetEntry.SetRange("Dimension Set ID", DimensionSetID);
        if TempDimSetEntry.FindSet() then
            repeat
                if not DimensionValue.Get(
                    TempDimSetEntry."Dimension Code",
                    TempDimSetEntry."Dimension Value Code"
                ) then
                    Error(
                        'Dimension Value %1 %2 not found',
                        TempDimSetEntry."Dimension Code",
                        TempDimSetEntry."Dimension Value Code"
                    );
            until TempDimSetEntry.Next() = 0;
    end;

    // Create journal line
    GenJournalLine.Init();
    GenJournalLine."Line No." := GetNextLineNo5();
    GenJournalLine."Document No." := DocumentNo;
    GenJournalLine."Posting Date" := PostingDate;
    GenJournalLine."Account Type" := GenJournalLine."Account Type"::"G/L Account";
    GenJournalLine."Account No." := AccountNo;
    GenJournalLine.Amount := TotalAmount;
    GenJournalLine."VAT Amount" := VATAmount;
    GenJournalLine."Dimension Set ID" := DimensionSetID;
    GenJournalLine.Insert();

    // Post the entry
    GLEntry.Init();
    if GLEntry.FindLast() then
        LastEntryNo := GLEntry."Entry No."
    else
        LastEntryNo := 0;

    GLEntry."Entry No." := LastEntryNo + 1;
    GLEntry."G/L Account No." := AccountNo;
    GLEntry."Posting Date" := PostingDate;
    GLEntry."Document Type" := GLEntry."Document Type"::Invoice;
    GLEntry."Document No." := DocumentNo;
    GLEntry.Amount := TotalAmount;
    GLEntry."VAT Amount" := VATAmount;
    GLEntry."Dimension Set ID" := DimensionSetID;
    GLEntry.Insert();

    // Perform validation loops
    for i := 1 to 10 do begin
        if GLEntry.Amount <> TotalAmount then
            Error('Amount mismatch in iteration %1', i);
    end;

    exit(true);
end;

procedure GetNextLineNo5(): Integer
var
    GenJnlLine: Record "Gen. Journal Line";
begin
    if GenJnlLine.FindLast() then
        exit(GenJnlLine."Line No." + 10000)
    else
        exit(10000);
end;

procedure ComplexBusinessLogic6(
    DocumentNo: Code[20];
    PostingDate: Date;
    AccountNo: Code[20];
    Amount: Decimal;
    DimensionSetID: Integer
): Boolean
var
    GenJournalLine: Record "Gen. Journal Line";
    GLEntry: Record "G/L Entry";
    DimensionValue: Record "Dimension Value";
    TempDimSetEntry: Record "Dimension Set Entry" temporary;
    LastEntryNo: Integer;
    VATAmount: Decimal;
    BaseAmount: Decimal;
    TotalAmount: Decimal;
    i: Integer;
begin
    // Initialize variables
    VATAmount := 0;
    BaseAmount := Amount;
    TotalAmount := 0;

    // Validate account
    if AccountNo = '' then
        Error('Account number cannot be empty');

    // Check if document already posted
    GLEntry.SetRange("Document No.", DocumentNo);
    if not GLEntry.IsEmpty() then
        Error('Document %1 already posted', DocumentNo);

    // Calculate VAT
    if PostingDate >= DMY2Date(1, 1, 2024) then
        VATAmount := BaseAmount * 0.25
    else
        VATAmount := BaseAmount * 0.20;

    TotalAmount := BaseAmount + VATAmount;

    // Process dimension set
    if DimensionSetID <> 0 then begin
        TempDimSetEntry.SetRange("Dimension Set ID", DimensionSetID);
        if TempDimSetEntry.FindSet() then
            repeat
                if not DimensionValue.Get(
                    TempDimSetEntry."Dimension Code",
                    TempDimSetEntry."Dimension Value Code"
                ) then
                    Error(
                        'Dimension Value %1 %2 not found',
                        TempDimSetEntry."Dimension Code",
                        TempDimSetEntry."Dimension Value Code"
                    );
            until TempDimSetEntry.Next() = 0;
    end;

    // Create journal line
    GenJournalLine.Init();
    GenJournalLine."Line No." := GetNextLineNo6();
    GenJournalLine."Document No." := DocumentNo;
    GenJournalLine."Posting Date" := PostingDate;
    GenJournalLine."Account Type" := GenJournalLine."Account Type"::"G/L Account";
    GenJournalLine."Account No." := AccountNo;
    GenJournalLine.Amount := TotalAmount;
    GenJournalLine."VAT Amount" := VATAmount;
    GenJournalLine."Dimension Set ID" := DimensionSetID;
    GenJournalLine.Insert();

    // Post the entry
    GLEntry.Init();
    if GLEntry.FindLast() then
        LastEntryNo := GLEntry."Entry No."
    else
        LastEntryNo := 0;

    GLEntry."Entry No." := LastEntryNo + 1;
    GLEntry."G/L Account No." := AccountNo;
    GLEntry."Posting Date" := PostingDate;
    GLEntry."Document Type" := GLEntry."Document Type"::Invoice;
    GLEntry."Document No." := DocumentNo;
    GLEntry.Amount := TotalAmount;
    GLEntry."VAT Amount" := VATAmount;
    GLEntry."Dimension Set ID" := DimensionSetID;
    GLEntry.Insert();

    // Perform validation loops
    for i := 1 to 10 do begin
        if GLEntry.Amount <> TotalAmount then
            Error('Amount mismatch in iteration %1', i);
    end;

    exit(true);
end;

procedure GetNextLineNo6(): Integer
var
    GenJnlLine: Record "Gen. Journal Line";
begin
    if GenJnlLine.FindLast() then
        exit(GenJnlLine."Line No." + 10000)
    else
        exit(10000);
end;

table 50005 "Custom Table 5"
{
    DataClassification = CustomerContent;

    fields
    {
        field(1; "Entry No."; Integer)
        {
            DataClassification = CustomerContent;
            AutoIncrement = true;
        }
        field(2; "Code"; Code[20])
        {
            DataClassification = CustomerContent;
        }
        field(3; "Description"; Text[100])
        {
            DataClassification = CustomerContent;
        }
        field(4; "Amount"; Decimal)
        {
            DataClassification = CustomerContent;
        }
        field(5; "Date"; Date)
        {
            DataClassification = CustomerContent;
        }
    }

    keys
    {
        key(PK; "Entry No.")
        {
            Clustered = true;
        }
    }
}

table 50006 "Custom Table 6"
{
    DataClassification = CustomerContent;

    fields
    {
        field(1; "Entry No."; Integer)
        {
            DataClassification = CustomerContent;
            AutoIncrement = true;
        }
        field(2; "Code"; Code[20])
        {
            DataClassification = CustomerContent;
        }
        field(3; "Description"; Text[100])
        {
            DataClassification = CustomerContent;
        }
        field(4; "Amount"; Decimal)
        {
            DataClassification = CustomerContent;
        }
        field(5; "Date"; Date)
        {
            DataClassification = CustomerContent;
        }
    }

    keys
    {
        key(PK; "Entry No.")
        {
            Clustered = true;
        }
    }
}

table 50007 "Custom Table 7"
{
    DataClassification = CustomerContent;

    fields
    {
        field(1; "Entry No."; Integer)
        {
            DataClassification = CustomerContent;
            AutoIncrement = true;
        }
        field(2; "Code"; Code[20])
        {
            DataClassification = CustomerContent;
        }
        field(3; "Description"; Text[100])
        {
            DataClassification = CustomerContent;
        }
        field(4; "Amount"; Decimal)
        {
            DataClassification = CustomerContent;
        }
        field(5; "Date"; Date)
        {
            DataClassification = CustomerContent;
        }
    }

    keys
    {
        key(PK; "Entry No.")
        {
            Clustered = true;
        }
    }
}

procedure ComplexBusinessLogic7(
    DocumentNo: Code[20];
    PostingDate: Date;
    AccountNo: Code[20];
    Amount: Decimal;
    DimensionSetID: Integer
): Boolean
var
    GenJournalLine: Record "Gen. Journal Line";
    GLEntry: Record "G/L Entry";
    DimensionValue: Record "Dimension Value";
    TempDimSetEntry: Record "Dimension Set Entry" temporary;
    LastEntryNo: Integer;
    VATAmount: Decimal;
    BaseAmount: Decimal;
    TotalAmount: Decimal;
    i: Integer;
begin
    // Initialize variables
    VATAmount := 0;
    BaseAmount := Amount;
    TotalAmount := 0;

    // Validate account
    if AccountNo = '' then
        Error('Account number cannot be empty');

    // Check if document already posted
    GLEntry.SetRange("Document No.", DocumentNo);
    if not GLEntry.IsEmpty() then
        Error('Document %1 already posted', DocumentNo);

    // Calculate VAT
    if PostingDate >= DMY2Date(1, 1, 2024) then
        VATAmount := BaseAmount * 0.25
    else
        VATAmount := BaseAmount * 0.20;

    TotalAmount := BaseAmount + VATAmount;

    // Process dimension set
    if DimensionSetID <> 0 then begin
        TempDimSetEntry.SetRange("Dimension Set ID", DimensionSetID);
        if TempDimSetEntry.FindSet() then
            repeat
                if not DimensionValue.Get(
                    TempDimSetEntry."Dimension Code",
                    TempDimSetEntry."Dimension Value Code"
                ) then
                    Error(
                        'Dimension Value %1 %2 not found',
                        TempDimSetEntry."Dimension Code",
                        TempDimSetEntry."Dimension Value Code"
                    );
            until TempDimSetEntry.Next() = 0;
    end;

    // Create journal line
    GenJournalLine.Init();
    GenJournalLine."Line No." := GetNextLineNo7();
    GenJournalLine."Document No." := DocumentNo;
    GenJournalLine."Posting Date" := PostingDate;
    GenJournalLine."Account Type" := GenJournalLine."Account Type"::"G/L Account";
    GenJournalLine."Account No." := AccountNo;
    GenJournalLine.Amount := TotalAmount;
    GenJournalLine."VAT Amount" := VATAmount;
    GenJournalLine."Dimension Set ID" := DimensionSetID;
    GenJournalLine.Insert();

    // Post the entry
    GLEntry.Init();
    if GLEntry.FindLast() then
        LastEntryNo := GLEntry."Entry No."
    else
        LastEntryNo := 0;

    GLEntry."Entry No." := LastEntryNo + 1;
    GLEntry."G/L Account No." := AccountNo;
    GLEntry."Posting Date" := PostingDate;
    GLEntry."Document Type" := GLEntry."Document Type"::Invoice;
    GLEntry."Document No." := DocumentNo;
    GLEntry.Amount := TotalAmount;
    GLEntry."VAT Amount" := VATAmount;
    GLEntry."Dimension Set ID" := DimensionSetID;
    GLEntry.Insert();

    // Perform validation loops
    for i := 1 to 10 do begin
        if GLEntry.Amount <> TotalAmount then
            Error('Amount mismatch in iteration %1', i);
    end;

    exit(true);
end;

procedure GetNextLineNo7(): Integer
var
    GenJnlLine: Record "Gen. Journal Line";
begin
    if GenJnlLine.FindLast() then
        exit(GenJnlLine."Line No." + 10000)
    else
        exit(10000);
end;

procedure ComplexBusinessLogic8(
    DocumentNo: Code[20];
    PostingDate: Date;
    AccountNo: Code[20];
    Amount: Decimal;
    DimensionSetID: Integer
): Boolean
var
    GenJournalLine: Record "Gen. Journal Line";
    GLEntry: Record "G/L Entry";
    DimensionValue: Record "Dimension Value";
    TempDimSetEntry: Record "Dimension Set Entry" temporary;
    LastEntryNo: Integer;
    VATAmount: Decimal;
    BaseAmount: Decimal;
    TotalAmount: Decimal;
    i: Integer;
begin
    // Initialize variables
    VATAmount := 0;
    BaseAmount := Amount;
    TotalAmount := 0;

    // Validate account
    if AccountNo = '' then
        Error('Account number cannot be empty');

    // Check if document already posted
    GLEntry.SetRange("Document No.", DocumentNo);
    if not GLEntry.IsEmpty() then
        Error('Document %1 already posted', DocumentNo);

    // Calculate VAT
    if PostingDate >= DMY2Date(1, 1, 2024) then
        VATAmount := BaseAmount * 0.25
    else
        VATAmount := BaseAmount * 0.20;

    TotalAmount := BaseAmount + VATAmount;

    // Process dimension set
    if DimensionSetID <> 0 then begin
        TempDimSetEntry.SetRange("Dimension Set ID", DimensionSetID);
        if TempDimSetEntry.FindSet() then
            repeat
                if not DimensionValue.Get(
                    TempDimSetEntry."Dimension Code",
                    TempDimSetEntry."Dimension Value Code"
                ) then
                    Error(
                        'Dimension Value %1 %2 not found',
                        TempDimSetEntry."Dimension Code",
                        TempDimSetEntry."Dimension Value Code"
                    );
            until TempDimSetEntry.Next() = 0;
    end;

    // Create journal line
    GenJournalLine.Init();
    GenJournalLine."Line No." := GetNextLineNo8();
    GenJournalLine."Document No." := DocumentNo;
    GenJournalLine."Posting Date" := PostingDate;
    GenJournalLine."Account Type" := GenJournalLine."Account Type"::"G/L Account";
    GenJournalLine."Account No." := AccountNo;
    GenJournalLine.Amount := TotalAmount;
    GenJournalLine."VAT Amount" := VATAmount;
    GenJournalLine."Dimension Set ID" := DimensionSetID;
    GenJournalLine.Insert();

    // Post the entry
    GLEntry.Init();
    if GLEntry.FindLast() then
        LastEntryNo := GLEntry."Entry No."
    else
        LastEntryNo := 0;

    GLEntry."Entry No." := LastEntryNo + 1;
    GLEntry."G/L Account No." := AccountNo;
    GLEntry."Posting Date" := PostingDate;
    GLEntry."Document Type" := GLEntry."Document Type"::Invoice;
    GLEntry."Document No." := DocumentNo;
    GLEntry.Amount := TotalAmount;
    GLEntry."VAT Amount" := VATAmount;
    GLEntry."Dimension Set ID" := DimensionSetID;
    GLEntry.Insert();

    // Perform validation loops
    for i := 1 to 10 do begin
        if GLEntry.Amount <> TotalAmount then
            Error('Amount mismatch in iteration %1', i);
    end;

    exit(true);
end;

procedure GetNextLineNo8(): Integer
var
    GenJnlLine: Record "Gen. Journal Line";
begin
    if GenJnlLine.FindLast() then
        exit(GenJnlLine."Line No." + 10000)
    else
        exit(10000);
end;

page 50003 "Custom List 3"
{
    PageType = List;
    SourceTable = "Custom Table 3";
    UsageCategory = Lists;
    ApplicationArea = All;

    layout
    {
        area(Content)
        {
            repeater(Group)
            {
                field("Entry No."; "Entry No.")
                {
                    ApplicationArea = All;
                }
                field("Code"; "Code")
                {
                    ApplicationArea = All;
                }
                field("Description"; "Description")
                {
                    ApplicationArea = All;
                }
                field("Amount"; "Amount")
                {
                    ApplicationArea = All;
                }
            }
        }
    }
}

procedure ComplexBusinessLogic9(
    DocumentNo: Code[20];
    PostingDate: Date;
    AccountNo: Code[20];
    Amount: Decimal;
    DimensionSetID: Integer
): Boolean
var
    GenJournalLine: Record "Gen. Journal Line";
    GLEntry: Record "G/L Entry";
    DimensionValue: Record "Dimension Value";
    TempDimSetEntry: Record "Dimension Set Entry" temporary;
    LastEntryNo: Integer;
    VATAmount: Decimal;
    BaseAmount: Decimal;
    TotalAmount: Decimal;
    i: Integer;
begin
    // Initialize variables
    VATAmount := 0;
    BaseAmount := Amount;
    TotalAmount := 0;

    // Validate account
    if AccountNo = '' then
        Error('Account number cannot be empty');

    // Check if document already posted
    GLEntry.SetRange("Document No.", DocumentNo);
    if not GLEntry.IsEmpty() then
        Error('Document %1 already posted', DocumentNo);

    // Calculate VAT
    if PostingDate >= DMY2Date(1, 1, 2024) then
        VATAmount := BaseAmount * 0.25
    else
        VATAmount := BaseAmount * 0.20;

    TotalAmount := BaseAmount + VATAmount;

    // Process dimension set
    if DimensionSetID <> 0 then begin
        TempDimSetEntry.SetRange("Dimension Set ID", DimensionSetID);
        if TempDimSetEntry.FindSet() then
            repeat
                if not DimensionValue.Get(
                    TempDimSetEntry."Dimension Code",
                    TempDimSetEntry."Dimension Value Code"
                ) then
                    Error(
                        'Dimension Value %1 %2 not found',
                        TempDimSetEntry."Dimension Code",
                        TempDimSetEntry."Dimension Value Code"
                    );
            until TempDimSetEntry.Next() = 0;
    end;

    // Create journal line
    GenJournalLine.Init();
    GenJournalLine."Line No." := GetNextLineNo9();
    GenJournalLine."Document No." := DocumentNo;
    GenJournalLine."Posting Date" := PostingDate;
    GenJournalLine."Account Type" := GenJournalLine."Account Type"::"G/L Account";
    GenJournalLine."Account No." := AccountNo;
    GenJournalLine.Amount := TotalAmount;
    GenJournalLine."VAT Amount" := VATAmount;
    GenJournalLine."Dimension Set ID" := DimensionSetID;
    GenJournalLine.Insert();

    // Post the entry
    GLEntry.Init();
    if GLEntry.FindLast() then
        LastEntryNo := GLEntry."Entry No."
    else
        LastEntryNo := 0;

    GLEntry."Entry No." := LastEntryNo + 1;
    GLEntry."G/L Account No." := AccountNo;
    GLEntry."Posting Date" := PostingDate;
    GLEntry."Document Type" := GLEntry."Document Type"::Invoice;
    GLEntry."Document No." := DocumentNo;
    GLEntry.Amount := TotalAmount;
    GLEntry."VAT Amount" := VATAmount;
    GLEntry."Dimension Set ID" := DimensionSetID;
    GLEntry.Insert();

    // Perform validation loops
    for i := 1 to 10 do begin
        if GLEntry.Amount <> TotalAmount then
            Error('Amount mismatch in iteration %1', i);
    end;

    exit(true);
end;

procedure GetNextLineNo9(): Integer
var
    GenJnlLine: Record "Gen. Journal Line";
begin
    if GenJnlLine.FindLast() then
        exit(GenJnlLine."Line No." + 10000)
    else
        exit(10000);
end;

table 50008 "Custom Table 8"
{
    DataClassification = CustomerContent;

    fields
    {
        field(1; "Entry No."; Integer)
        {
            DataClassification = CustomerContent;
            AutoIncrement = true;
        }
        field(2; "Code"; Code[20])
        {
            DataClassification = CustomerContent;
        }
        field(3; "Description"; Text[100])
        {
            DataClassification = CustomerContent;
        }
        field(4; "Amount"; Decimal)
        {
            DataClassification = CustomerContent;
        }
        field(5; "Date"; Date)
        {
            DataClassification = CustomerContent;
        }
    }

    keys
    {
        key(PK; "Entry No.")
        {
            Clustered = true;
        }
    }
}

procedure ComplexBusinessLogic10(
    DocumentNo: Code[20];
    PostingDate: Date;
    AccountNo: Code[20];
    Amount: Decimal;
    DimensionSetID: Integer
): Boolean
var
    GenJournalLine: Record "Gen. Journal Line";
    GLEntry: Record "G/L Entry";
    DimensionValue: Record "Dimension Value";
    TempDimSetEntry: Record "Dimension Set Entry" temporary;
    LastEntryNo: Integer;
    VATAmount: Decimal;
    BaseAmount: Decimal;
    TotalAmount: Decimal;
    i: Integer;
begin
    // Initialize variables
    VATAmount := 0;
    BaseAmount := Amount;
    TotalAmount := 0;

    // Validate account
    if AccountNo = '' then
        Error('Account number cannot be empty');

    // Check if document already posted
    GLEntry.SetRange("Document No.", DocumentNo);
    if not GLEntry.IsEmpty() then
        Error('Document %1 already posted', DocumentNo);

    // Calculate VAT
    if PostingDate >= DMY2Date(1, 1, 2024) then
        VATAmount := BaseAmount * 0.25
    else
        VATAmount := BaseAmount * 0.20;

    TotalAmount := BaseAmount + VATAmount;

    // Process dimension set
    if DimensionSetID <> 0 then begin
        TempDimSetEntry.SetRange("Dimension Set ID", DimensionSetID);
        if TempDimSetEntry.FindSet() then
            repeat
                if not DimensionValue.Get(
                    TempDimSetEntry."Dimension Code",
                    TempDimSetEntry."Dimension Value Code"
                ) then
                    Error(
                        'Dimension Value %1 %2 not found',
                        TempDimSetEntry."Dimension Code",
                        TempDimSetEntry."Dimension Value Code"
                    );
            until TempDimSetEntry.Next() = 0;
    end;

    // Create journal line
    GenJournalLine.Init();
    GenJournalLine."Line No." := GetNextLineNo10();
    GenJournalLine."Document No." := DocumentNo;
    GenJournalLine."Posting Date" := PostingDate;
    GenJournalLine."Account Type" := GenJournalLine."Account Type"::"G/L Account";
    GenJournalLine."Account No." := AccountNo;
    GenJournalLine.Amount := TotalAmount;
    GenJournalLine."VAT Amount" := VATAmount;
    GenJournalLine."Dimension Set ID" := DimensionSetID;
    GenJournalLine.Insert();

    // Post the entry
    GLEntry.Init();
    if GLEntry.FindLast() then
        LastEntryNo := GLEntry."Entry No."
    else
        LastEntryNo := 0;

    GLEntry."Entry No." := LastEntryNo + 1;
    GLEntry."G/L Account No." := AccountNo;
    GLEntry."Posting Date" := PostingDate;
    GLEntry."Document Type" := GLEntry."Document Type"::Invoice;
    GLEntry."Document No." := DocumentNo;
    GLEntry.Amount := TotalAmount;
    GLEntry."VAT Amount" := VATAmount;
    GLEntry."Dimension Set ID" := DimensionSetID;
    GLEntry.Insert();

    // Perform validation loops
    for i := 1 to 10 do begin
        if GLEntry.Amount <> TotalAmount then
            Error('Amount mismatch in iteration %1', i);
    end;

    exit(true);
end;

procedure GetNextLineNo10(): Integer
var
    GenJnlLine: Record "Gen. Journal Line";
begin
    if GenJnlLine.FindLast() then
        exit(GenJnlLine."Line No." + 10000)
    else
        exit(10000);
end;

procedure ComplexBusinessLogic11(
    DocumentNo: Code[20];
    PostingDate: Date;
    AccountNo: Code[20];
    Amount: Decimal;
    DimensionSetID: Integer
): Boolean
var
    GenJournalLine: Record "Gen. Journal Line";
    GLEntry: Record "G/L Entry";
    DimensionValue: Record "Dimension Value";
    TempDimSetEntry: Record "Dimension Set Entry" temporary;
    LastEntryNo: Integer;
    VATAmount: Decimal;
    BaseAmount: Decimal;
    TotalAmount: Decimal;
    i: Integer;
begin
    // Initialize variables
    VATAmount := 0;
    BaseAmount := Amount;
    TotalAmount := 0;

    // Validate account
    if AccountNo = '' then
        Error('Account number cannot be empty');

    // Check if document already posted
    GLEntry.SetRange("Document No.", DocumentNo);
    if not GLEntry.IsEmpty() then
        Error('Document %1 already posted', DocumentNo);

    // Calculate VAT
    if PostingDate >= DMY2Date(1, 1, 2024) then
        VATAmount := BaseAmount * 0.25
    else
        VATAmount := BaseAmount * 0.20;

    TotalAmount := BaseAmount + VATAmount;

    // Process dimension set
    if DimensionSetID <> 0 then begin
        TempDimSetEntry.SetRange("Dimension Set ID", DimensionSetID);
        if TempDimSetEntry.FindSet() then
            repeat
                if not DimensionValue.Get(
                    TempDimSetEntry."Dimension Code",
                    TempDimSetEntry."Dimension Value Code"
                ) then
                    Error(
                        'Dimension Value %1 %2 not found',
                        TempDimSetEntry."Dimension Code",
                        TempDimSetEntry."Dimension Value Code"
                    );
            until TempDimSetEntry.Next() = 0;
    end;

    // Create journal line
    GenJournalLine.Init();
    GenJournalLine."Line No." := GetNextLineNo11();
    GenJournalLine."Document No." := DocumentNo;
    GenJournalLine."Posting Date" := PostingDate;
    GenJournalLine."Account Type" := GenJournalLine."Account Type"::"G/L Account";
    GenJournalLine."Account No." := AccountNo;
    GenJournalLine.Amount := TotalAmount;
    GenJournalLine."VAT Amount" := VATAmount;
    GenJournalLine."Dimension Set ID" := DimensionSetID;
    GenJournalLine.Insert();

    // Post the entry
    GLEntry.Init();
    if GLEntry.FindLast() then
        LastEntryNo := GLEntry."Entry No."
    else
        LastEntryNo := 0;

    GLEntry."Entry No." := LastEntryNo + 1;
    GLEntry."G/L Account No." := AccountNo;
    GLEntry."Posting Date" := PostingDate;
    GLEntry."Document Type" := GLEntry."Document Type"::Invoice;
    GLEntry."Document No." := DocumentNo;
    GLEntry.Amount := TotalAmount;
    GLEntry."VAT Amount" := VATAmount;
    GLEntry."Dimension Set ID" := DimensionSetID;
    GLEntry.Insert();

    // Perform validation loops
    for i := 1 to 10 do begin
        if GLEntry.Amount <> TotalAmount then
            Error('Amount mismatch in iteration %1', i);
    end;

    exit(true);
end;

procedure GetNextLineNo11(): Integer
var
    GenJnlLine: Record "Gen. Journal Line";
begin
    if GenJnlLine.FindLast() then
        exit(GenJnlLine."Line No." + 10000)
    else
        exit(10000);
end;

procedure ComplexBusinessLogic12(
    DocumentNo: Code[20];
    PostingDate: Date;
    AccountNo: Code[20];
    Amount: Decimal;
    DimensionSetID: Integer
): Boolean
var
    GenJournalLine: Record "Gen. Journal Line";
    GLEntry: Record "G/L Entry";
    DimensionValue: Record "Dimension Value";
    TempDimSetEntry: Record "Dimension Set Entry" temporary;
    LastEntryNo: Integer;
    VATAmount: Decimal;
    BaseAmount: Decimal;
    TotalAmount: Decimal;
    i: Integer;
begin
    // Initialize variables
    VATAmount := 0;
    BaseAmount := Amount;
    TotalAmount := 0;

    // Validate account
    if AccountNo = '' then
        Error('Account number cannot be empty');

    // Check if document already posted
    GLEntry.SetRange("Document No.", DocumentNo);
    if not GLEntry.IsEmpty() then
        Error('Document %1 already posted', DocumentNo);

    // Calculate VAT
    if PostingDate >= DMY2Date(1, 1, 2024) then
        VATAmount := BaseAmount * 0.25
    else
        VATAmount := BaseAmount * 0.20;

    TotalAmount := BaseAmount + VATAmount;

    // Process dimension set
    if DimensionSetID <> 0 then begin
        TempDimSetEntry.SetRange("Dimension Set ID", DimensionSetID);
        if TempDimSetEntry.FindSet() then
            repeat
                if not DimensionValue.Get(
                    TempDimSetEntry."Dimension Code",
                    TempDimSetEntry."Dimension Value Code"
                ) then
                    Error(
                        'Dimension Value %1 %2 not found',
                        TempDimSetEntry."Dimension Code",
                        TempDimSetEntry."Dimension Value Code"
                    );
            until TempDimSetEntry.Next() = 0;
    end;

    // Create journal line
    GenJournalLine.Init();
    GenJournalLine."Line No." := GetNextLineNo12();
    GenJournalLine."Document No." := DocumentNo;
    GenJournalLine."Posting Date" := PostingDate;
    GenJournalLine."Account Type" := GenJournalLine."Account Type"::"G/L Account";
    GenJournalLine."Account No." := AccountNo;
    GenJournalLine.Amount := TotalAmount;
    GenJournalLine."VAT Amount" := VATAmount;
    GenJournalLine."Dimension Set ID" := DimensionSetID;
    GenJournalLine.Insert();

    // Post the entry
    GLEntry.Init();
    if GLEntry.FindLast() then
        LastEntryNo := GLEntry."Entry No."
    else
        LastEntryNo := 0;

    GLEntry."Entry No." := LastEntryNo + 1;
    GLEntry."G/L Account No." := AccountNo;
    GLEntry."Posting Date" := PostingDate;
    GLEntry."Document Type" := GLEntry."Document Type"::Invoice;
    GLEntry."Document No." := DocumentNo;
    GLEntry.Amount := TotalAmount;
    GLEntry."VAT Amount" := VATAmount;
    GLEntry."Dimension Set ID" := DimensionSetID;
    GLEntry.Insert();

    // Perform validation loops
    for i := 1 to 10 do begin
        if GLEntry.Amount <> TotalAmount then
            Error('Amount mismatch in iteration %1', i);
    end;

    exit(true);
end;

procedure GetNextLineNo12(): Integer
var
    GenJnlLine: Record "Gen. Journal Line";
begin
    if GenJnlLine.FindLast() then
        exit(GenJnlLine."Line No." + 10000)
    else
        exit(10000);
end;

procedure ComplexBusinessLogic13(
    DocumentNo: Code[20];
    PostingDate: Date;
    AccountNo: Code[20];
    Amount: Decimal;
    DimensionSetID: Integer
): Boolean
var
    GenJournalLine: Record "Gen. Journal Line";
    GLEntry: Record "G/L Entry";
    DimensionValue: Record "Dimension Value";
    TempDimSetEntry: Record "Dimension Set Entry" temporary;
    LastEntryNo: Integer;
    VATAmount: Decimal;
    BaseAmount: Decimal;
    TotalAmount: Decimal;
    i: Integer;
begin
    // Initialize variables
    VATAmount := 0;
    BaseAmount := Amount;
    TotalAmount := 0;

    // Validate account
    if AccountNo = '' then
        Error('Account number cannot be empty');

    // Check if document already posted
    GLEntry.SetRange("Document No.", DocumentNo);
    if not GLEntry.IsEmpty() then
        Error('Document %1 already posted', DocumentNo);

    // Calculate VAT
    if PostingDate >= DMY2Date(1, 1, 2024) then
        VATAmount := BaseAmount * 0.25
    else
        VATAmount := BaseAmount * 0.20;

    TotalAmount := BaseAmount + VATAmount;

    // Process dimension set
    if DimensionSetID <> 0 then begin
        TempDimSetEntry.SetRange("Dimension Set ID", DimensionSetID);
        if TempDimSetEntry.FindSet() then
            repeat
                if not DimensionValue.Get(
                    TempDimSetEntry."Dimension Code",
                    TempDimSetEntry."Dimension Value Code"
                ) then
                    Error(
                        'Dimension Value %1 %2 not found',
                        TempDimSetEntry."Dimension Code",
                        TempDimSetEntry."Dimension Value Code"
                    );
            until TempDimSetEntry.Next() = 0;
    end;

    // Create journal line
    GenJournalLine.Init();
    GenJournalLine."Line No." := GetNextLineNo13();
    GenJournalLine."Document No." := DocumentNo;
    GenJournalLine."Posting Date" := PostingDate;
    GenJournalLine."Account Type" := GenJournalLine."Account Type"::"G/L Account";
    GenJournalLine."Account No." := AccountNo;
    GenJournalLine.Amount := TotalAmount;
    GenJournalLine."VAT Amount" := VATAmount;
    GenJournalLine."Dimension Set ID" := DimensionSetID;
    GenJournalLine.Insert();

    // Post the entry
    GLEntry.Init();
    if GLEntry.FindLast() then
        LastEntryNo := GLEntry."Entry No."
    else
        LastEntryNo := 0;

    GLEntry."Entry No." := LastEntryNo + 1;
    GLEntry."G/L Account No." := AccountNo;
    GLEntry."Posting Date" := PostingDate;
    GLEntry."Document Type" := GLEntry."Document Type"::Invoice;
    GLEntry."Document No." := DocumentNo;
    GLEntry.Amount := TotalAmount;
    GLEntry."VAT Amount" := VATAmount;
    GLEntry."Dimension Set ID" := DimensionSetID;
    GLEntry.Insert();

    // Perform validation loops
    for i := 1 to 10 do begin
        if GLEntry.Amount <> TotalAmount then
            Error('Amount mismatch in iteration %1', i);
    end;

    exit(true);
end;

procedure GetNextLineNo13(): Integer
var
    GenJnlLine: Record "Gen. Journal Line";
begin
    if GenJnlLine.FindLast() then
        exit(GenJnlLine."Line No." + 10000)
    else
        exit(10000);
end;

page 50004 "Custom List 4"
{
    PageType = List;
    SourceTable = "Custom Table 4";
    UsageCategory = Lists;
    ApplicationArea = All;

    layout
    {
        area(Content)
        {
            repeater(Group)
            {
                field("Entry No."; "Entry No.")
                {
                    ApplicationArea = All;
                }
                field("Code"; "Code")
                {
                    ApplicationArea = All;
                }
                field("Description"; "Description")
                {
                    ApplicationArea = All;
                }
                field("Amount"; "Amount")
                {
                    ApplicationArea = All;
                }
            }
        }
    }
}

table 50009 "Custom Table 9"
{
    DataClassification = CustomerContent;

    fields
    {
        field(1; "Entry No."; Integer)
        {
            DataClassification = CustomerContent;
            AutoIncrement = true;
        }
        field(2; "Code"; Code[20])
        {
            DataClassification = CustomerContent;
        }
        field(3; "Description"; Text[100])
        {
            DataClassification = CustomerContent;
        }
        field(4; "Amount"; Decimal)
        {
            DataClassification = CustomerContent;
        }
        field(5; "Date"; Date)
        {
            DataClassification = CustomerContent;
        }
    }

    keys
    {
        key(PK; "Entry No.")
        {
            Clustered = true;
        }
    }
}

procedure ComplexBusinessLogic14(
    DocumentNo: Code[20];
    PostingDate: Date;
    AccountNo: Code[20];
    Amount: Decimal;
    DimensionSetID: Integer
): Boolean
var
    GenJournalLine: Record "Gen. Journal Line";
    GLEntry: Record "G/L Entry";
    DimensionValue: Record "Dimension Value";
    TempDimSetEntry: Record "Dimension Set Entry" temporary;
    LastEntryNo: Integer;
    VATAmount: Decimal;
    BaseAmount: Decimal;
    TotalAmount: Decimal;
    i: Integer;
begin
    // Initialize variables
    VATAmount := 0;
    BaseAmount := Amount;
    TotalAmount := 0;

    // Validate account
    if AccountNo = '' then
        Error('Account number cannot be empty');

    // Check if document already posted
    GLEntry.SetRange("Document No.", DocumentNo);
    if not GLEntry.IsEmpty() then
        Error('Document %1 already posted', DocumentNo);

    // Calculate VAT
    if PostingDate >= DMY2Date(1, 1, 2024) then
        VATAmount := BaseAmount * 0.25
    else
        VATAmount := BaseAmount * 0.20;

    TotalAmount := BaseAmount + VATAmount;

    // Process dimension set
    if DimensionSetID <> 0 then begin
        TempDimSetEntry.SetRange("Dimension Set ID", DimensionSetID);
        if TempDimSetEntry.FindSet() then
            repeat
                if not DimensionValue.Get(
                    TempDimSetEntry."Dimension Code",
                    TempDimSetEntry."Dimension Value Code"
                ) then
                    Error(
                        'Dimension Value %1 %2 not found',
                        TempDimSetEntry."Dimension Code",
                        TempDimSetEntry."Dimension Value Code"
                    );
            until TempDimSetEntry.Next() = 0;
    end;

    // Create journal line
    GenJournalLine.Init();
    GenJournalLine."Line No." := GetNextLineNo14();
    GenJournalLine."Document No." := DocumentNo;
    GenJournalLine."Posting Date" := PostingDate;
    GenJournalLine."Account Type" := GenJournalLine."Account Type"::"G/L Account";
    GenJournalLine."Account No." := AccountNo;
    GenJournalLine.Amount := TotalAmount;
    GenJournalLine."VAT Amount" := VATAmount;
    GenJournalLine."Dimension Set ID" := DimensionSetID;
    GenJournalLine.Insert();

    // Post the entry
    GLEntry.Init();
    if GLEntry.FindLast() then
        LastEntryNo := GLEntry."Entry No."
    else
        LastEntryNo := 0;

    GLEntry."Entry No." := LastEntryNo + 1;
    GLEntry."G/L Account No." := AccountNo;
    GLEntry."Posting Date" := PostingDate;
    GLEntry."Document Type" := GLEntry."Document Type"::Invoice;
    GLEntry."Document No." := DocumentNo;
    GLEntry.Amount := TotalAmount;
    GLEntry."VAT Amount" := VATAmount;
    GLEntry."Dimension Set ID" := DimensionSetID;
    GLEntry.Insert();

    // Perform validation loops
    for i := 1 to 10 do begin
        if GLEntry.Amount <> TotalAmount then
            Error('Amount mismatch in iteration %1', i);
    end;

    exit(true);
end;

procedure GetNextLineNo14(): Integer
var
    GenJnlLine: Record "Gen. Journal Line";
begin
    if GenJnlLine.FindLast() then
        exit(GenJnlLine."Line No." + 10000)
    else
        exit(10000);
end;

procedure ComplexBusinessLogic15(
    DocumentNo: Code[20];
    PostingDate: Date;
    AccountNo: Code[20];
    Amount: Decimal;
    DimensionSetID: Integer
): Boolean
var
    GenJournalLine: Record "Gen. Journal Line";
    GLEntry: Record "G/L Entry";
    DimensionValue: Record "Dimension Value";
    TempDimSetEntry: Record "Dimension Set Entry" temporary;
    LastEntryNo: Integer;
    VATAmount: Decimal;
    BaseAmount: Decimal;
    TotalAmount: Decimal;
    i: Integer;
begin
    // Initialize variables
    VATAmount := 0;
    BaseAmount := Amount;
    TotalAmount := 0;

    // Validate account
    if AccountNo = '' then
        Error('Account number cannot be empty');

    // Check if document already posted
    GLEntry.SetRange("Document No.", DocumentNo);
    if not GLEntry.IsEmpty() then
        Error('Document %1 already posted', DocumentNo);

    // Calculate VAT
    if PostingDate >= DMY2Date(1, 1, 2024) then
        VATAmount := BaseAmount * 0.25
    else
        VATAmount := BaseAmount * 0.20;

    TotalAmount := BaseAmount + VATAmount;

    // Process dimension set
    if DimensionSetID <> 0 then begin
        TempDimSetEntry.SetRange("Dimension Set ID", DimensionSetID);
        if TempDimSetEntry.FindSet() then
            repeat
                if not DimensionValue.Get(
                    TempDimSetEntry."Dimension Code",
                    TempDimSetEntry."Dimension Value Code"
                ) then
                    Error(
                        'Dimension Value %1 %2 not found',
                        TempDimSetEntry."Dimension Code",
                        TempDimSetEntry."Dimension Value Code"
                    );
            until TempDimSetEntry.Next() = 0;
    end;

    // Create journal line
    GenJournalLine.Init();
    GenJournalLine."Line No." := GetNextLineNo15();
    GenJournalLine."Document No." := DocumentNo;
    GenJournalLine."Posting Date" := PostingDate;
    GenJournalLine."Account Type" := GenJournalLine."Account Type"::"G/L Account";
    GenJournalLine."Account No." := AccountNo;
    GenJournalLine.Amount := TotalAmount;
    GenJournalLine."VAT Amount" := VATAmount;
    GenJournalLine."Dimension Set ID" := DimensionSetID;
    GenJournalLine.Insert();

    // Post the entry
    GLEntry.Init();
    if GLEntry.FindLast() then
        LastEntryNo := GLEntry."Entry No."
    else
        LastEntryNo := 0;

    GLEntry."Entry No." := LastEntryNo + 1;
    GLEntry."G/L Account No." := AccountNo;
    GLEntry."Posting Date" := PostingDate;
    GLEntry."Document Type" := GLEntry."Document Type"::Invoice;
    GLEntry."Document No." := DocumentNo;
    GLEntry.Amount := TotalAmount;
    GLEntry."VAT Amount" := VATAmount;
    GLEntry."Dimension Set ID" := DimensionSetID;
    GLEntry.Insert();

    // Perform validation loops
    for i := 1 to 10 do begin
        if GLEntry.Amount <> TotalAmount then
            Error('Amount mismatch in iteration %1', i);
    end;

    exit(true);
end;

procedure GetNextLineNo15(): Integer
var
    GenJnlLine: Record "Gen. Journal Line";
begin
    if GenJnlLine.FindLast() then
        exit(GenJnlLine."Line No." + 10000)
    else
        exit(10000);
end;

page 50005 "Custom List 5"
{
    PageType = List;
    SourceTable = "Custom Table 5";
    UsageCategory = Lists;
    ApplicationArea = All;

    layout
    {
        area(Content)
        {
            repeater(Group)
            {
                field("Entry No."; "Entry No.")
                {
                    ApplicationArea = All;
                }
                field("Code"; "Code")
                {
                    ApplicationArea = All;
                }
                field("Description"; "Description")
                {
                    ApplicationArea = All;
                }
                field("Amount"; "Amount")
                {
                    ApplicationArea = All;
                }
            }
        }
    }
}

procedure ComplexBusinessLogic16(
    DocumentNo: Code[20];
    PostingDate: Date;
    AccountNo: Code[20];
    Amount: Decimal;
    DimensionSetID: Integer
): Boolean
var
    GenJournalLine: Record "Gen. Journal Line";
    GLEntry: Record "G/L Entry";
    DimensionValue: Record "Dimension Value";
    TempDimSetEntry: Record "Dimension Set Entry" temporary;
    LastEntryNo: Integer;
    VATAmount: Decimal;
    BaseAmount: Decimal;
    TotalAmount: Decimal;
    i: Integer;
begin
    // Initialize variables
    VATAmount := 0;
    BaseAmount := Amount;
    TotalAmount := 0;

    // Validate account
    if AccountNo = '' then
        Error('Account number cannot be empty');

    // Check if document already posted
    GLEntry.SetRange("Document No.", DocumentNo);
    if not GLEntry.IsEmpty() then
        Error('Document %1 already posted', DocumentNo);

    // Calculate VAT
    if PostingDate >= DMY2Date(1, 1, 2024) then
        VATAmount := BaseAmount * 0.25
    else
        VATAmount := BaseAmount * 0.20;

    TotalAmount := BaseAmount + VATAmount;

    // Process dimension set
    if DimensionSetID <> 0 then begin
        TempDimSetEntry.SetRange("Dimension Set ID", DimensionSetID);
        if TempDimSetEntry.FindSet() then
            repeat
                if not DimensionValue.Get(
                    TempDimSetEntry."Dimension Code",
                    TempDimSetEntry."Dimension Value Code"
                ) then
                    Error(
                        'Dimension Value %1 %2 not found',
                        TempDimSetEntry."Dimension Code",
                        TempDimSetEntry."Dimension Value Code"
                    );
            until TempDimSetEntry.Next() = 0;
    end;

    // Create journal line
    GenJournalLine.Init();
    GenJournalLine."Line No." := GetNextLineNo16();
    GenJournalLine."Document No." := DocumentNo;
    GenJournalLine."Posting Date" := PostingDate;
    GenJournalLine."Account Type" := GenJournalLine."Account Type"::"G/L Account";
    GenJournalLine."Account No." := AccountNo;
    GenJournalLine.Amount := TotalAmount;
    GenJournalLine."VAT Amount" := VATAmount;
    GenJournalLine."Dimension Set ID" := DimensionSetID;
    GenJournalLine.Insert();

    // Post the entry
    GLEntry.Init();
    if GLEntry.FindLast() then
        LastEntryNo := GLEntry."Entry No."
    else
        LastEntryNo := 0;

    GLEntry."Entry No." := LastEntryNo + 1;
    GLEntry."G/L Account No." := AccountNo;
    GLEntry."Posting Date" := PostingDate;
    GLEntry."Document Type" := GLEntry."Document Type"::Invoice;
    GLEntry."Document No." := DocumentNo;
    GLEntry.Amount := TotalAmount;
    GLEntry."VAT Amount" := VATAmount;
    GLEntry."Dimension Set ID" := DimensionSetID;
    GLEntry.Insert();

    // Perform validation loops
    for i := 1 to 10 do begin
        if GLEntry.Amount <> TotalAmount then
            Error('Amount mismatch in iteration %1', i);
    end;

    exit(true);
end;

procedure GetNextLineNo16(): Integer
var
    GenJnlLine: Record "Gen. Journal Line";
begin
    if GenJnlLine.FindLast() then
        exit(GenJnlLine."Line No." + 10000)
    else
        exit(10000);
end;

page 50006 "Custom List 6"
{
    PageType = List;
    SourceTable = "Custom Table 6";
    UsageCategory = Lists;
    ApplicationArea = All;

    layout
    {
        area(Content)
        {
            repeater(Group)
            {
                field("Entry No."; "Entry No.")
                {
                    ApplicationArea = All;
                }
                field("Code"; "Code")
                {
                    ApplicationArea = All;
                }
                field("Description"; "Description")
                {
                    ApplicationArea = All;
                }
                field("Amount"; "Amount")
                {
                    ApplicationArea = All;
                }
            }
        }
    }
}

procedure ComplexBusinessLogic17(
    DocumentNo: Code[20];
    PostingDate: Date;
    AccountNo: Code[20];
    Amount: Decimal;
    DimensionSetID: Integer
): Boolean
var
    GenJournalLine: Record "Gen. Journal Line";
    GLEntry: Record "G/L Entry";
    DimensionValue: Record "Dimension Value";
    TempDimSetEntry: Record "Dimension Set Entry" temporary;
    LastEntryNo: Integer;
    VATAmount: Decimal;
    BaseAmount: Decimal;
    TotalAmount: Decimal;
    i: Integer;
begin
    // Initialize variables
    VATAmount := 0;
    BaseAmount := Amount;
    TotalAmount := 0;

    // Validate account
    if AccountNo = '' then
        Error('Account number cannot be empty');

    // Check if document already posted
    GLEntry.SetRange("Document No.", DocumentNo);
    if not GLEntry.IsEmpty() then
        Error('Document %1 already posted', DocumentNo);

    // Calculate VAT
    if PostingDate >= DMY2Date(1, 1, 2024) then
        VATAmount := BaseAmount * 0.25
    else
        VATAmount := BaseAmount * 0.20;

    TotalAmount := BaseAmount + VATAmount;

    // Process dimension set
    if DimensionSetID <> 0 then begin
        TempDimSetEntry.SetRange("Dimension Set ID", DimensionSetID);
        if TempDimSetEntry.FindSet() then
            repeat
                if not DimensionValue.Get(
                    TempDimSetEntry."Dimension Code",
                    TempDimSetEntry."Dimension Value Code"
                ) then
                    Error(
                        'Dimension Value %1 %2 not found',
                        TempDimSetEntry."Dimension Code",
                        TempDimSetEntry."Dimension Value Code"
                    );
            until TempDimSetEntry.Next() = 0;
    end;

    // Create journal line
    GenJournalLine.Init();
    GenJournalLine."Line No." := GetNextLineNo17();
    GenJournalLine."Document No." := DocumentNo;
    GenJournalLine."Posting Date" := PostingDate;
    GenJournalLine."Account Type" := GenJournalLine."Account Type"::"G/L Account";
    GenJournalLine."Account No." := AccountNo;
    GenJournalLine.Amount := TotalAmount;
    GenJournalLine."VAT Amount" := VATAmount;
    GenJournalLine."Dimension Set ID" := DimensionSetID;
    GenJournalLine.Insert();

    // Post the entry
    GLEntry.Init();
    if GLEntry.FindLast() then
        LastEntryNo := GLEntry."Entry No."
    else
        LastEntryNo := 0;

    GLEntry."Entry No." := LastEntryNo + 1;
    GLEntry."G/L Account No." := AccountNo;
    GLEntry."Posting Date" := PostingDate;
    GLEntry."Document Type" := GLEntry."Document Type"::Invoice;
    GLEntry."Document No." := DocumentNo;
    GLEntry.Amount := TotalAmount;
    GLEntry."VAT Amount" := VATAmount;
    GLEntry."Dimension Set ID" := DimensionSetID;
    GLEntry.Insert();

    // Perform validation loops
    for i := 1 to 10 do begin
        if GLEntry.Amount <> TotalAmount then
            Error('Amount mismatch in iteration %1', i);
    end;

    exit(true);
end;

procedure GetNextLineNo17(): Integer
var
    GenJnlLine: Record "Gen. Journal Line";
begin
    if GenJnlLine.FindLast() then
        exit(GenJnlLine."Line No." + 10000)
    else
        exit(10000);
end;

procedure ComplexBusinessLogic18(
    DocumentNo: Code[20];
    PostingDate: Date;
    AccountNo: Code[20];
    Amount: Decimal;
    DimensionSetID: Integer
): Boolean
var
    GenJournalLine: Record "Gen. Journal Line";
    GLEntry: Record "G/L Entry";
    DimensionValue: Record "Dimension Value";
    TempDimSetEntry: Record "Dimension Set Entry" temporary;
    LastEntryNo: Integer;
    VATAmount: Decimal;
    BaseAmount: Decimal;
    TotalAmount: Decimal;
    i: Integer;
begin
    // Initialize variables
    VATAmount := 0;
    BaseAmount := Amount;
    TotalAmount := 0;

    // Validate account
    if AccountNo = '' then
        Error('Account number cannot be empty');

    // Check if document already posted
    GLEntry.SetRange("Document No.", DocumentNo);
    if not GLEntry.IsEmpty() then
        Error('Document %1 already posted', DocumentNo);

    // Calculate VAT
    if PostingDate >= DMY2Date(1, 1, 2024) then
        VATAmount := BaseAmount * 0.25
    else
        VATAmount := BaseAmount * 0.20;

    TotalAmount := BaseAmount + VATAmount;

    // Process dimension set
    if DimensionSetID <> 0 then begin
        TempDimSetEntry.SetRange("Dimension Set ID", DimensionSetID);
        if TempDimSetEntry.FindSet() then
            repeat
                if not DimensionValue.Get(
                    TempDimSetEntry."Dimension Code",
                    TempDimSetEntry."Dimension Value Code"
                ) then
                    Error(
                        'Dimension Value %1 %2 not found',
                        TempDimSetEntry."Dimension Code",
                        TempDimSetEntry."Dimension Value Code"
                    );
            until TempDimSetEntry.Next() = 0;
    end;

    // Create journal line
    GenJournalLine.Init();
    GenJournalLine."Line No." := GetNextLineNo18();
    GenJournalLine."Document No." := DocumentNo;
    GenJournalLine."Posting Date" := PostingDate;
    GenJournalLine."Account Type" := GenJournalLine."Account Type"::"G/L Account";
    GenJournalLine."Account No." := AccountNo;
    GenJournalLine.Amount := TotalAmount;
    GenJournalLine."VAT Amount" := VATAmount;
    GenJournalLine."Dimension Set ID" := DimensionSetID;
    GenJournalLine.Insert();

    // Post the entry
    GLEntry.Init();
    if GLEntry.FindLast() then
        LastEntryNo := GLEntry."Entry No."
    else
        LastEntryNo := 0;

    GLEntry."Entry No." := LastEntryNo + 1;
    GLEntry."G/L Account No." := AccountNo;
    GLEntry."Posting Date" := PostingDate;
    GLEntry."Document Type" := GLEntry."Document Type"::Invoice;
    GLEntry."Document No." := DocumentNo;
    GLEntry.Amount := TotalAmount;
    GLEntry."VAT Amount" := VATAmount;
    GLEntry."Dimension Set ID" := DimensionSetID;
    GLEntry.Insert();

    // Perform validation loops
    for i := 1 to 10 do begin
        if GLEntry.Amount <> TotalAmount then
            Error('Amount mismatch in iteration %1', i);
    end;

    exit(true);
end;

procedure GetNextLineNo18(): Integer
var
    GenJnlLine: Record "Gen. Journal Line";
begin
    if GenJnlLine.FindLast() then
        exit(GenJnlLine."Line No." + 10000)
    else
        exit(10000);
end;

procedure ComplexBusinessLogic19(
    DocumentNo: Code[20];
    PostingDate: Date;
    AccountNo: Code[20];
    Amount: Decimal;
    DimensionSetID: Integer
): Boolean
var
    GenJournalLine: Record "Gen. Journal Line";
    GLEntry: Record "G/L Entry";
    DimensionValue: Record "Dimension Value";
    TempDimSetEntry: Record "Dimension Set Entry" temporary;
    LastEntryNo: Integer;
    VATAmount: Decimal;
    BaseAmount: Decimal;
    TotalAmount: Decimal;
    i: Integer;
begin
    // Initialize variables
    VATAmount := 0;
    BaseAmount := Amount;
    TotalAmount := 0;

    // Validate account
    if AccountNo = '' then
        Error('Account number cannot be empty');

    // Check if document already posted
    GLEntry.SetRange("Document No.", DocumentNo);
    if not GLEntry.IsEmpty() then
        Error('Document %1 already posted', DocumentNo);

    // Calculate VAT
    if PostingDate >= DMY2Date(1, 1, 2024) then
        VATAmount := BaseAmount * 0.25
    else
        VATAmount := BaseAmount * 0.20;

    TotalAmount := BaseAmount + VATAmount;

    // Process dimension set
    if DimensionSetID <> 0 then begin
        TempDimSetEntry.SetRange("Dimension Set ID", DimensionSetID);
        if TempDimSetEntry.FindSet() then
            repeat
                if not DimensionValue.Get(
                    TempDimSetEntry."Dimension Code",
                    TempDimSetEntry."Dimension Value Code"
                ) then
                    Error(
                        'Dimension Value %1 %2 not found',
                        TempDimSetEntry."Dimension Code",
                        TempDimSetEntry."Dimension Value Code"
                    );
            until TempDimSetEntry.Next() = 0;
    end;

    // Create journal line
    GenJournalLine.Init();
    GenJournalLine."Line No." := GetNextLineNo19();
    GenJournalLine."Document No." := DocumentNo;
    GenJournalLine."Posting Date" := PostingDate;
    GenJournalLine."Account Type" := GenJournalLine."Account Type"::"G/L Account";
    GenJournalLine."Account No." := AccountNo;
    GenJournalLine.Amount := TotalAmount;
    GenJournalLine."VAT Amount" := VATAmount;
    GenJournalLine."Dimension Set ID" := DimensionSetID;
    GenJournalLine.Insert();

    // Post the entry
    GLEntry.Init();
    if GLEntry.FindLast() then
        LastEntryNo := GLEntry."Entry No."
    else
        LastEntryNo := 0;

    GLEntry."Entry No." := LastEntryNo + 1;
    GLEntry."G/L Account No." := AccountNo;
    GLEntry."Posting Date" := PostingDate;
    GLEntry."Document Type" := GLEntry."Document Type"::Invoice;
    GLEntry."Document No." := DocumentNo;
    GLEntry.Amount := TotalAmount;
    GLEntry."VAT Amount" := VATAmount;
    GLEntry."Dimension Set ID" := DimensionSetID;
    GLEntry.Insert();

    // Perform validation loops
    for i := 1 to 10 do begin
        if GLEntry.Amount <> TotalAmount then
            Error('Amount mismatch in iteration %1', i);
    end;

    exit(true);
end;

procedure GetNextLineNo19(): Integer
var
    GenJnlLine: Record "Gen. Journal Line";
begin
    if GenJnlLine.FindLast() then
        exit(GenJnlLine."Line No." + 10000)
    else
        exit(10000);
end;

procedure ComplexBusinessLogic20(
    DocumentNo: Code[20];
    PostingDate: Date;
    AccountNo: Code[20];
    Amount: Decimal;
    DimensionSetID: Integer
): Boolean
var
    GenJournalLine: Record "Gen. Journal Line";
    GLEntry: Record "G/L Entry";
    DimensionValue: Record "Dimension Value";
    TempDimSetEntry: Record "Dimension Set Entry" temporary;
    LastEntryNo: Integer;
    VATAmount: Decimal;
    BaseAmount: Decimal;
    TotalAmount: Decimal;
    i: Integer;
begin
    // Initialize variables
    VATAmount := 0;
    BaseAmount := Amount;
    TotalAmount := 0;

    // Validate account
    if AccountNo = '' then
        Error('Account number cannot be empty');

    // Check if document already posted
    GLEntry.SetRange("Document No.", DocumentNo);
    if not GLEntry.IsEmpty() then
        Error('Document %1 already posted', DocumentNo);

    // Calculate VAT
    if PostingDate >= DMY2Date(1, 1, 2024) then
        VATAmount := BaseAmount * 0.25
    else
        VATAmount := BaseAmount * 0.20;

    TotalAmount := BaseAmount + VATAmount;

    // Process dimension set
    if DimensionSetID <> 0 then begin
        TempDimSetEntry.SetRange("Dimension Set ID", DimensionSetID);
        if TempDimSetEntry.FindSet() then
            repeat
                if not DimensionValue.Get(
                    TempDimSetEntry."Dimension Code",
                    TempDimSetEntry."Dimension Value Code"
                ) then
                    Error(
                        'Dimension Value %1 %2 not found',
                        TempDimSetEntry."Dimension Code",
                        TempDimSetEntry."Dimension Value Code"
                    );
            until TempDimSetEntry.Next() = 0;
    end;

    // Create journal line
    GenJournalLine.Init();
    GenJournalLine."Line No." := GetNextLineNo20();
    GenJournalLine."Document No." := DocumentNo;
    GenJournalLine."Posting Date" := PostingDate;
    GenJournalLine."Account Type" := GenJournalLine."Account Type"::"G/L Account";
    GenJournalLine."Account No." := AccountNo;
    GenJournalLine.Amount := TotalAmount;
    GenJournalLine."VAT Amount" := VATAmount;
    GenJournalLine."Dimension Set ID" := DimensionSetID;
    GenJournalLine.Insert();

    // Post the entry
    GLEntry.Init();
    if GLEntry.FindLast() then
        LastEntryNo := GLEntry."Entry No."
    else
        LastEntryNo := 0;

    GLEntry."Entry No." := LastEntryNo + 1;
    GLEntry."G/L Account No." := AccountNo;
    GLEntry."Posting Date" := PostingDate;
    GLEntry."Document Type" := GLEntry."Document Type"::Invoice;
    GLEntry."Document No." := DocumentNo;
    GLEntry.Amount := TotalAmount;
    GLEntry."VAT Amount" := VATAmount;
    GLEntry."Dimension Set ID" := DimensionSetID;
    GLEntry.Insert();

    // Perform validation loops
    for i := 1 to 10 do begin
        if GLEntry.Amount <> TotalAmount then
            Error('Amount mismatch in iteration %1', i);
    end;

    exit(true);
end;

procedure GetNextLineNo20(): Integer
var
    GenJnlLine: Record "Gen. Journal Line";
begin
    if GenJnlLine.FindLast() then
        exit(GenJnlLine."Line No." + 10000)
    else
        exit(10000);
end;

table 50010 "Custom Table 10"
{
    DataClassification = CustomerContent;

    fields
    {
        field(1; "Entry No."; Integer)
        {
            DataClassification = CustomerContent;
            AutoIncrement = true;
        }
        field(2; "Code"; Code[20])
        {
            DataClassification = CustomerContent;
        }
        field(3; "Description"; Text[100])
        {
            DataClassification = CustomerContent;
        }
        field(4; "Amount"; Decimal)
        {
            DataClassification = CustomerContent;
        }
        field(5; "Date"; Date)
        {
            DataClassification = CustomerContent;
        }
    }

    keys
    {
        key(PK; "Entry No.")
        {
            Clustered = true;
        }
    }
}

table 50011 "Custom Table 11"
{
    DataClassification = CustomerContent;

    fields
    {
        field(1; "Entry No."; Integer)
        {
            DataClassification = CustomerContent;
            AutoIncrement = true;
        }
        field(2; "Code"; Code[20])
        {
            DataClassification = CustomerContent;
        }
        field(3; "Description"; Text[100])
        {
            DataClassification = CustomerContent;
        }
        field(4; "Amount"; Decimal)
        {
            DataClassification = CustomerContent;
        }
        field(5; "Date"; Date)
        {
            DataClassification = CustomerContent;
        }
    }

    keys
    {
        key(PK; "Entry No.")
        {
            Clustered = true;
        }
    }
}

procedure ComplexBusinessLogic21(
    DocumentNo: Code[20];
    PostingDate: Date;
    AccountNo: Code[20];
    Amount: Decimal;
    DimensionSetID: Integer
): Boolean
var
    GenJournalLine: Record "Gen. Journal Line";
    GLEntry: Record "G/L Entry";
    DimensionValue: Record "Dimension Value";
    TempDimSetEntry: Record "Dimension Set Entry" temporary;
    LastEntryNo: Integer;
    VATAmount: Decimal;
    BaseAmount: Decimal;
    TotalAmount: Decimal;
    i: Integer;
begin
    // Initialize variables
    VATAmount := 0;
    BaseAmount := Amount;
    TotalAmount := 0;

    // Validate account
    if AccountNo = '' then
        Error('Account number cannot be empty');

    // Check if document already posted
    GLEntry.SetRange("Document No.", DocumentNo);
    if not GLEntry.IsEmpty() then
        Error('Document %1 already posted', DocumentNo);

    // Calculate VAT
    if PostingDate >= DMY2Date(1, 1, 2024) then
        VATAmount := BaseAmount * 0.25
    else
        VATAmount := BaseAmount * 0.20;

    TotalAmount := BaseAmount + VATAmount;

    // Process dimension set
    if DimensionSetID <> 0 then begin
        TempDimSetEntry.SetRange("Dimension Set ID", DimensionSetID);
        if TempDimSetEntry.FindSet() then
            repeat
                if not DimensionValue.Get(
                    TempDimSetEntry."Dimension Code",
                    TempDimSetEntry."Dimension Value Code"
                ) then
                    Error(
                        'Dimension Value %1 %2 not found',
                        TempDimSetEntry."Dimension Code",
                        TempDimSetEntry."Dimension Value Code"
                    );
            until TempDimSetEntry.Next() = 0;
    end;

    // Create journal line
    GenJournalLine.Init();
    GenJournalLine."Line No." := GetNextLineNo21();
    GenJournalLine."Document No." := DocumentNo;
    GenJournalLine."Posting Date" := PostingDate;
    GenJournalLine."Account Type" := GenJournalLine."Account Type"::"G/L Account";
    GenJournalLine."Account No." := AccountNo;
    GenJournalLine.Amount := TotalAmount;
    GenJournalLine."VAT Amount" := VATAmount;
    GenJournalLine."Dimension Set ID" := DimensionSetID;
    GenJournalLine.Insert();

    // Post the entry
    GLEntry.Init();
    if GLEntry.FindLast() then
        LastEntryNo := GLEntry."Entry No."
    else
        LastEntryNo := 0;

    GLEntry."Entry No." := LastEntryNo + 1;
    GLEntry."G/L Account No." := AccountNo;
    GLEntry."Posting Date" := PostingDate;
    GLEntry."Document Type" := GLEntry."Document Type"::Invoice;
    GLEntry."Document No." := DocumentNo;
    GLEntry.Amount := TotalAmount;
    GLEntry."VAT Amount" := VATAmount;
    GLEntry."Dimension Set ID" := DimensionSetID;
    GLEntry.Insert();

    // Perform validation loops
    for i := 1 to 10 do begin
        if GLEntry.Amount <> TotalAmount then
            Error('Amount mismatch in iteration %1', i);
    end;

    exit(true);
end;

procedure GetNextLineNo21(): Integer
var
    GenJnlLine: Record "Gen. Journal Line";
begin
    if GenJnlLine.FindLast() then
        exit(GenJnlLine."Line No." + 10000)
    else
        exit(10000);
end;

table 50012 "Custom Table 12"
{
    DataClassification = CustomerContent;

    fields
    {
        field(1; "Entry No."; Integer)
        {
            DataClassification = CustomerContent;
            AutoIncrement = true;
        }
        field(2; "Code"; Code[20])
        {
            DataClassification = CustomerContent;
        }
        field(3; "Description"; Text[100])
        {
            DataClassification = CustomerContent;
        }
        field(4; "Amount"; Decimal)
        {
            DataClassification = CustomerContent;
        }
        field(5; "Date"; Date)
        {
            DataClassification = CustomerContent;
        }
    }

    keys
    {
        key(PK; "Entry No.")
        {
            Clustered = true;
        }
    }
}

procedure ComplexBusinessLogic22(
    DocumentNo: Code[20];
    PostingDate: Date;
    AccountNo: Code[20];
    Amount: Decimal;
    DimensionSetID: Integer
): Boolean
var
    GenJournalLine: Record "Gen. Journal Line";
    GLEntry: Record "G/L Entry";
    DimensionValue: Record "Dimension Value";
    TempDimSetEntry: Record "Dimension Set Entry" temporary;
    LastEntryNo: Integer;
    VATAmount: Decimal;
    BaseAmount: Decimal;
    TotalAmount: Decimal;
    i: Integer;
begin
    // Initialize variables
    VATAmount := 0;
    BaseAmount := Amount;
    TotalAmount := 0;

    // Validate account
    if AccountNo = '' then
        Error('Account number cannot be empty');

    // Check if document already posted
    GLEntry.SetRange("Document No.", DocumentNo);
    if not GLEntry.IsEmpty() then
        Error('Document %1 already posted', DocumentNo);

    // Calculate VAT
    if PostingDate >= DMY2Date(1, 1, 2024) then
        VATAmount := BaseAmount * 0.25
    else
        VATAmount := BaseAmount * 0.20;

    TotalAmount := BaseAmount + VATAmount;

    // Process dimension set
    if DimensionSetID <> 0 then begin
        TempDimSetEntry.SetRange("Dimension Set ID", DimensionSetID);
        if TempDimSetEntry.FindSet() then
            repeat
                if not DimensionValue.Get(
                    TempDimSetEntry."Dimension Code",
                    TempDimSetEntry."Dimension Value Code"
                ) then
                    Error(
                        'Dimension Value %1 %2 not found',
                        TempDimSetEntry."Dimension Code",
                        TempDimSetEntry."Dimension Value Code"
                    );
            until TempDimSetEntry.Next() = 0;
    end;

    // Create journal line
    GenJournalLine.Init();
    GenJournalLine."Line No." := GetNextLineNo22();
    GenJournalLine."Document No." := DocumentNo;
    GenJournalLine."Posting Date" := PostingDate;
    GenJournalLine."Account Type" := GenJournalLine."Account Type"::"G/L Account";
    GenJournalLine."Account No." := AccountNo;
    GenJournalLine.Amount := TotalAmount;
    GenJournalLine."VAT Amount" := VATAmount;
    GenJournalLine."Dimension Set ID" := DimensionSetID;
    GenJournalLine.Insert();

    // Post the entry
    GLEntry.Init();
    if GLEntry.FindLast() then
        LastEntryNo := GLEntry."Entry No."
    else
        LastEntryNo := 0;

    GLEntry."Entry No." := LastEntryNo + 1;
    GLEntry."G/L Account No." := AccountNo;
    GLEntry."Posting Date" := PostingDate;
    GLEntry."Document Type" := GLEntry."Document Type"::Invoice;
    GLEntry."Document No." := DocumentNo;
    GLEntry.Amount := TotalAmount;
    GLEntry."VAT Amount" := VATAmount;
    GLEntry."Dimension Set ID" := DimensionSetID;
    GLEntry.Insert();

    // Perform validation loops
    for i := 1 to 10 do begin
        if GLEntry.Amount <> TotalAmount then
            Error('Amount mismatch in iteration %1', i);
    end;

    exit(true);
end;

procedure GetNextLineNo22(): Integer
var
    GenJnlLine: Record "Gen. Journal Line";
begin
    if GenJnlLine.FindLast() then
        exit(GenJnlLine."Line No." + 10000)
    else
        exit(10000);
end;

procedure ComplexBusinessLogic23(
    DocumentNo: Code[20];
    PostingDate: Date;
    AccountNo: Code[20];
    Amount: Decimal;
    DimensionSetID: Integer
): Boolean
var
    GenJournalLine: Record "Gen. Journal Line";
    GLEntry: Record "G/L Entry";
    DimensionValue: Record "Dimension Value";
    TempDimSetEntry: Record "Dimension Set Entry" temporary;
    LastEntryNo: Integer;
    VATAmount: Decimal;
    BaseAmount: Decimal;
    TotalAmount: Decimal;
    i: Integer;
begin
    // Initialize variables
    VATAmount := 0;
    BaseAmount := Amount;
    TotalAmount := 0;

    // Validate account
    if AccountNo = '' then
        Error('Account number cannot be empty');

    // Check if document already posted
    GLEntry.SetRange("Document No.", DocumentNo);
    if not GLEntry.IsEmpty() then
        Error('Document %1 already posted', DocumentNo);

    // Calculate VAT
    if PostingDate >= DMY2Date(1, 1, 2024) then
        VATAmount := BaseAmount * 0.25
    else
        VATAmount := BaseAmount * 0.20;

    TotalAmount := BaseAmount + VATAmount;

    // Process dimension set
    if DimensionSetID <> 0 then begin
        TempDimSetEntry.SetRange("Dimension Set ID", DimensionSetID);
        if TempDimSetEntry.FindSet() then
            repeat
                if not DimensionValue.Get(
                    TempDimSetEntry."Dimension Code",
                    TempDimSetEntry."Dimension Value Code"
                ) then
                    Error(
                        'Dimension Value %1 %2 not found',
                        TempDimSetEntry."Dimension Code",
                        TempDimSetEntry."Dimension Value Code"
                    );
            until TempDimSetEntry.Next() = 0;
    end;

    // Create journal line
    GenJournalLine.Init();
    GenJournalLine."Line No." := GetNextLineNo23();
    GenJournalLine."Document No." := DocumentNo;
    GenJournalLine."Posting Date" := PostingDate;
    GenJournalLine."Account Type" := GenJournalLine."Account Type"::"G/L Account";
    GenJournalLine."Account No." := AccountNo;
    GenJournalLine.Amount := TotalAmount;
    GenJournalLine."VAT Amount" := VATAmount;
    GenJournalLine."Dimension Set ID" := DimensionSetID;
    GenJournalLine.Insert();

    // Post the entry
    GLEntry.Init();
    if GLEntry.FindLast() then
        LastEntryNo := GLEntry."Entry No."
    else
        LastEntryNo := 0;

    GLEntry."Entry No." := LastEntryNo + 1;
    GLEntry."G/L Account No." := AccountNo;
    GLEntry."Posting Date" := PostingDate;
    GLEntry."Document Type" := GLEntry."Document Type"::Invoice;
    GLEntry."Document No." := DocumentNo;
    GLEntry.Amount := TotalAmount;
    GLEntry."VAT Amount" := VATAmount;
    GLEntry."Dimension Set ID" := DimensionSetID;
    GLEntry.Insert();

    // Perform validation loops
    for i := 1 to 10 do begin
        if GLEntry.Amount <> TotalAmount then
            Error('Amount mismatch in iteration %1', i);
    end;

    exit(true);
end;

procedure GetNextLineNo23(): Integer
var
    GenJnlLine: Record "Gen. Journal Line";
begin
    if GenJnlLine.FindLast() then
        exit(GenJnlLine."Line No." + 10000)
    else
        exit(10000);
end;

table 50013 "Custom Table 13"
{
    DataClassification = CustomerContent;

    fields
    {
        field(1; "Entry No."; Integer)
        {
            DataClassification = CustomerContent;
            AutoIncrement = true;
        }
        field(2; "Code"; Code[20])
        {
            DataClassification = CustomerContent;
        }
        field(3; "Description"; Text[100])
        {
            DataClassification = CustomerContent;
        }
        field(4; "Amount"; Decimal)
        {
            DataClassification = CustomerContent;
        }
        field(5; "Date"; Date)
        {
            DataClassification = CustomerContent;
        }
    }

    keys
    {
        key(PK; "Entry No.")
        {
            Clustered = true;
        }
    }
}

page 50007 "Custom List 7"
{
    PageType = List;
    SourceTable = "Custom Table 7";
    UsageCategory = Lists;
    ApplicationArea = All;

    layout
    {
        area(Content)
        {
            repeater(Group)
            {
                field("Entry No."; "Entry No.")
                {
                    ApplicationArea = All;
                }
                field("Code"; "Code")
                {
                    ApplicationArea = All;
                }
                field("Description"; "Description")
                {
                    ApplicationArea = All;
                }
                field("Amount"; "Amount")
                {
                    ApplicationArea = All;
                }
            }
        }
    }
}

table 50014 "Custom Table 14"
{
    DataClassification = CustomerContent;

    fields
    {
        field(1; "Entry No."; Integer)
        {
            DataClassification = CustomerContent;
            AutoIncrement = true;
        }
        field(2; "Code"; Code[20])
        {
            DataClassification = CustomerContent;
        }
        field(3; "Description"; Text[100])
        {
            DataClassification = CustomerContent;
        }
        field(4; "Amount"; Decimal)
        {
            DataClassification = CustomerContent;
        }
        field(5; "Date"; Date)
        {
            DataClassification = CustomerContent;
        }
    }

    keys
    {
        key(PK; "Entry No.")
        {
            Clustered = true;
        }
    }
}

procedure ComplexBusinessLogic24(
    DocumentNo: Code[20];
    PostingDate: Date;
    AccountNo: Code[20];
    Amount: Decimal;
    DimensionSetID: Integer
): Boolean
var
    GenJournalLine: Record "Gen. Journal Line";
    GLEntry: Record "G/L Entry";
    DimensionValue: Record "Dimension Value";
    TempDimSetEntry: Record "Dimension Set Entry" temporary;
    LastEntryNo: Integer;
    VATAmount: Decimal;
    BaseAmount: Decimal;
    TotalAmount: Decimal;
    i: Integer;
begin
    // Initialize variables
    VATAmount := 0;
    BaseAmount := Amount;
    TotalAmount := 0;

    // Validate account
    if AccountNo = '' then
        Error('Account number cannot be empty');

    // Check if document already posted
    GLEntry.SetRange("Document No.", DocumentNo);
    if not GLEntry.IsEmpty() then
        Error('Document %1 already posted', DocumentNo);

    // Calculate VAT
    if PostingDate >= DMY2Date(1, 1, 2024) then
        VATAmount := BaseAmount * 0.25
    else
        VATAmount := BaseAmount * 0.20;

    TotalAmount := BaseAmount + VATAmount;

    // Process dimension set
    if DimensionSetID <> 0 then begin
        TempDimSetEntry.SetRange("Dimension Set ID", DimensionSetID);
        if TempDimSetEntry.FindSet() then
            repeat
                if not DimensionValue.Get(
                    TempDimSetEntry."Dimension Code",
                    TempDimSetEntry."Dimension Value Code"
                ) then
                    Error(
                        'Dimension Value %1 %2 not found',
                        TempDimSetEntry."Dimension Code",
                        TempDimSetEntry."Dimension Value Code"
                    );
            until TempDimSetEntry.Next() = 0;
    end;

    // Create journal line
    GenJournalLine.Init();
    GenJournalLine."Line No." := GetNextLineNo24();
    GenJournalLine."Document No." := DocumentNo;
    GenJournalLine."Posting Date" := PostingDate;
    GenJournalLine."Account Type" := GenJournalLine."Account Type"::"G/L Account";
    GenJournalLine."Account No." := AccountNo;
    GenJournalLine.Amount := TotalAmount;
    GenJournalLine."VAT Amount" := VATAmount;
    GenJournalLine."Dimension Set ID" := DimensionSetID;
    GenJournalLine.Insert();

    // Post the entry
    GLEntry.Init();
    if GLEntry.FindLast() then
        LastEntryNo := GLEntry."Entry No."
    else
        LastEntryNo := 0;

    GLEntry."Entry No." := LastEntryNo + 1;
    GLEntry."G/L Account No." := AccountNo;
    GLEntry."Posting Date" := PostingDate;
    GLEntry."Document Type" := GLEntry."Document Type"::Invoice;
    GLEntry."Document No." := DocumentNo;
    GLEntry.Amount := TotalAmount;
    GLEntry."VAT Amount" := VATAmount;
    GLEntry."Dimension Set ID" := DimensionSetID;
    GLEntry.Insert();

    // Perform validation loops
    for i := 1 to 10 do begin
        if GLEntry.Amount <> TotalAmount then
            Error('Amount mismatch in iteration %1', i);
    end;

    exit(true);
end;

procedure GetNextLineNo24(): Integer
var
    GenJnlLine: Record "Gen. Journal Line";
begin
    if GenJnlLine.FindLast() then
        exit(GenJnlLine."Line No." + 10000)
    else
        exit(10000);
end;

procedure ComplexBusinessLogic25(
    DocumentNo: Code[20];
    PostingDate: Date;
    AccountNo: Code[20];
    Amount: Decimal;
    DimensionSetID: Integer
): Boolean
var
    GenJournalLine: Record "Gen. Journal Line";
    GLEntry: Record "G/L Entry";
    DimensionValue: Record "Dimension Value";
    TempDimSetEntry: Record "Dimension Set Entry" temporary;
    LastEntryNo: Integer;
    VATAmount: Decimal;
    BaseAmount: Decimal;
    TotalAmount: Decimal;
    i: Integer;
begin
    // Initialize variables
    VATAmount := 0;
    BaseAmount := Amount;
    TotalAmount := 0;

    // Validate account
    if AccountNo = '' then
        Error('Account number cannot be empty');

    // Check if document already posted
    GLEntry.SetRange("Document No.", DocumentNo);
    if not GLEntry.IsEmpty() then
        Error('Document %1 already posted', DocumentNo);

    // Calculate VAT
    if PostingDate >= DMY2Date(1, 1, 2024) then
        VATAmount := BaseAmount * 0.25
    else
        VATAmount := BaseAmount * 0.20;

    TotalAmount := BaseAmount + VATAmount;

    // Process dimension set
    if DimensionSetID <> 0 then begin
        TempDimSetEntry.SetRange("Dimension Set ID", DimensionSetID);
        if TempDimSetEntry.FindSet() then
            repeat
                if not DimensionValue.Get(
                    TempDimSetEntry."Dimension Code",
                    TempDimSetEntry."Dimension Value Code"
                ) then
                    Error(
                        'Dimension Value %1 %2 not found',
                        TempDimSetEntry."Dimension Code",
                        TempDimSetEntry."Dimension Value Code"
                    );
            until TempDimSetEntry.Next() = 0;
    end;

    // Create journal line
    GenJournalLine.Init();
    GenJournalLine."Line No." := GetNextLineNo25();
    GenJournalLine."Document No." := DocumentNo;
    GenJournalLine."Posting Date" := PostingDate;
    GenJournalLine."Account Type" := GenJournalLine."Account Type"::"G/L Account";
    GenJournalLine."Account No." := AccountNo;
    GenJournalLine.Amount := TotalAmount;
    GenJournalLine."VAT Amount" := VATAmount;
    GenJournalLine."Dimension Set ID" := DimensionSetID;
    GenJournalLine.Insert();

    // Post the entry
    GLEntry.Init();
    if GLEntry.FindLast() then
        LastEntryNo := GLEntry."Entry No."
    else
        LastEntryNo := 0;

    GLEntry."Entry No." := LastEntryNo + 1;
    GLEntry."G/L Account No." := AccountNo;
    GLEntry."Posting Date" := PostingDate;
    GLEntry."Document Type" := GLEntry."Document Type"::Invoice;
    GLEntry."Document No." := DocumentNo;
    GLEntry.Amount := TotalAmount;
    GLEntry."VAT Amount" := VATAmount;
    GLEntry."Dimension Set ID" := DimensionSetID;
    GLEntry.Insert();

    // Perform validation loops
    for i := 1 to 10 do begin
        if GLEntry.Amount <> TotalAmount then
            Error('Amount mismatch in iteration %1', i);
    end;

    exit(true);
end;

procedure GetNextLineNo25(): Integer
var
    GenJnlLine: Record "Gen. Journal Line";
begin
    if GenJnlLine.FindLast() then
        exit(GenJnlLine."Line No." + 10000)
    else
        exit(10000);
end;

procedure ComplexBusinessLogic26(
    DocumentNo: Code[20];
    PostingDate: Date;
    AccountNo: Code[20];
    Amount: Decimal;
    DimensionSetID: Integer
): Boolean
var
    GenJournalLine: Record "Gen. Journal Line";
    GLEntry: Record "G/L Entry";
    DimensionValue: Record "Dimension Value";
    TempDimSetEntry: Record "Dimension Set Entry" temporary;
    LastEntryNo: Integer;
    VATAmount: Decimal;
    BaseAmount: Decimal;
    TotalAmount: Decimal;
    i: Integer;
begin
    // Initialize variables
    VATAmount := 0;
    BaseAmount := Amount;
    TotalAmount := 0;

    // Validate account
    if AccountNo = '' then
        Error('Account number cannot be empty');

    // Check if document already posted
    GLEntry.SetRange("Document No.", DocumentNo);
    if not GLEntry.IsEmpty() then
        Error('Document %1 already posted', DocumentNo);

    // Calculate VAT
    if PostingDate >= DMY2Date(1, 1, 2024) then
        VATAmount := BaseAmount * 0.25
    else
        VATAmount := BaseAmount * 0.20;

    TotalAmount := BaseAmount + VATAmount;

    // Process dimension set
    if DimensionSetID <> 0 then begin
        TempDimSetEntry.SetRange("Dimension Set ID", DimensionSetID);
        if TempDimSetEntry.FindSet() then
            repeat
                if not DimensionValue.Get(
                    TempDimSetEntry."Dimension Code",
                    TempDimSetEntry."Dimension Value Code"
                ) then
                    Error(
                        'Dimension Value %1 %2 not found',
                        TempDimSetEntry."Dimension Code",
                        TempDimSetEntry."Dimension Value Code"
                    );
            until TempDimSetEntry.Next() = 0;
    end;

    // Create journal line
    GenJournalLine.Init();
    GenJournalLine."Line No." := GetNextLineNo26();
    GenJournalLine."Document No." := DocumentNo;
    GenJournalLine."Posting Date" := PostingDate;
    GenJournalLine."Account Type" := GenJournalLine."Account Type"::"G/L Account";
    GenJournalLine."Account No." := AccountNo;
    GenJournalLine.Amount := TotalAmount;
    GenJournalLine."VAT Amount" := VATAmount;
    GenJournalLine."Dimension Set ID" := DimensionSetID;
    GenJournalLine.Insert();

    // Post the entry
    GLEntry.Init();
    if GLEntry.FindLast() then
        LastEntryNo := GLEntry."Entry No."
    else
        LastEntryNo := 0;

    GLEntry."Entry No." := LastEntryNo + 1;
    GLEntry."G/L Account No." := AccountNo;
    GLEntry."Posting Date" := PostingDate;
    GLEntry."Document Type" := GLEntry."Document Type"::Invoice;
    GLEntry."Document No." := DocumentNo;
    GLEntry.Amount := TotalAmount;
    GLEntry."VAT Amount" := VATAmount;
    GLEntry."Dimension Set ID" := DimensionSetID;
    GLEntry.Insert();

    // Perform validation loops
    for i := 1 to 10 do begin
        if GLEntry.Amount <> TotalAmount then
            Error('Amount mismatch in iteration %1', i);
    end;

    exit(true);
end;

procedure GetNextLineNo26(): Integer
var
    GenJnlLine: Record "Gen. Journal Line";
begin
    if GenJnlLine.FindLast() then
        exit(GenJnlLine."Line No." + 10000)
    else
        exit(10000);
end;

procedure ComplexBusinessLogic27(
    DocumentNo: Code[20];
    PostingDate: Date;
    AccountNo: Code[20];
    Amount: Decimal;
    DimensionSetID: Integer
): Boolean
var
    GenJournalLine: Record "Gen. Journal Line";
    GLEntry: Record "G/L Entry";
    DimensionValue: Record "Dimension Value";
    TempDimSetEntry: Record "Dimension Set Entry" temporary;
    LastEntryNo: Integer;
    VATAmount: Decimal;
    BaseAmount: Decimal;
    TotalAmount: Decimal;
    i: Integer;
begin
    // Initialize variables
    VATAmount := 0;
    BaseAmount := Amount;
    TotalAmount := 0;

    // Validate account
    if AccountNo = '' then
        Error('Account number cannot be empty');

    // Check if document already posted
    GLEntry.SetRange("Document No.", DocumentNo);
    if not GLEntry.IsEmpty() then
        Error('Document %1 already posted', DocumentNo);

    // Calculate VAT
    if PostingDate >= DMY2Date(1, 1, 2024) then
        VATAmount := BaseAmount * 0.25
    else
        VATAmount := BaseAmount * 0.20;

    TotalAmount := BaseAmount + VATAmount;

    // Process dimension set
    if DimensionSetID <> 0 then begin
        TempDimSetEntry.SetRange("Dimension Set ID", DimensionSetID);
        if TempDimSetEntry.FindSet() then
            repeat
                if not DimensionValue.Get(
                    TempDimSetEntry."Dimension Code",
                    TempDimSetEntry."Dimension Value Code"
                ) then
                    Error(
                        'Dimension Value %1 %2 not found',
                        TempDimSetEntry."Dimension Code",
                        TempDimSetEntry."Dimension Value Code"
                    );
            until TempDimSetEntry.Next() = 0;
    end;

    // Create journal line
    GenJournalLine.Init();
    GenJournalLine."Line No." := GetNextLineNo27();
    GenJournalLine."Document No." := DocumentNo;
    GenJournalLine."Posting Date" := PostingDate;
    GenJournalLine."Account Type" := GenJournalLine."Account Type"::"G/L Account";
    GenJournalLine."Account No." := AccountNo;
    GenJournalLine.Amount := TotalAmount;
    GenJournalLine."VAT Amount" := VATAmount;
    GenJournalLine."Dimension Set ID" := DimensionSetID;
    GenJournalLine.Insert();

    // Post the entry
    GLEntry.Init();
    if GLEntry.FindLast() then
        LastEntryNo := GLEntry."Entry No."
    else
        LastEntryNo := 0;

    GLEntry."Entry No." := LastEntryNo + 1;
    GLEntry."G/L Account No." := AccountNo;
    GLEntry."Posting Date" := PostingDate;
    GLEntry."Document Type" := GLEntry."Document Type"::Invoice;
    GLEntry."Document No." := DocumentNo;
    GLEntry.Amount := TotalAmount;
    GLEntry."VAT Amount" := VATAmount;
    GLEntry."Dimension Set ID" := DimensionSetID;
    GLEntry.Insert();

    // Perform validation loops
    for i := 1 to 10 do begin
        if GLEntry.Amount <> TotalAmount then
            Error('Amount mismatch in iteration %1', i);
    end;

    exit(true);
end;

procedure GetNextLineNo27(): Integer
var
    GenJnlLine: Record "Gen. Journal Line";
begin
    if GenJnlLine.FindLast() then
        exit(GenJnlLine."Line No." + 10000)
    else
        exit(10000);
end;

table 50015 "Custom Table 15"
{
    DataClassification = CustomerContent;

    fields
    {
        field(1; "Entry No."; Integer)
        {
            DataClassification = CustomerContent;
            AutoIncrement = true;
        }
        field(2; "Code"; Code[20])
        {
            DataClassification = CustomerContent;
        }
        field(3; "Description"; Text[100])
        {
            DataClassification = CustomerContent;
        }
        field(4; "Amount"; Decimal)
        {
            DataClassification = CustomerContent;
        }
        field(5; "Date"; Date)
        {
            DataClassification = CustomerContent;
        }
    }

    keys
    {
        key(PK; "Entry No.")
        {
            Clustered = true;
        }
    }
}

table 50016 "Custom Table 16"
{
    DataClassification = CustomerContent;

    fields
    {
        field(1; "Entry No."; Integer)
        {
            DataClassification = CustomerContent;
            AutoIncrement = true;
        }
        field(2; "Code"; Code[20])
        {
            DataClassification = CustomerContent;
        }
        field(3; "Description"; Text[100])
        {
            DataClassification = CustomerContent;
        }
        field(4; "Amount"; Decimal)
        {
            DataClassification = CustomerContent;
        }
        field(5; "Date"; Date)
        {
            DataClassification = CustomerContent;
        }
    }

    keys
    {
        key(PK; "Entry No.")
        {
            Clustered = true;
        }
    }
}

table 50017 "Custom Table 17"
{
    DataClassification = CustomerContent;

    fields
    {
        field(1; "Entry No."; Integer)
        {
            DataClassification = CustomerContent;
            AutoIncrement = true;
        }
        field(2; "Code"; Code[20])
        {
            DataClassification = CustomerContent;
        }
        field(3; "Description"; Text[100])
        {
            DataClassification = CustomerContent;
        }
        field(4; "Amount"; Decimal)
        {
            DataClassification = CustomerContent;
        }
        field(5; "Date"; Date)
        {
            DataClassification = CustomerContent;
        }
    }

    keys
    {
        key(PK; "Entry No.")
        {
            Clustered = true;
        }
    }
}

page 50008 "Custom List 8"
{
    PageType = List;
    SourceTable = "Custom Table 8";
    UsageCategory = Lists;
    ApplicationArea = All;

    layout
    {
        area(Content)
        {
            repeater(Group)
            {
                field("Entry No."; "Entry No.")
                {
                    ApplicationArea = All;
                }
                field("Code"; "Code")
                {
                    ApplicationArea = All;
                }
                field("Description"; "Description")
                {
                    ApplicationArea = All;
                }
                field("Amount"; "Amount")
                {
                    ApplicationArea = All;
                }
            }
        }
    }
}

procedure ComplexBusinessLogic28(
    DocumentNo: Code[20];
    PostingDate: Date;
    AccountNo: Code[20];
    Amount: Decimal;
    DimensionSetID: Integer
): Boolean
var
    GenJournalLine: Record "Gen. Journal Line";
    GLEntry: Record "G/L Entry";
    DimensionValue: Record "Dimension Value";
    TempDimSetEntry: Record "Dimension Set Entry" temporary;
    LastEntryNo: Integer;
    VATAmount: Decimal;
    BaseAmount: Decimal;
    TotalAmount: Decimal;
    i: Integer;
begin
    // Initialize variables
    VATAmount := 0;
    BaseAmount := Amount;
    TotalAmount := 0;

    // Validate account
    if AccountNo = '' then
        Error('Account number cannot be empty');

    // Check if document already posted
    GLEntry.SetRange("Document No.", DocumentNo);
    if not GLEntry.IsEmpty() then
        Error('Document %1 already posted', DocumentNo);

    // Calculate VAT
    if PostingDate >= DMY2Date(1, 1, 2024) then
        VATAmount := BaseAmount * 0.25
    else
        VATAmount := BaseAmount * 0.20;

    TotalAmount := BaseAmount + VATAmount;

    // Process dimension set
    if DimensionSetID <> 0 then begin
        TempDimSetEntry.SetRange("Dimension Set ID", DimensionSetID);
        if TempDimSetEntry.FindSet() then
            repeat
                if not DimensionValue.Get(
                    TempDimSetEntry."Dimension Code",
                    TempDimSetEntry."Dimension Value Code"
                ) then
                    Error(
                        'Dimension Value %1 %2 not found',
                        TempDimSetEntry."Dimension Code",
                        TempDimSetEntry."Dimension Value Code"
                    );
            until TempDimSetEntry.Next() = 0;
    end;

    // Create journal line
    GenJournalLine.Init();
    GenJournalLine."Line No." := GetNextLineNo28();
    GenJournalLine."Document No." := DocumentNo;
    GenJournalLine."Posting Date" := PostingDate;
    GenJournalLine."Account Type" := GenJournalLine."Account Type"::"G/L Account";
    GenJournalLine."Account No." := AccountNo;
    GenJournalLine.Amount := TotalAmount;
    GenJournalLine."VAT Amount" := VATAmount;
    GenJournalLine."Dimension Set ID" := DimensionSetID;
    GenJournalLine.Insert();

    // Post the entry
    GLEntry.Init();
    if GLEntry.FindLast() then
        LastEntryNo := GLEntry."Entry No."
    else
        LastEntryNo := 0;

    GLEntry."Entry No." := LastEntryNo + 1;
    GLEntry."G/L Account No." := AccountNo;
    GLEntry."Posting Date" := PostingDate;
    GLEntry."Document Type" := GLEntry."Document Type"::Invoice;
    GLEntry."Document No." := DocumentNo;
    GLEntry.Amount := TotalAmount;
    GLEntry."VAT Amount" := VATAmount;
    GLEntry."Dimension Set ID" := DimensionSetID;
    GLEntry.Insert();

    // Perform validation loops
    for i := 1 to 10 do begin
        if GLEntry.Amount <> TotalAmount then
            Error('Amount mismatch in iteration %1', i);
    end;

    exit(true);
end;

procedure GetNextLineNo28(): Integer
var
    GenJnlLine: Record "Gen. Journal Line";
begin
    if GenJnlLine.FindLast() then
        exit(GenJnlLine."Line No." + 10000)
    else
        exit(10000);
end;

page 50009 "Custom List 9"
{
    PageType = List;
    SourceTable = "Custom Table 9";
    UsageCategory = Lists;
    ApplicationArea = All;

    layout
    {
        area(Content)
        {
            repeater(Group)
            {
                field("Entry No."; "Entry No.")
                {
                    ApplicationArea = All;
                }
                field("Code"; "Code")
                {
                    ApplicationArea = All;
                }
                field("Description"; "Description")
                {
                    ApplicationArea = All;
                }
                field("Amount"; "Amount")
                {
                    ApplicationArea = All;
                }
            }
        }
    }
}

procedure ComplexBusinessLogic29(
    DocumentNo: Code[20];
    PostingDate: Date;
    AccountNo: Code[20];
    Amount: Decimal;
    DimensionSetID: Integer
): Boolean
var
    GenJournalLine: Record "Gen. Journal Line";
    GLEntry: Record "G/L Entry";
    DimensionValue: Record "Dimension Value";
    TempDimSetEntry: Record "Dimension Set Entry" temporary;
    LastEntryNo: Integer;
    VATAmount: Decimal;
    BaseAmount: Decimal;
    TotalAmount: Decimal;
    i: Integer;
begin
    // Initialize variables
    VATAmount := 0;
    BaseAmount := Amount;
    TotalAmount := 0;

    // Validate account
    if AccountNo = '' then
        Error('Account number cannot be empty');

    // Check if document already posted
    GLEntry.SetRange("Document No.", DocumentNo);
    if not GLEntry.IsEmpty() then
        Error('Document %1 already posted', DocumentNo);

    // Calculate VAT
    if PostingDate >= DMY2Date(1, 1, 2024) then
        VATAmount := BaseAmount * 0.25
    else
        VATAmount := BaseAmount * 0.20;

    TotalAmount := BaseAmount + VATAmount;

    // Process dimension set
    if DimensionSetID <> 0 then begin
        TempDimSetEntry.SetRange("Dimension Set ID", DimensionSetID);
        if TempDimSetEntry.FindSet() then
            repeat
                if not DimensionValue.Get(
                    TempDimSetEntry."Dimension Code",
                    TempDimSetEntry."Dimension Value Code"
                ) then
                    Error(
                        'Dimension Value %1 %2 not found',
                        TempDimSetEntry."Dimension Code",
                        TempDimSetEntry."Dimension Value Code"
                    );
            until TempDimSetEntry.Next() = 0;
    end;

    // Create journal line
    GenJournalLine.Init();
    GenJournalLine."Line No." := GetNextLineNo29();
    GenJournalLine."Document No." := DocumentNo;
    GenJournalLine."Posting Date" := PostingDate;
    GenJournalLine."Account Type" := GenJournalLine."Account Type"::"G/L Account";
    GenJournalLine."Account No." := AccountNo;
    GenJournalLine.Amount := TotalAmount;
    GenJournalLine."VAT Amount" := VATAmount;
    GenJournalLine."Dimension Set ID" := DimensionSetID;
    GenJournalLine.Insert();

    // Post the entry
    GLEntry.Init();
    if GLEntry.FindLast() then
        LastEntryNo := GLEntry."Entry No."
    else
        LastEntryNo := 0;

    GLEntry."Entry No." := LastEntryNo + 1;
    GLEntry."G/L Account No." := AccountNo;
    GLEntry."Posting Date" := PostingDate;
    GLEntry."Document Type" := GLEntry."Document Type"::Invoice;
    GLEntry."Document No." := DocumentNo;
    GLEntry.Amount := TotalAmount;
    GLEntry."VAT Amount" := VATAmount;
    GLEntry."Dimension Set ID" := DimensionSetID;
    GLEntry.Insert();

    // Perform validation loops
    for i := 1 to 10 do begin
        if GLEntry.Amount <> TotalAmount then
            Error('Amount mismatch in iteration %1', i);
    end;

    exit(true);
end;

procedure GetNextLineNo29(): Integer
var
    GenJnlLine: Record "Gen. Journal Line";
begin
    if GenJnlLine.FindLast() then
        exit(GenJnlLine."Line No." + 10000)
    else
        exit(10000);
end;

page 50010 "Custom List 10"
{
    PageType = List;
    SourceTable = "Custom Table 10";
    UsageCategory = Lists;
    ApplicationArea = All;

    layout
    {
        area(Content)
        {
            repeater(Group)
            {
                field("Entry No."; "Entry No.")
                {
                    ApplicationArea = All;
                }
                field("Code"; "Code")
                {
                    ApplicationArea = All;
                }
                field("Description"; "Description")
                {
                    ApplicationArea = All;
                }
                field("Amount"; "Amount")
                {
                    ApplicationArea = All;
                }
            }
        }
    }
}

procedure ComplexBusinessLogic30(
    DocumentNo: Code[20];
    PostingDate: Date;
    AccountNo: Code[20];
    Amount: Decimal;
    DimensionSetID: Integer
): Boolean
var
    GenJournalLine: Record "Gen. Journal Line";
    GLEntry: Record "G/L Entry";
    DimensionValue: Record "Dimension Value";
    TempDimSetEntry: Record "Dimension Set Entry" temporary;
    LastEntryNo: Integer;
    VATAmount: Decimal;
    BaseAmount: Decimal;
    TotalAmount: Decimal;
    i: Integer;
begin
    // Initialize variables
    VATAmount := 0;
    BaseAmount := Amount;
    TotalAmount := 0;

    // Validate account
    if AccountNo = '' then
        Error('Account number cannot be empty');

    // Check if document already posted
    GLEntry.SetRange("Document No.", DocumentNo);
    if not GLEntry.IsEmpty() then
        Error('Document %1 already posted', DocumentNo);

    // Calculate VAT
    if PostingDate >= DMY2Date(1, 1, 2024) then
        VATAmount := BaseAmount * 0.25
    else
        VATAmount := BaseAmount * 0.20;

    TotalAmount := BaseAmount + VATAmount;

    // Process dimension set
    if DimensionSetID <> 0 then begin
        TempDimSetEntry.SetRange("Dimension Set ID", DimensionSetID);
        if TempDimSetEntry.FindSet() then
            repeat
                if not DimensionValue.Get(
                    TempDimSetEntry."Dimension Code",
                    TempDimSetEntry."Dimension Value Code"
                ) then
                    Error(
                        'Dimension Value %1 %2 not found',
                        TempDimSetEntry."Dimension Code",
                        TempDimSetEntry."Dimension Value Code"
                    );
            until TempDimSetEntry.Next() = 0;
    end;

    // Create journal line
    GenJournalLine.Init();
    GenJournalLine."Line No." := GetNextLineNo30();
    GenJournalLine."Document No." := DocumentNo;
    GenJournalLine."Posting Date" := PostingDate;
    GenJournalLine."Account Type" := GenJournalLine."Account Type"::"G/L Account";
    GenJournalLine."Account No." := AccountNo;
    GenJournalLine.Amount := TotalAmount;
    GenJournalLine."VAT Amount" := VATAmount;
    GenJournalLine."Dimension Set ID" := DimensionSetID;
    GenJournalLine.Insert();

    // Post the entry
    GLEntry.Init();
    if GLEntry.FindLast() then
        LastEntryNo := GLEntry."Entry No."
    else
        LastEntryNo := 0;

    GLEntry."Entry No." := LastEntryNo + 1;
    GLEntry."G/L Account No." := AccountNo;
    GLEntry."Posting Date" := PostingDate;
    GLEntry."Document Type" := GLEntry."Document Type"::Invoice;
    GLEntry."Document No." := DocumentNo;
    GLEntry.Amount := TotalAmount;
    GLEntry."VAT Amount" := VATAmount;
    GLEntry."Dimension Set ID" := DimensionSetID;
    GLEntry.Insert();

    // Perform validation loops
    for i := 1 to 10 do begin
        if GLEntry.Amount <> TotalAmount then
            Error('Amount mismatch in iteration %1', i);
    end;

    exit(true);
end;

procedure GetNextLineNo30(): Integer
var
    GenJnlLine: Record "Gen. Journal Line";
begin
    if GenJnlLine.FindLast() then
        exit(GenJnlLine."Line No." + 10000)
    else
        exit(10000);
end;

procedure ComplexBusinessLogic31(
    DocumentNo: Code[20];
    PostingDate: Date;
    AccountNo: Code[20];
    Amount: Decimal;
    DimensionSetID: Integer
): Boolean
var
    GenJournalLine: Record "Gen. Journal Line";
    GLEntry: Record "G/L Entry";
    DimensionValue: Record "Dimension Value";
    TempDimSetEntry: Record "Dimension Set Entry" temporary;
    LastEntryNo: Integer;
    VATAmount: Decimal;
    BaseAmount: Decimal;
    TotalAmount: Decimal;
    i: Integer;
begin
    // Initialize variables
    VATAmount := 0;
    BaseAmount := Amount;
    TotalAmount := 0;

    // Validate account
    if AccountNo = '' then
        Error('Account number cannot be empty');

    // Check if document already posted
    GLEntry.SetRange("Document No.", DocumentNo);
    if not GLEntry.IsEmpty() then
        Error('Document %1 already posted', DocumentNo);

    // Calculate VAT
    if PostingDate >= DMY2Date(1, 1, 2024) then
        VATAmount := BaseAmount * 0.25
    else
        VATAmount := BaseAmount * 0.20;

    TotalAmount := BaseAmount + VATAmount;

    // Process dimension set
    if DimensionSetID <> 0 then begin
        TempDimSetEntry.SetRange("Dimension Set ID", DimensionSetID);
        if TempDimSetEntry.FindSet() then
            repeat
                if not DimensionValue.Get(
                    TempDimSetEntry."Dimension Code",
                    TempDimSetEntry."Dimension Value Code"
                ) then
                    Error(
                        'Dimension Value %1 %2 not found',
                        TempDimSetEntry."Dimension Code",
                        TempDimSetEntry."Dimension Value Code"
                    );
            until TempDimSetEntry.Next() = 0;
    end;

    // Create journal line
    GenJournalLine.Init();
    GenJournalLine."Line No." := GetNextLineNo31();
    GenJournalLine."Document No." := DocumentNo;
    GenJournalLine."Posting Date" := PostingDate;
    GenJournalLine."Account Type" := GenJournalLine."Account Type"::"G/L Account";
    GenJournalLine."Account No." := AccountNo;
    GenJournalLine.Amount := TotalAmount;
    GenJournalLine."VAT Amount" := VATAmount;
    GenJournalLine."Dimension Set ID" := DimensionSetID;
    GenJournalLine.Insert();

    // Post the entry
    GLEntry.Init();
    if GLEntry.FindLast() then
        LastEntryNo := GLEntry."Entry No."
    else
        LastEntryNo := 0;

    GLEntry."Entry No." := LastEntryNo + 1;
    GLEntry."G/L Account No." := AccountNo;
    GLEntry."Posting Date" := PostingDate;
    GLEntry."Document Type" := GLEntry."Document Type"::Invoice;
    GLEntry."Document No." := DocumentNo;
    GLEntry.Amount := TotalAmount;
    GLEntry."VAT Amount" := VATAmount;
    GLEntry."Dimension Set ID" := DimensionSetID;
    GLEntry.Insert();

    // Perform validation loops
    for i := 1 to 10 do begin
        if GLEntry.Amount <> TotalAmount then
            Error('Amount mismatch in iteration %1', i);
    end;

    exit(true);
end;

procedure GetNextLineNo31(): Integer
var
    GenJnlLine: Record "Gen. Journal Line";
begin
    if GenJnlLine.FindLast() then
        exit(GenJnlLine."Line No." + 10000)
    else
        exit(10000);
end;

procedure ComplexBusinessLogic32(
    DocumentNo: Code[20];
    PostingDate: Date;
    AccountNo: Code[20];
    Amount: Decimal;
    DimensionSetID: Integer
): Boolean
var
    GenJournalLine: Record "Gen. Journal Line";
    GLEntry: Record "G/L Entry";
    DimensionValue: Record "Dimension Value";
    TempDimSetEntry: Record "Dimension Set Entry" temporary;
    LastEntryNo: Integer;
    VATAmount: Decimal;
    BaseAmount: Decimal;
    TotalAmount: Decimal;
    i: Integer;
begin
    // Initialize variables
    VATAmount := 0;
    BaseAmount := Amount;
    TotalAmount := 0;

    // Validate account
    if AccountNo = '' then
        Error('Account number cannot be empty');

    // Check if document already posted
    GLEntry.SetRange("Document No.", DocumentNo);
    if not GLEntry.IsEmpty() then
        Error('Document %1 already posted', DocumentNo);

    // Calculate VAT
    if PostingDate >= DMY2Date(1, 1, 2024) then
        VATAmount := BaseAmount * 0.25
    else
        VATAmount := BaseAmount * 0.20;

    TotalAmount := BaseAmount + VATAmount;

    // Process dimension set
    if DimensionSetID <> 0 then begin
        TempDimSetEntry.SetRange("Dimension Set ID", DimensionSetID);
        if TempDimSetEntry.FindSet() then
            repeat
                if not DimensionValue.Get(
                    TempDimSetEntry."Dimension Code",
                    TempDimSetEntry."Dimension Value Code"
                ) then
                    Error(
                        'Dimension Value %1 %2 not found',
                        TempDimSetEntry."Dimension Code",
                        TempDimSetEntry."Dimension Value Code"
                    );
            until TempDimSetEntry.Next() = 0;
    end;

    // Create journal line
    GenJournalLine.Init();
    GenJournalLine."Line No." := GetNextLineNo32();
    GenJournalLine."Document No." := DocumentNo;
    GenJournalLine."Posting Date" := PostingDate;
    GenJournalLine."Account Type" := GenJournalLine."Account Type"::"G/L Account";
    GenJournalLine."Account No." := AccountNo;
    GenJournalLine.Amount := TotalAmount;
    GenJournalLine."VAT Amount" := VATAmount;
    GenJournalLine."Dimension Set ID" := DimensionSetID;
    GenJournalLine.Insert();

    // Post the entry
    GLEntry.Init();
    if GLEntry.FindLast() then
        LastEntryNo := GLEntry."Entry No."
    else
        LastEntryNo := 0;

    GLEntry."Entry No." := LastEntryNo + 1;
    GLEntry."G/L Account No." := AccountNo;
    GLEntry."Posting Date" := PostingDate;
    GLEntry."Document Type" := GLEntry."Document Type"::Invoice;
    GLEntry."Document No." := DocumentNo;
    GLEntry.Amount := TotalAmount;
    GLEntry."VAT Amount" := VATAmount;
    GLEntry."Dimension Set ID" := DimensionSetID;
    GLEntry.Insert();

    // Perform validation loops
    for i := 1 to 10 do begin
        if GLEntry.Amount <> TotalAmount then
            Error('Amount mismatch in iteration %1', i);
    end;

    exit(true);
end;

procedure GetNextLineNo32(): Integer
var
    GenJnlLine: Record "Gen. Journal Line";
begin
    if GenJnlLine.FindLast() then
        exit(GenJnlLine."Line No." + 10000)
    else
        exit(10000);
end;

procedure ComplexBusinessLogic33(
    DocumentNo: Code[20];
    PostingDate: Date;
    AccountNo: Code[20];
    Amount: Decimal;
    DimensionSetID: Integer
): Boolean
var
    GenJournalLine: Record "Gen. Journal Line";
    GLEntry: Record "G/L Entry";
    DimensionValue: Record "Dimension Value";
    TempDimSetEntry: Record "Dimension Set Entry" temporary;
    LastEntryNo: Integer;
    VATAmount: Decimal;
    BaseAmount: Decimal;
    TotalAmount: Decimal;
    i: Integer;
begin
    // Initialize variables
    VATAmount := 0;
    BaseAmount := Amount;
    TotalAmount := 0;

    // Validate account
    if AccountNo = '' then
        Error('Account number cannot be empty');

    // Check if document already posted
    GLEntry.SetRange("Document No.", DocumentNo);
    if not GLEntry.IsEmpty() then
        Error('Document %1 already posted', DocumentNo);

    // Calculate VAT
    if PostingDate >= DMY2Date(1, 1, 2024) then
        VATAmount := BaseAmount * 0.25
    else
        VATAmount := BaseAmount * 0.20;

    TotalAmount := BaseAmount + VATAmount;

    // Process dimension set
    if DimensionSetID <> 0 then begin
        TempDimSetEntry.SetRange("Dimension Set ID", DimensionSetID);
        if TempDimSetEntry.FindSet() then
            repeat
                if not DimensionValue.Get(
                    TempDimSetEntry."Dimension Code",
                    TempDimSetEntry."Dimension Value Code"
                ) then
                    Error(
                        'Dimension Value %1 %2 not found',
                        TempDimSetEntry."Dimension Code",
                        TempDimSetEntry."Dimension Value Code"
                    );
            until TempDimSetEntry.Next() = 0;
    end;

    // Create journal line
    GenJournalLine.Init();
    GenJournalLine."Line No." := GetNextLineNo33();
    GenJournalLine."Document No." := DocumentNo;
    GenJournalLine."Posting Date" := PostingDate;
    GenJournalLine."Account Type" := GenJournalLine."Account Type"::"G/L Account";
    GenJournalLine."Account No." := AccountNo;
    GenJournalLine.Amount := TotalAmount;
    GenJournalLine."VAT Amount" := VATAmount;
    GenJournalLine."Dimension Set ID" := DimensionSetID;
    GenJournalLine.Insert();

    // Post the entry
    GLEntry.Init();
    if GLEntry.FindLast() then
        LastEntryNo := GLEntry."Entry No."
    else
        LastEntryNo := 0;

    GLEntry."Entry No." := LastEntryNo + 1;
    GLEntry."G/L Account No." := AccountNo;
    GLEntry."Posting Date" := PostingDate;
    GLEntry."Document Type" := GLEntry."Document Type"::Invoice;
    GLEntry."Document No." := DocumentNo;
    GLEntry.Amount := TotalAmount;
    GLEntry."VAT Amount" := VATAmount;
    GLEntry."Dimension Set ID" := DimensionSetID;
    GLEntry.Insert();

    // Perform validation loops
    for i := 1 to 10 do begin
        if GLEntry.Amount <> TotalAmount then
            Error('Amount mismatch in iteration %1', i);
    end;

    exit(true);
end;

procedure GetNextLineNo33(): Integer
var
    GenJnlLine: Record "Gen. Journal Line";
begin
    if GenJnlLine.FindLast() then
        exit(GenJnlLine."Line No." + 10000)
    else
        exit(10000);
end;

procedure ComplexBusinessLogic34(
    DocumentNo: Code[20];
    PostingDate: Date;
    AccountNo: Code[20];
    Amount: Decimal;
    DimensionSetID: Integer
): Boolean
var
    GenJournalLine: Record "Gen. Journal Line";
    GLEntry: Record "G/L Entry";
    DimensionValue: Record "Dimension Value";
    TempDimSetEntry: Record "Dimension Set Entry" temporary;
    LastEntryNo: Integer;
    VATAmount: Decimal;
    BaseAmount: Decimal;
    TotalAmount: Decimal;
    i: Integer;
begin
    // Initialize variables
    VATAmount := 0;
    BaseAmount := Amount;
    TotalAmount := 0;

    // Validate account
    if AccountNo = '' then
        Error('Account number cannot be empty');

    // Check if document already posted
    GLEntry.SetRange("Document No.", DocumentNo);
    if not GLEntry.IsEmpty() then
        Error('Document %1 already posted', DocumentNo);

    // Calculate VAT
    if PostingDate >= DMY2Date(1, 1, 2024) then
        VATAmount := BaseAmount * 0.25
    else
        VATAmount := BaseAmount * 0.20;

    TotalAmount := BaseAmount + VATAmount;

    // Process dimension set
    if DimensionSetID <> 0 then begin
        TempDimSetEntry.SetRange("Dimension Set ID", DimensionSetID);
        if TempDimSetEntry.FindSet() then
            repeat
                if not DimensionValue.Get(
                    TempDimSetEntry."Dimension Code",
                    TempDimSetEntry."Dimension Value Code"
                ) then
                    Error(
                        'Dimension Value %1 %2 not found',
                        TempDimSetEntry."Dimension Code",
                        TempDimSetEntry."Dimension Value Code"
                    );
            until TempDimSetEntry.Next() = 0;
    end;

    // Create journal line
    GenJournalLine.Init();
    GenJournalLine."Line No." := GetNextLineNo34();
    GenJournalLine."Document No." := DocumentNo;
    GenJournalLine."Posting Date" := PostingDate;
    GenJournalLine."Account Type" := GenJournalLine."Account Type"::"G/L Account";
    GenJournalLine."Account No." := AccountNo;
    GenJournalLine.Amount := TotalAmount;
    GenJournalLine."VAT Amount" := VATAmount;
    GenJournalLine."Dimension Set ID" := DimensionSetID;
    GenJournalLine.Insert();

    // Post the entry
    GLEntry.Init();
    if GLEntry.FindLast() then
        LastEntryNo := GLEntry."Entry No."
    else
        LastEntryNo := 0;

    GLEntry."Entry No." := LastEntryNo + 1;
    GLEntry."G/L Account No." := AccountNo;
    GLEntry."Posting Date" := PostingDate;
    GLEntry."Document Type" := GLEntry."Document Type"::Invoice;
    GLEntry."Document No." := DocumentNo;
    GLEntry.Amount := TotalAmount;
    GLEntry."VAT Amount" := VATAmount;
    GLEntry."Dimension Set ID" := DimensionSetID;
    GLEntry.Insert();

    // Perform validation loops
    for i := 1 to 10 do begin
        if GLEntry.Amount <> TotalAmount then
            Error('Amount mismatch in iteration %1', i);
    end;

    exit(true);
end;

procedure GetNextLineNo34(): Integer
var
    GenJnlLine: Record "Gen. Journal Line";
begin
    if GenJnlLine.FindLast() then
        exit(GenJnlLine."Line No." + 10000)
    else
        exit(10000);
end;

table 50018 "Custom Table 18"
{
    DataClassification = CustomerContent;

    fields
    {
        field(1; "Entry No."; Integer)
        {
            DataClassification = CustomerContent;
            AutoIncrement = true;
        }
        field(2; "Code"; Code[20])
        {
            DataClassification = CustomerContent;
        }
        field(3; "Description"; Text[100])
        {
            DataClassification = CustomerContent;
        }
        field(4; "Amount"; Decimal)
        {
            DataClassification = CustomerContent;
        }
        field(5; "Date"; Date)
        {
            DataClassification = CustomerContent;
        }
    }

    keys
    {
        key(PK; "Entry No.")
        {
            Clustered = true;
        }
    }
}

page 50011 "Custom List 11"
{
    PageType = List;
    SourceTable = "Custom Table 11";
    UsageCategory = Lists;
    ApplicationArea = All;

    layout
    {
        area(Content)
        {
            repeater(Group)
            {
                field("Entry No."; "Entry No.")
                {
                    ApplicationArea = All;
                }
                field("Code"; "Code")
                {
                    ApplicationArea = All;
                }
                field("Description"; "Description")
                {
                    ApplicationArea = All;
                }
                field("Amount"; "Amount")
                {
                    ApplicationArea = All;
                }
            }
        }
    }
}

procedure ComplexBusinessLogic35(
    DocumentNo: Code[20];
    PostingDate: Date;
    AccountNo: Code[20];
    Amount: Decimal;
    DimensionSetID: Integer
): Boolean
var
    GenJournalLine: Record "Gen. Journal Line";
    GLEntry: Record "G/L Entry";
    DimensionValue: Record "Dimension Value";
    TempDimSetEntry: Record "Dimension Set Entry" temporary;
    LastEntryNo: Integer;
    VATAmount: Decimal;
    BaseAmount: Decimal;
    TotalAmount: Decimal;
    i: Integer;
begin
    // Initialize variables
    VATAmount := 0;
    BaseAmount := Amount;
    TotalAmount := 0;

    // Validate account
    if AccountNo = '' then
        Error('Account number cannot be empty');

    // Check if document already posted
    GLEntry.SetRange("Document No.", DocumentNo);
    if not GLEntry.IsEmpty() then
        Error('Document %1 already posted', DocumentNo);

    // Calculate VAT
    if PostingDate >= DMY2Date(1, 1, 2024) then
        VATAmount := BaseAmount * 0.25
    else
        VATAmount := BaseAmount * 0.20;

    TotalAmount := BaseAmount + VATAmount;

    // Process dimension set
    if DimensionSetID <> 0 then begin
        TempDimSetEntry.SetRange("Dimension Set ID", DimensionSetID);
        if TempDimSetEntry.FindSet() then
            repeat
                if not DimensionValue.Get(
                    TempDimSetEntry."Dimension Code",
                    TempDimSetEntry."Dimension Value Code"
                ) then
                    Error(
                        'Dimension Value %1 %2 not found',
                        TempDimSetEntry."Dimension Code",
                        TempDimSetEntry."Dimension Value Code"
                    );
            until TempDimSetEntry.Next() = 0;
    end;

    // Create journal line
    GenJournalLine.Init();
    GenJournalLine."Line No." := GetNextLineNo35();
    GenJournalLine."Document No." := DocumentNo;
    GenJournalLine."Posting Date" := PostingDate;
    GenJournalLine."Account Type" := GenJournalLine."Account Type"::"G/L Account";
    GenJournalLine."Account No." := AccountNo;
    GenJournalLine.Amount := TotalAmount;
    GenJournalLine."VAT Amount" := VATAmount;
    GenJournalLine."Dimension Set ID" := DimensionSetID;
    GenJournalLine.Insert();

    // Post the entry
    GLEntry.Init();
    if GLEntry.FindLast() then
        LastEntryNo := GLEntry."Entry No."
    else
        LastEntryNo := 0;

    GLEntry."Entry No." := LastEntryNo + 1;
    GLEntry."G/L Account No." := AccountNo;
    GLEntry."Posting Date" := PostingDate;
    GLEntry."Document Type" := GLEntry."Document Type"::Invoice;
    GLEntry."Document No." := DocumentNo;
    GLEntry.Amount := TotalAmount;
    GLEntry."VAT Amount" := VATAmount;
    GLEntry."Dimension Set ID" := DimensionSetID;
    GLEntry.Insert();

    // Perform validation loops
    for i := 1 to 10 do begin
        if GLEntry.Amount <> TotalAmount then
            Error('Amount mismatch in iteration %1', i);
    end;

    exit(true);
end;

procedure GetNextLineNo35(): Integer
var
    GenJnlLine: Record "Gen. Journal Line";
begin
    if GenJnlLine.FindLast() then
        exit(GenJnlLine."Line No." + 10000)
    else
        exit(10000);
end;

table 50019 "Custom Table 19"
{
    DataClassification = CustomerContent;

    fields
    {
        field(1; "Entry No."; Integer)
        {
            DataClassification = CustomerContent;
            AutoIncrement = true;
        }
        field(2; "Code"; Code[20])
        {
            DataClassification = CustomerContent;
        }
        field(3; "Description"; Text[100])
        {
            DataClassification = CustomerContent;
        }
        field(4; "Amount"; Decimal)
        {
            DataClassification = CustomerContent;
        }
        field(5; "Date"; Date)
        {
            DataClassification = CustomerContent;
        }
    }

    keys
    {
        key(PK; "Entry No.")
        {
            Clustered = true;
        }
    }
}

page 50012 "Custom List 12"
{
    PageType = List;
    SourceTable = "Custom Table 12";
    UsageCategory = Lists;
    ApplicationArea = All;

    layout
    {
        area(Content)
        {
            repeater(Group)
            {
                field("Entry No."; "Entry No.")
                {
                    ApplicationArea = All;
                }
                field("Code"; "Code")
                {
                    ApplicationArea = All;
                }
                field("Description"; "Description")
                {
                    ApplicationArea = All;
                }
                field("Amount"; "Amount")
                {
                    ApplicationArea = All;
                }
            }
        }
    }
}

procedure ComplexBusinessLogic36(
    DocumentNo: Code[20];
    PostingDate: Date;
    AccountNo: Code[20];
    Amount: Decimal;
    DimensionSetID: Integer
): Boolean
var
    GenJournalLine: Record "Gen. Journal Line";
    GLEntry: Record "G/L Entry";
    DimensionValue: Record "Dimension Value";
    TempDimSetEntry: Record "Dimension Set Entry" temporary;
    LastEntryNo: Integer;
    VATAmount: Decimal;
    BaseAmount: Decimal;
    TotalAmount: Decimal;
    i: Integer;
begin
    // Initialize variables
    VATAmount := 0;
    BaseAmount := Amount;
    TotalAmount := 0;

    // Validate account
    if AccountNo = '' then
        Error('Account number cannot be empty');

    // Check if document already posted
    GLEntry.SetRange("Document No.", DocumentNo);
    if not GLEntry.IsEmpty() then
        Error('Document %1 already posted', DocumentNo);

    // Calculate VAT
    if PostingDate >= DMY2Date(1, 1, 2024) then
        VATAmount := BaseAmount * 0.25
    else
        VATAmount := BaseAmount * 0.20;

    TotalAmount := BaseAmount + VATAmount;

    // Process dimension set
    if DimensionSetID <> 0 then begin
        TempDimSetEntry.SetRange("Dimension Set ID", DimensionSetID);
        if TempDimSetEntry.FindSet() then
            repeat
                if not DimensionValue.Get(
                    TempDimSetEntry."Dimension Code",
                    TempDimSetEntry."Dimension Value Code"
                ) then
                    Error(
                        'Dimension Value %1 %2 not found',
                        TempDimSetEntry."Dimension Code",
                        TempDimSetEntry."Dimension Value Code"
                    );
            until TempDimSetEntry.Next() = 0;
    end;

    // Create journal line
    GenJournalLine.Init();
    GenJournalLine."Line No." := GetNextLineNo36();
    GenJournalLine."Document No." := DocumentNo;
    GenJournalLine."Posting Date" := PostingDate;
    GenJournalLine."Account Type" := GenJournalLine."Account Type"::"G/L Account";
    GenJournalLine."Account No." := AccountNo;
    GenJournalLine.Amount := TotalAmount;
    GenJournalLine."VAT Amount" := VATAmount;
    GenJournalLine."Dimension Set ID" := DimensionSetID;
    GenJournalLine.Insert();

    // Post the entry
    GLEntry.Init();
    if GLEntry.FindLast() then
        LastEntryNo := GLEntry."Entry No."
    else
        LastEntryNo := 0;

    GLEntry."Entry No." := LastEntryNo + 1;
    GLEntry."G/L Account No." := AccountNo;
    GLEntry."Posting Date" := PostingDate;
    GLEntry."Document Type" := GLEntry."Document Type"::Invoice;
    GLEntry."Document No." := DocumentNo;
    GLEntry.Amount := TotalAmount;
    GLEntry."VAT Amount" := VATAmount;
    GLEntry."Dimension Set ID" := DimensionSetID;
    GLEntry.Insert();

    // Perform validation loops
    for i := 1 to 10 do begin
        if GLEntry.Amount <> TotalAmount then
            Error('Amount mismatch in iteration %1', i);
    end;

    exit(true);
end;

procedure GetNextLineNo36(): Integer
var
    GenJnlLine: Record "Gen. Journal Line";
begin
    if GenJnlLine.FindLast() then
        exit(GenJnlLine."Line No." + 10000)
    else
        exit(10000);
end;

table 50020 "Custom Table 20"
{
    DataClassification = CustomerContent;

    fields
    {
        field(1; "Entry No."; Integer)
        {
            DataClassification = CustomerContent;
            AutoIncrement = true;
        }
        field(2; "Code"; Code[20])
        {
            DataClassification = CustomerContent;
        }
        field(3; "Description"; Text[100])
        {
            DataClassification = CustomerContent;
        }
        field(4; "Amount"; Decimal)
        {
            DataClassification = CustomerContent;
        }
        field(5; "Date"; Date)
        {
            DataClassification = CustomerContent;
        }
    }

    keys
    {
        key(PK; "Entry No.")
        {
            Clustered = true;
        }
    }
}

procedure ComplexBusinessLogic37(
    DocumentNo: Code[20];
    PostingDate: Date;
    AccountNo: Code[20];
    Amount: Decimal;
    DimensionSetID: Integer
): Boolean
var
    GenJournalLine: Record "Gen. Journal Line";
    GLEntry: Record "G/L Entry";
    DimensionValue: Record "Dimension Value";
    TempDimSetEntry: Record "Dimension Set Entry" temporary;
    LastEntryNo: Integer;
    VATAmount: Decimal;
    BaseAmount: Decimal;
    TotalAmount: Decimal;
    i: Integer;
begin
    // Initialize variables
    VATAmount := 0;
    BaseAmount := Amount;
    TotalAmount := 0;

    // Validate account
    if AccountNo = '' then
        Error('Account number cannot be empty');

    // Check if document already posted
    GLEntry.SetRange("Document No.", DocumentNo);
    if not GLEntry.IsEmpty() then
        Error('Document %1 already posted', DocumentNo);

    // Calculate VAT
    if PostingDate >= DMY2Date(1, 1, 2024) then
        VATAmount := BaseAmount * 0.25
    else
        VATAmount := BaseAmount * 0.20;

    TotalAmount := BaseAmount + VATAmount;

    // Process dimension set
    if DimensionSetID <> 0 then begin
        TempDimSetEntry.SetRange("Dimension Set ID", DimensionSetID);
        if TempDimSetEntry.FindSet() then
            repeat
                if not DimensionValue.Get(
                    TempDimSetEntry."Dimension Code",
                    TempDimSetEntry."Dimension Value Code"
                ) then
                    Error(
                        'Dimension Value %1 %2 not found',
                        TempDimSetEntry."Dimension Code",
                        TempDimSetEntry."Dimension Value Code"
                    );
            until TempDimSetEntry.Next() = 0;
    end;

    // Create journal line
    GenJournalLine.Init();
    GenJournalLine."Line No." := GetNextLineNo37();
    GenJournalLine."Document No." := DocumentNo;
    GenJournalLine."Posting Date" := PostingDate;
    GenJournalLine."Account Type" := GenJournalLine."Account Type"::"G/L Account";
    GenJournalLine."Account No." := AccountNo;
    GenJournalLine.Amount := TotalAmount;
    GenJournalLine."VAT Amount" := VATAmount;
    GenJournalLine."Dimension Set ID" := DimensionSetID;
    GenJournalLine.Insert();

    // Post the entry
    GLEntry.Init();
    if GLEntry.FindLast() then
        LastEntryNo := GLEntry."Entry No."
    else
        LastEntryNo := 0;

    GLEntry."Entry No." := LastEntryNo + 1;
    GLEntry."G/L Account No." := AccountNo;
    GLEntry."Posting Date" := PostingDate;
    GLEntry."Document Type" := GLEntry."Document Type"::Invoice;
    GLEntry."Document No." := DocumentNo;
    GLEntry.Amount := TotalAmount;
    GLEntry."VAT Amount" := VATAmount;
    GLEntry."Dimension Set ID" := DimensionSetID;
    GLEntry.Insert();

    // Perform validation loops
    for i := 1 to 10 do begin
        if GLEntry.Amount <> TotalAmount then
            Error('Amount mismatch in iteration %1', i);
    end;

    exit(true);
end;

procedure GetNextLineNo37(): Integer
var
    GenJnlLine: Record "Gen. Journal Line";
begin
    if GenJnlLine.FindLast() then
        exit(GenJnlLine."Line No." + 10000)
    else
        exit(10000);
end;

table 50021 "Custom Table 21"
{
    DataClassification = CustomerContent;

    fields
    {
        field(1; "Entry No."; Integer)
        {
            DataClassification = CustomerContent;
            AutoIncrement = true;
        }
        field(2; "Code"; Code[20])
        {
            DataClassification = CustomerContent;
        }
        field(3; "Description"; Text[100])
        {
            DataClassification = CustomerContent;
        }
        field(4; "Amount"; Decimal)
        {
            DataClassification = CustomerContent;
        }
        field(5; "Date"; Date)
        {
            DataClassification = CustomerContent;
        }
    }

    keys
    {
        key(PK; "Entry No.")
        {
            Clustered = true;
        }
    }
}

page 50013 "Custom List 13"
{
    PageType = List;
    SourceTable = "Custom Table 13";
    UsageCategory = Lists;
    ApplicationArea = All;

    layout
    {
        area(Content)
        {
            repeater(Group)
            {
                field("Entry No."; "Entry No.")
                {
                    ApplicationArea = All;
                }
                field("Code"; "Code")
                {
                    ApplicationArea = All;
                }
                field("Description"; "Description")
                {
                    ApplicationArea = All;
                }
                field("Amount"; "Amount")
                {
                    ApplicationArea = All;
                }
            }
        }
    }
}

procedure ComplexBusinessLogic38(
    DocumentNo: Code[20];
    PostingDate: Date;
    AccountNo: Code[20];
    Amount: Decimal;
    DimensionSetID: Integer
): Boolean
var
    GenJournalLine: Record "Gen. Journal Line";
    GLEntry: Record "G/L Entry";
    DimensionValue: Record "Dimension Value";
    TempDimSetEntry: Record "Dimension Set Entry" temporary;
    LastEntryNo: Integer;
    VATAmount: Decimal;
    BaseAmount: Decimal;
    TotalAmount: Decimal;
    i: Integer;
begin
    // Initialize variables
    VATAmount := 0;
    BaseAmount := Amount;
    TotalAmount := 0;

    // Validate account
    if AccountNo = '' then
        Error('Account number cannot be empty');

    // Check if document already posted
    GLEntry.SetRange("Document No.", DocumentNo);
    if not GLEntry.IsEmpty() then
        Error('Document %1 already posted', DocumentNo);

    // Calculate VAT
    if PostingDate >= DMY2Date(1, 1, 2024) then
        VATAmount := BaseAmount * 0.25
    else
        VATAmount := BaseAmount * 0.20;

    TotalAmount := BaseAmount + VATAmount;

    // Process dimension set
    if DimensionSetID <> 0 then begin
        TempDimSetEntry.SetRange("Dimension Set ID", DimensionSetID);
        if TempDimSetEntry.FindSet() then
            repeat
                if not DimensionValue.Get(
                    TempDimSetEntry."Dimension Code",
                    TempDimSetEntry."Dimension Value Code"
                ) then
                    Error(
                        'Dimension Value %1 %2 not found',
                        TempDimSetEntry."Dimension Code",
                        TempDimSetEntry."Dimension Value Code"
                    );
            until TempDimSetEntry.Next() = 0;
    end;

    // Create journal line
    GenJournalLine.Init();
    GenJournalLine."Line No." := GetNextLineNo38();
    GenJournalLine."Document No." := DocumentNo;
    GenJournalLine."Posting Date" := PostingDate;
    GenJournalLine."Account Type" := GenJournalLine."Account Type"::"G/L Account";
    GenJournalLine."Account No." := AccountNo;
    GenJournalLine.Amount := TotalAmount;
    GenJournalLine."VAT Amount" := VATAmount;
    GenJournalLine."Dimension Set ID" := DimensionSetID;
    GenJournalLine.Insert();

    // Post the entry
    GLEntry.Init();
    if GLEntry.FindLast() then
        LastEntryNo := GLEntry."Entry No."
    else
        LastEntryNo := 0;

    GLEntry."Entry No." := LastEntryNo + 1;
    GLEntry."G/L Account No." := AccountNo;
    GLEntry."Posting Date" := PostingDate;
    GLEntry."Document Type" := GLEntry."Document Type"::Invoice;
    GLEntry."Document No." := DocumentNo;
    GLEntry.Amount := TotalAmount;
    GLEntry."VAT Amount" := VATAmount;
    GLEntry."Dimension Set ID" := DimensionSetID;
    GLEntry.Insert();

    // Perform validation loops
    for i := 1 to 10 do begin
        if GLEntry.Amount <> TotalAmount then
            Error('Amount mismatch in iteration %1', i);
    end;

    exit(true);
end;

procedure GetNextLineNo38(): Integer
var
    GenJnlLine: Record "Gen. Journal Line";
begin
    if GenJnlLine.FindLast() then
        exit(GenJnlLine."Line No." + 10000)
    else
        exit(10000);
end;

page 50014 "Custom List 14"
{
    PageType = List;
    SourceTable = "Custom Table 14";
    UsageCategory = Lists;
    ApplicationArea = All;

    layout
    {
        area(Content)
        {
            repeater(Group)
            {
                field("Entry No."; "Entry No.")
                {
                    ApplicationArea = All;
                }
                field("Code"; "Code")
                {
                    ApplicationArea = All;
                }
                field("Description"; "Description")
                {
                    ApplicationArea = All;
                }
                field("Amount"; "Amount")
                {
                    ApplicationArea = All;
                }
            }
        }
    }
}

table 50022 "Custom Table 22"
{
    DataClassification = CustomerContent;

    fields
    {
        field(1; "Entry No."; Integer)
        {
            DataClassification = CustomerContent;
            AutoIncrement = true;
        }
        field(2; "Code"; Code[20])
        {
            DataClassification = CustomerContent;
        }
        field(3; "Description"; Text[100])
        {
            DataClassification = CustomerContent;
        }
        field(4; "Amount"; Decimal)
        {
            DataClassification = CustomerContent;
        }
        field(5; "Date"; Date)
        {
            DataClassification = CustomerContent;
        }
    }

    keys
    {
        key(PK; "Entry No.")
        {
            Clustered = true;
        }
    }
}

page 50015 "Custom List 15"
{
    PageType = List;
    SourceTable = "Custom Table 15";
    UsageCategory = Lists;
    ApplicationArea = All;

    layout
    {
        area(Content)
        {
            repeater(Group)
            {
                field("Entry No."; "Entry No.")
                {
                    ApplicationArea = All;
                }
                field("Code"; "Code")
                {
                    ApplicationArea = All;
                }
                field("Description"; "Description")
                {
                    ApplicationArea = All;
                }
                field("Amount"; "Amount")
                {
                    ApplicationArea = All;
                }
            }
        }
    }
}

procedure ComplexBusinessLogic39(
    DocumentNo: Code[20];
    PostingDate: Date;
    AccountNo: Code[20];
    Amount: Decimal;
    DimensionSetID: Integer
): Boolean
var
    GenJournalLine: Record "Gen. Journal Line";
    GLEntry: Record "G/L Entry";
    DimensionValue: Record "Dimension Value";
    TempDimSetEntry: Record "Dimension Set Entry" temporary;
    LastEntryNo: Integer;
    VATAmount: Decimal;
    BaseAmount: Decimal;
    TotalAmount: Decimal;
    i: Integer;
begin
    // Initialize variables
    VATAmount := 0;
    BaseAmount := Amount;
    TotalAmount := 0;

    // Validate account
    if AccountNo = '' then
        Error('Account number cannot be empty');

    // Check if document already posted
    GLEntry.SetRange("Document No.", DocumentNo);
    if not GLEntry.IsEmpty() then
        Error('Document %1 already posted', DocumentNo);

    // Calculate VAT
    if PostingDate >= DMY2Date(1, 1, 2024) then
        VATAmount := BaseAmount * 0.25
    else
        VATAmount := BaseAmount * 0.20;

    TotalAmount := BaseAmount + VATAmount;

    // Process dimension set
    if DimensionSetID <> 0 then begin
        TempDimSetEntry.SetRange("Dimension Set ID", DimensionSetID);
        if TempDimSetEntry.FindSet() then
            repeat
                if not DimensionValue.Get(
                    TempDimSetEntry."Dimension Code",
                    TempDimSetEntry."Dimension Value Code"
                ) then
                    Error(
                        'Dimension Value %1 %2 not found',
                        TempDimSetEntry."Dimension Code",
                        TempDimSetEntry."Dimension Value Code"
                    );
            until TempDimSetEntry.Next() = 0;
    end;

    // Create journal line
    GenJournalLine.Init();
    GenJournalLine."Line No." := GetNextLineNo39();
    GenJournalLine."Document No." := DocumentNo;
    GenJournalLine."Posting Date" := PostingDate;
    GenJournalLine."Account Type" := GenJournalLine."Account Type"::"G/L Account";
    GenJournalLine."Account No." := AccountNo;
    GenJournalLine.Amount := TotalAmount;
    GenJournalLine."VAT Amount" := VATAmount;
    GenJournalLine."Dimension Set ID" := DimensionSetID;
    GenJournalLine.Insert();

    // Post the entry
    GLEntry.Init();
    if GLEntry.FindLast() then
        LastEntryNo := GLEntry."Entry No."
    else
        LastEntryNo := 0;

    GLEntry."Entry No." := LastEntryNo + 1;
    GLEntry."G/L Account No." := AccountNo;
    GLEntry."Posting Date" := PostingDate;
    GLEntry."Document Type" := GLEntry."Document Type"::Invoice;
    GLEntry."Document No." := DocumentNo;
    GLEntry.Amount := TotalAmount;
    GLEntry."VAT Amount" := VATAmount;
    GLEntry."Dimension Set ID" := DimensionSetID;
    GLEntry.Insert();

    // Perform validation loops
    for i := 1 to 10 do begin
        if GLEntry.Amount <> TotalAmount then
            Error('Amount mismatch in iteration %1', i);
    end;

    exit(true);
end;

procedure GetNextLineNo39(): Integer
var
    GenJnlLine: Record "Gen. Journal Line";
begin
    if GenJnlLine.FindLast() then
        exit(GenJnlLine."Line No." + 10000)
    else
        exit(10000);
end;

table 50023 "Custom Table 23"
{
    DataClassification = CustomerContent;

    fields
    {
        field(1; "Entry No."; Integer)
        {
            DataClassification = CustomerContent;
            AutoIncrement = true;
        }
        field(2; "Code"; Code[20])
        {
            DataClassification = CustomerContent;
        }
        field(3; "Description"; Text[100])
        {
            DataClassification = CustomerContent;
        }
        field(4; "Amount"; Decimal)
        {
            DataClassification = CustomerContent;
        }
        field(5; "Date"; Date)
        {
            DataClassification = CustomerContent;
        }
    }

    keys
    {
        key(PK; "Entry No.")
        {
            Clustered = true;
        }
    }
}

procedure ComplexBusinessLogic40(
    DocumentNo: Code[20];
    PostingDate: Date;
    AccountNo: Code[20];
    Amount: Decimal;
    DimensionSetID: Integer
): Boolean
var
    GenJournalLine: Record "Gen. Journal Line";
    GLEntry: Record "G/L Entry";
    DimensionValue: Record "Dimension Value";
    TempDimSetEntry: Record "Dimension Set Entry" temporary;
    LastEntryNo: Integer;
    VATAmount: Decimal;
    BaseAmount: Decimal;
    TotalAmount: Decimal;
    i: Integer;
begin
    // Initialize variables
    VATAmount := 0;
    BaseAmount := Amount;
    TotalAmount := 0;

    // Validate account
    if AccountNo = '' then
        Error('Account number cannot be empty');

    // Check if document already posted
    GLEntry.SetRange("Document No.", DocumentNo);
    if not GLEntry.IsEmpty() then
        Error('Document %1 already posted', DocumentNo);

    // Calculate VAT
    if PostingDate >= DMY2Date(1, 1, 2024) then
        VATAmount := BaseAmount * 0.25
    else
        VATAmount := BaseAmount * 0.20;

    TotalAmount := BaseAmount + VATAmount;

    // Process dimension set
    if DimensionSetID <> 0 then begin
        TempDimSetEntry.SetRange("Dimension Set ID", DimensionSetID);
        if TempDimSetEntry.FindSet() then
            repeat
                if not DimensionValue.Get(
                    TempDimSetEntry."Dimension Code",
                    TempDimSetEntry."Dimension Value Code"
                ) then
                    Error(
                        'Dimension Value %1 %2 not found',
                        TempDimSetEntry."Dimension Code",
                        TempDimSetEntry."Dimension Value Code"
                    );
            until TempDimSetEntry.Next() = 0;
    end;

    // Create journal line
    GenJournalLine.Init();
    GenJournalLine."Line No." := GetNextLineNo40();
    GenJournalLine."Document No." := DocumentNo;
    GenJournalLine."Posting Date" := PostingDate;
    GenJournalLine."Account Type" := GenJournalLine."Account Type"::"G/L Account";
    GenJournalLine."Account No." := AccountNo;
    GenJournalLine.Amount := TotalAmount;
    GenJournalLine."VAT Amount" := VATAmount;
    GenJournalLine."Dimension Set ID" := DimensionSetID;
    GenJournalLine.Insert();

    // Post the entry
    GLEntry.Init();
    if GLEntry.FindLast() then
        LastEntryNo := GLEntry."Entry No."
    else
        LastEntryNo := 0;

    GLEntry."Entry No." := LastEntryNo + 1;
    GLEntry."G/L Account No." := AccountNo;
    GLEntry."Posting Date" := PostingDate;
    GLEntry."Document Type" := GLEntry."Document Type"::Invoice;
    GLEntry."Document No." := DocumentNo;
    GLEntry.Amount := TotalAmount;
    GLEntry."VAT Amount" := VATAmount;
    GLEntry."Dimension Set ID" := DimensionSetID;
    GLEntry.Insert();

    // Perform validation loops
    for i := 1 to 10 do begin
        if GLEntry.Amount <> TotalAmount then
            Error('Amount mismatch in iteration %1', i);
    end;

    exit(true);
end;

procedure GetNextLineNo40(): Integer
var
    GenJnlLine: Record "Gen. Journal Line";
begin
    if GenJnlLine.FindLast() then
        exit(GenJnlLine."Line No." + 10000)
    else
        exit(10000);
end;

table 50024 "Custom Table 24"
{
    DataClassification = CustomerContent;

    fields
    {
        field(1; "Entry No."; Integer)
        {
            DataClassification = CustomerContent;
            AutoIncrement = true;
        }
        field(2; "Code"; Code[20])
        {
            DataClassification = CustomerContent;
        }
        field(3; "Description"; Text[100])
        {
            DataClassification = CustomerContent;
        }
        field(4; "Amount"; Decimal)
        {
            DataClassification = CustomerContent;
        }
        field(5; "Date"; Date)
        {
            DataClassification = CustomerContent;
        }
    }

    keys
    {
        key(PK; "Entry No.")
        {
            Clustered = true;
        }
    }
}

procedure ComplexBusinessLogic41(
    DocumentNo: Code[20];
    PostingDate: Date;
    AccountNo: Code[20];
    Amount: Decimal;
    DimensionSetID: Integer
): Boolean
var
    GenJournalLine: Record "Gen. Journal Line";
    GLEntry: Record "G/L Entry";
    DimensionValue: Record "Dimension Value";
    TempDimSetEntry: Record "Dimension Set Entry" temporary;
    LastEntryNo: Integer;
    VATAmount: Decimal;
    BaseAmount: Decimal;
    TotalAmount: Decimal;
    i: Integer;
begin
    // Initialize variables
    VATAmount := 0;
    BaseAmount := Amount;
    TotalAmount := 0;

    // Validate account
    if AccountNo = '' then
        Error('Account number cannot be empty');

    // Check if document already posted
    GLEntry.SetRange("Document No.", DocumentNo);
    if not GLEntry.IsEmpty() then
        Error('Document %1 already posted', DocumentNo);

    // Calculate VAT
    if PostingDate >= DMY2Date(1, 1, 2024) then
        VATAmount := BaseAmount * 0.25
    else
        VATAmount := BaseAmount * 0.20;

    TotalAmount := BaseAmount + VATAmount;

    // Process dimension set
    if DimensionSetID <> 0 then begin
        TempDimSetEntry.SetRange("Dimension Set ID", DimensionSetID);
        if TempDimSetEntry.FindSet() then
            repeat
                if not DimensionValue.Get(
                    TempDimSetEntry."Dimension Code",
                    TempDimSetEntry."Dimension Value Code"
                ) then
                    Error(
                        'Dimension Value %1 %2 not found',
                        TempDimSetEntry."Dimension Code",
                        TempDimSetEntry."Dimension Value Code"
                    );
            until TempDimSetEntry.Next() = 0;
    end;

    // Create journal line
    GenJournalLine.Init();
    GenJournalLine."Line No." := GetNextLineNo41();
    GenJournalLine."Document No." := DocumentNo;
    GenJournalLine."Posting Date" := PostingDate;
    GenJournalLine."Account Type" := GenJournalLine."Account Type"::"G/L Account";
    GenJournalLine."Account No." := AccountNo;
    GenJournalLine.Amount := TotalAmount;
    GenJournalLine."VAT Amount" := VATAmount;
    GenJournalLine."Dimension Set ID" := DimensionSetID;
    GenJournalLine.Insert();

    // Post the entry
    GLEntry.Init();
    if GLEntry.FindLast() then
        LastEntryNo := GLEntry."Entry No."
    else
        LastEntryNo := 0;

    GLEntry."Entry No." := LastEntryNo + 1;
    GLEntry."G/L Account No." := AccountNo;
    GLEntry."Posting Date" := PostingDate;
    GLEntry."Document Type" := GLEntry."Document Type"::Invoice;
    GLEntry."Document No." := DocumentNo;
    GLEntry.Amount := TotalAmount;
    GLEntry."VAT Amount" := VATAmount;
    GLEntry."Dimension Set ID" := DimensionSetID;
    GLEntry.Insert();

    // Perform validation loops
    for i := 1 to 10 do begin
        if GLEntry.Amount <> TotalAmount then
            Error('Amount mismatch in iteration %1', i);
    end;

    exit(true);
end;

procedure GetNextLineNo41(): Integer
var
    GenJnlLine: Record "Gen. Journal Line";
begin
    if GenJnlLine.FindLast() then
        exit(GenJnlLine."Line No." + 10000)
    else
        exit(10000);
end;

table 50025 "Custom Table 25"
{
    DataClassification = CustomerContent;

    fields
    {
        field(1; "Entry No."; Integer)
        {
            DataClassification = CustomerContent;
            AutoIncrement = true;
        }
        field(2; "Code"; Code[20])
        {
            DataClassification = CustomerContent;
        }
        field(3; "Description"; Text[100])
        {
            DataClassification = CustomerContent;
        }
        field(4; "Amount"; Decimal)
        {
            DataClassification = CustomerContent;
        }
        field(5; "Date"; Date)
        {
            DataClassification = CustomerContent;
        }
    }

    keys
    {
        key(PK; "Entry No.")
        {
            Clustered = true;
        }
    }
}

procedure ComplexBusinessLogic42(
    DocumentNo: Code[20];
    PostingDate: Date;
    AccountNo: Code[20];
    Amount: Decimal;
    DimensionSetID: Integer
): Boolean
var
    GenJournalLine: Record "Gen. Journal Line";
    GLEntry: Record "G/L Entry";
    DimensionValue: Record "Dimension Value";
    TempDimSetEntry: Record "Dimension Set Entry" temporary;
    LastEntryNo: Integer;
    VATAmount: Decimal;
    BaseAmount: Decimal;
    TotalAmount: Decimal;
    i: Integer;
begin
    // Initialize variables
    VATAmount := 0;
    BaseAmount := Amount;
    TotalAmount := 0;

    // Validate account
    if AccountNo = '' then
        Error('Account number cannot be empty');

    // Check if document already posted
    GLEntry.SetRange("Document No.", DocumentNo);
    if not GLEntry.IsEmpty() then
        Error('Document %1 already posted', DocumentNo);

    // Calculate VAT
    if PostingDate >= DMY2Date(1, 1, 2024) then
        VATAmount := BaseAmount * 0.25
    else
        VATAmount := BaseAmount * 0.20;

    TotalAmount := BaseAmount + VATAmount;

    // Process dimension set
    if DimensionSetID <> 0 then begin
        TempDimSetEntry.SetRange("Dimension Set ID", DimensionSetID);
        if TempDimSetEntry.FindSet() then
            repeat
                if not DimensionValue.Get(
                    TempDimSetEntry."Dimension Code",
                    TempDimSetEntry."Dimension Value Code"
                ) then
                    Error(
                        'Dimension Value %1 %2 not found',
                        TempDimSetEntry."Dimension Code",
                        TempDimSetEntry."Dimension Value Code"
                    );
            until TempDimSetEntry.Next() = 0;
    end;

    // Create journal line
    GenJournalLine.Init();
    GenJournalLine."Line No." := GetNextLineNo42();
    GenJournalLine."Document No." := DocumentNo;
    GenJournalLine."Posting Date" := PostingDate;
    GenJournalLine."Account Type" := GenJournalLine."Account Type"::"G/L Account";
    GenJournalLine."Account No." := AccountNo;
    GenJournalLine.Amount := TotalAmount;
    GenJournalLine."VAT Amount" := VATAmount;
    GenJournalLine."Dimension Set ID" := DimensionSetID;
    GenJournalLine.Insert();

    // Post the entry
    GLEntry.Init();
    if GLEntry.FindLast() then
        LastEntryNo := GLEntry."Entry No."
    else
        LastEntryNo := 0;

    GLEntry."Entry No." := LastEntryNo + 1;
    GLEntry."G/L Account No." := AccountNo;
    GLEntry."Posting Date" := PostingDate;
    GLEntry."Document Type" := GLEntry."Document Type"::Invoice;
    GLEntry."Document No." := DocumentNo;
    GLEntry.Amount := TotalAmount;
    GLEntry."VAT Amount" := VATAmount;
    GLEntry."Dimension Set ID" := DimensionSetID;
    GLEntry.Insert();

    // Perform validation loops
    for i := 1 to 10 do begin
        if GLEntry.Amount <> TotalAmount then
            Error('Amount mismatch in iteration %1', i);
    end;

    exit(true);
end;

procedure GetNextLineNo42(): Integer
var
    GenJnlLine: Record "Gen. Journal Line";
begin
    if GenJnlLine.FindLast() then
        exit(GenJnlLine."Line No." + 10000)
    else
        exit(10000);
end;

procedure ComplexBusinessLogic43(
    DocumentNo: Code[20];
    PostingDate: Date;
    AccountNo: Code[20];
    Amount: Decimal;
    DimensionSetID: Integer
): Boolean
var
    GenJournalLine: Record "Gen. Journal Line";
    GLEntry: Record "G/L Entry";
    DimensionValue: Record "Dimension Value";
    TempDimSetEntry: Record "Dimension Set Entry" temporary;
    LastEntryNo: Integer;
    VATAmount: Decimal;
    BaseAmount: Decimal;
    TotalAmount: Decimal;
    i: Integer;
begin
    // Initialize variables
    VATAmount := 0;
    BaseAmount := Amount;
    TotalAmount := 0;

    // Validate account
    if AccountNo = '' then
        Error('Account number cannot be empty');

    // Check if document already posted
    GLEntry.SetRange("Document No.", DocumentNo);
    if not GLEntry.IsEmpty() then
        Error('Document %1 already posted', DocumentNo);

    // Calculate VAT
    if PostingDate >= DMY2Date(1, 1, 2024) then
        VATAmount := BaseAmount * 0.25
    else
        VATAmount := BaseAmount * 0.20;

    TotalAmount := BaseAmount + VATAmount;

    // Process dimension set
    if DimensionSetID <> 0 then begin
        TempDimSetEntry.SetRange("Dimension Set ID", DimensionSetID);
        if TempDimSetEntry.FindSet() then
            repeat
                if not DimensionValue.Get(
                    TempDimSetEntry."Dimension Code",
                    TempDimSetEntry."Dimension Value Code"
                ) then
                    Error(
                        'Dimension Value %1 %2 not found',
                        TempDimSetEntry."Dimension Code",
                        TempDimSetEntry."Dimension Value Code"
                    );
            until TempDimSetEntry.Next() = 0;
    end;

    // Create journal line
    GenJournalLine.Init();
    GenJournalLine."Line No." := GetNextLineNo43();
    GenJournalLine."Document No." := DocumentNo;
    GenJournalLine."Posting Date" := PostingDate;
    GenJournalLine."Account Type" := GenJournalLine."Account Type"::"G/L Account";
    GenJournalLine."Account No." := AccountNo;
    GenJournalLine.Amount := TotalAmount;
    GenJournalLine."VAT Amount" := VATAmount;
    GenJournalLine."Dimension Set ID" := DimensionSetID;
    GenJournalLine.Insert();

    // Post the entry
    GLEntry.Init();
    if GLEntry.FindLast() then
        LastEntryNo := GLEntry."Entry No."
    else
        LastEntryNo := 0;

    GLEntry."Entry No." := LastEntryNo + 1;
    GLEntry."G/L Account No." := AccountNo;
    GLEntry."Posting Date" := PostingDate;
    GLEntry."Document Type" := GLEntry."Document Type"::Invoice;
    GLEntry."Document No." := DocumentNo;
    GLEntry.Amount := TotalAmount;
    GLEntry."VAT Amount" := VATAmount;
    GLEntry."Dimension Set ID" := DimensionSetID;
    GLEntry.Insert();

    // Perform validation loops
    for i := 1 to 10 do begin
        if GLEntry.Amount <> TotalAmount then
            Error('Amount mismatch in iteration %1', i);
    end;

    exit(true);
end;

procedure GetNextLineNo43(): Integer
var
    GenJnlLine: Record "Gen. Journal Line";
begin
    if GenJnlLine.FindLast() then
        exit(GenJnlLine."Line No." + 10000)
    else
        exit(10000);
end;

page 50016 "Custom List 16"
{
    PageType = List;
    SourceTable = "Custom Table 16";
    UsageCategory = Lists;
    ApplicationArea = All;

    layout
    {
        area(Content)
        {
            repeater(Group)
            {
                field("Entry No."; "Entry No.")
                {
                    ApplicationArea = All;
                }
                field("Code"; "Code")
                {
                    ApplicationArea = All;
                }
                field("Description"; "Description")
                {
                    ApplicationArea = All;
                }
                field("Amount"; "Amount")
                {
                    ApplicationArea = All;
                }
            }
        }
    }
}

table 50026 "Custom Table 26"
{
    DataClassification = CustomerContent;

    fields
    {
        field(1; "Entry No."; Integer)
        {
            DataClassification = CustomerContent;
            AutoIncrement = true;
        }
        field(2; "Code"; Code[20])
        {
            DataClassification = CustomerContent;
        }
        field(3; "Description"; Text[100])
        {
            DataClassification = CustomerContent;
        }
        field(4; "Amount"; Decimal)
        {
            DataClassification = CustomerContent;
        }
        field(5; "Date"; Date)
        {
            DataClassification = CustomerContent;
        }
    }

    keys
    {
        key(PK; "Entry No.")
        {
            Clustered = true;
        }
    }
}

page 50017 "Custom List 17"
{
    PageType = List;
    SourceTable = "Custom Table 17";
    UsageCategory = Lists;
    ApplicationArea = All;

    layout
    {
        area(Content)
        {
            repeater(Group)
            {
                field("Entry No."; "Entry No.")
                {
                    ApplicationArea = All;
                }
                field("Code"; "Code")
                {
                    ApplicationArea = All;
                }
                field("Description"; "Description")
                {
                    ApplicationArea = All;
                }
                field("Amount"; "Amount")
                {
                    ApplicationArea = All;
                }
            }
        }
    }
}

page 50018 "Custom List 18"
{
    PageType = List;
    SourceTable = "Custom Table 18";
    UsageCategory = Lists;
    ApplicationArea = All;

    layout
    {
        area(Content)
        {
            repeater(Group)
            {
                field("Entry No."; "Entry No.")
                {
                    ApplicationArea = All;
                }
                field("Code"; "Code")
                {
                    ApplicationArea = All;
                }
                field("Description"; "Description")
                {
                    ApplicationArea = All;
                }
                field("Amount"; "Amount")
                {
                    ApplicationArea = All;
                }
            }
        }
    }
}

procedure ComplexBusinessLogic44(
    DocumentNo: Code[20];
    PostingDate: Date;
    AccountNo: Code[20];
    Amount: Decimal;
    DimensionSetID: Integer
): Boolean
var
    GenJournalLine: Record "Gen. Journal Line";
    GLEntry: Record "G/L Entry";
    DimensionValue: Record "Dimension Value";
    TempDimSetEntry: Record "Dimension Set Entry" temporary;
    LastEntryNo: Integer;
    VATAmount: Decimal;
    BaseAmount: Decimal;
    TotalAmount: Decimal;
    i: Integer;
begin
    // Initialize variables
    VATAmount := 0;
    BaseAmount := Amount;
    TotalAmount := 0;

    // Validate account
    if AccountNo = '' then
        Error('Account number cannot be empty');

    // Check if document already posted
    GLEntry.SetRange("Document No.", DocumentNo);
    if not GLEntry.IsEmpty() then
        Error('Document %1 already posted', DocumentNo);

    // Calculate VAT
    if PostingDate >= DMY2Date(1, 1, 2024) then
        VATAmount := BaseAmount * 0.25
    else
        VATAmount := BaseAmount * 0.20;

    TotalAmount := BaseAmount + VATAmount;

    // Process dimension set
    if DimensionSetID <> 0 then begin
        TempDimSetEntry.SetRange("Dimension Set ID", DimensionSetID);
        if TempDimSetEntry.FindSet() then
            repeat
                if not DimensionValue.Get(
                    TempDimSetEntry."Dimension Code",
                    TempDimSetEntry."Dimension Value Code"
                ) then
                    Error(
                        'Dimension Value %1 %2 not found',
                        TempDimSetEntry."Dimension Code",
                        TempDimSetEntry."Dimension Value Code"
                    );
            until TempDimSetEntry.Next() = 0;
    end;

    // Create journal line
    GenJournalLine.Init();
    GenJournalLine."Line No." := GetNextLineNo44();
    GenJournalLine."Document No." := DocumentNo;
    GenJournalLine."Posting Date" := PostingDate;
    GenJournalLine."Account Type" := GenJournalLine."Account Type"::"G/L Account";
    GenJournalLine."Account No." := AccountNo;
    GenJournalLine.Amount := TotalAmount;
    GenJournalLine."VAT Amount" := VATAmount;
    GenJournalLine."Dimension Set ID" := DimensionSetID;
    GenJournalLine.Insert();

    // Post the entry
    GLEntry.Init();
    if GLEntry.FindLast() then
        LastEntryNo := GLEntry."Entry No."
    else
        LastEntryNo := 0;

    GLEntry."Entry No." := LastEntryNo + 1;
    GLEntry."G/L Account No." := AccountNo;
    GLEntry."Posting Date" := PostingDate;
    GLEntry."Document Type" := GLEntry."Document Type"::Invoice;
    GLEntry."Document No." := DocumentNo;
    GLEntry.Amount := TotalAmount;
    GLEntry."VAT Amount" := VATAmount;
    GLEntry."Dimension Set ID" := DimensionSetID;
    GLEntry.Insert();

    // Perform validation loops
    for i := 1 to 10 do begin
        if GLEntry.Amount <> TotalAmount then
            Error('Amount mismatch in iteration %1', i);
    end;

    exit(true);
end;

procedure GetNextLineNo44(): Integer
var
    GenJnlLine: Record "Gen. Journal Line";
begin
    if GenJnlLine.FindLast() then
        exit(GenJnlLine."Line No." + 10000)
    else
        exit(10000);
end;

table 50027 "Custom Table 27"
{
    DataClassification = CustomerContent;

    fields
    {
        field(1; "Entry No."; Integer)
        {
            DataClassification = CustomerContent;
            AutoIncrement = true;
        }
        field(2; "Code"; Code[20])
        {
            DataClassification = CustomerContent;
        }
        field(3; "Description"; Text[100])
        {
            DataClassification = CustomerContent;
        }
        field(4; "Amount"; Decimal)
        {
            DataClassification = CustomerContent;
        }
        field(5; "Date"; Date)
        {
            DataClassification = CustomerContent;
        }
    }

    keys
    {
        key(PK; "Entry No.")
        {
            Clustered = true;
        }
    }
}

procedure ComplexBusinessLogic45(
    DocumentNo: Code[20];
    PostingDate: Date;
    AccountNo: Code[20];
    Amount: Decimal;
    DimensionSetID: Integer
): Boolean
var
    GenJournalLine: Record "Gen. Journal Line";
    GLEntry: Record "G/L Entry";
    DimensionValue: Record "Dimension Value";
    TempDimSetEntry: Record "Dimension Set Entry" temporary;
    LastEntryNo: Integer;
    VATAmount: Decimal;
    BaseAmount: Decimal;
    TotalAmount: Decimal;
    i: Integer;
begin
    // Initialize variables
    VATAmount := 0;
    BaseAmount := Amount;
    TotalAmount := 0;

    // Validate account
    if AccountNo = '' then
        Error('Account number cannot be empty');

    // Check if document already posted
    GLEntry.SetRange("Document No.", DocumentNo);
    if not GLEntry.IsEmpty() then
        Error('Document %1 already posted', DocumentNo);

    // Calculate VAT
    if PostingDate >= DMY2Date(1, 1, 2024) then
        VATAmount := BaseAmount * 0.25
    else
        VATAmount := BaseAmount * 0.20;

    TotalAmount := BaseAmount + VATAmount;

    // Process dimension set
    if DimensionSetID <> 0 then begin
        TempDimSetEntry.SetRange("Dimension Set ID", DimensionSetID);
        if TempDimSetEntry.FindSet() then
            repeat
                if not DimensionValue.Get(
                    TempDimSetEntry."Dimension Code",
                    TempDimSetEntry."Dimension Value Code"
                ) then
                    Error(
                        'Dimension Value %1 %2 not found',
                        TempDimSetEntry."Dimension Code",
                        TempDimSetEntry."Dimension Value Code"
                    );
            until TempDimSetEntry.Next() = 0;
    end;

    // Create journal line
    GenJournalLine.Init();
    GenJournalLine."Line No." := GetNextLineNo45();
    GenJournalLine."Document No." := DocumentNo;
    GenJournalLine."Posting Date" := PostingDate;
    GenJournalLine."Account Type" := GenJournalLine."Account Type"::"G/L Account";
    GenJournalLine."Account No." := AccountNo;
    GenJournalLine.Amount := TotalAmount;
    GenJournalLine."VAT Amount" := VATAmount;
    GenJournalLine."Dimension Set ID" := DimensionSetID;
    GenJournalLine.Insert();

    // Post the entry
    GLEntry.Init();
    if GLEntry.FindLast() then
        LastEntryNo := GLEntry."Entry No."
    else
        LastEntryNo := 0;

    GLEntry."Entry No." := LastEntryNo + 1;
    GLEntry."G/L Account No." := AccountNo;
    GLEntry."Posting Date" := PostingDate;
    GLEntry."Document Type" := GLEntry."Document Type"::Invoice;
    GLEntry."Document No." := DocumentNo;
    GLEntry.Amount := TotalAmount;
    GLEntry."VAT Amount" := VATAmount;
    GLEntry."Dimension Set ID" := DimensionSetID;
    GLEntry.Insert();

    // Perform validation loops
    for i := 1 to 10 do begin
        if GLEntry.Amount <> TotalAmount then
            Error('Amount mismatch in iteration %1', i);
    end;

    exit(true);
end;

procedure GetNextLineNo45(): Integer
var
    GenJnlLine: Record "Gen. Journal Line";
begin
    if GenJnlLine.FindLast() then
        exit(GenJnlLine."Line No." + 10000)
    else
        exit(10000);
end;

procedure ComplexBusinessLogic46(
    DocumentNo: Code[20];
    PostingDate: Date;
    AccountNo: Code[20];
    Amount: Decimal;
    DimensionSetID: Integer
): Boolean
var
    GenJournalLine: Record "Gen. Journal Line";
    GLEntry: Record "G/L Entry";
    DimensionValue: Record "Dimension Value";
    TempDimSetEntry: Record "Dimension Set Entry" temporary;
    LastEntryNo: Integer;
    VATAmount: Decimal;
    BaseAmount: Decimal;
    TotalAmount: Decimal;
    i: Integer;
begin
    // Initialize variables
    VATAmount := 0;
    BaseAmount := Amount;
    TotalAmount := 0;

    // Validate account
    if AccountNo = '' then
        Error('Account number cannot be empty');

    // Check if document already posted
    GLEntry.SetRange("Document No.", DocumentNo);
    if not GLEntry.IsEmpty() then
        Error('Document %1 already posted', DocumentNo);

    // Calculate VAT
    if PostingDate >= DMY2Date(1, 1, 2024) then
        VATAmount := BaseAmount * 0.25
    else
        VATAmount := BaseAmount * 0.20;

    TotalAmount := BaseAmount + VATAmount;

    // Process dimension set
    if DimensionSetID <> 0 then begin
        TempDimSetEntry.SetRange("Dimension Set ID", DimensionSetID);
        if TempDimSetEntry.FindSet() then
            repeat
                if not DimensionValue.Get(
                    TempDimSetEntry."Dimension Code",
                    TempDimSetEntry."Dimension Value Code"
                ) then
                    Error(
                        'Dimension Value %1 %2 not found',
                        TempDimSetEntry."Dimension Code",
                        TempDimSetEntry."Dimension Value Code"
                    );
            until TempDimSetEntry.Next() = 0;
    end;

    // Create journal line
    GenJournalLine.Init();
    GenJournalLine."Line No." := GetNextLineNo46();
    GenJournalLine."Document No." := DocumentNo;
    GenJournalLine."Posting Date" := PostingDate;
    GenJournalLine."Account Type" := GenJournalLine."Account Type"::"G/L Account";
    GenJournalLine."Account No." := AccountNo;
    GenJournalLine.Amount := TotalAmount;
    GenJournalLine."VAT Amount" := VATAmount;
    GenJournalLine."Dimension Set ID" := DimensionSetID;
    GenJournalLine.Insert();

    // Post the entry
    GLEntry.Init();
    if GLEntry.FindLast() then
        LastEntryNo := GLEntry."Entry No."
    else
        LastEntryNo := 0;

    GLEntry."Entry No." := LastEntryNo + 1;
    GLEntry."G/L Account No." := AccountNo;
    GLEntry."Posting Date" := PostingDate;
    GLEntry."Document Type" := GLEntry."Document Type"::Invoice;
    GLEntry."Document No." := DocumentNo;
    GLEntry.Amount := TotalAmount;
    GLEntry."VAT Amount" := VATAmount;
    GLEntry."Dimension Set ID" := DimensionSetID;
    GLEntry.Insert();

    // Perform validation loops
    for i := 1 to 10 do begin
        if GLEntry.Amount <> TotalAmount then
            Error('Amount mismatch in iteration %1', i);
    end;

    exit(true);
end;

procedure GetNextLineNo46(): Integer
var
    GenJnlLine: Record "Gen. Journal Line";
begin
    if GenJnlLine.FindLast() then
        exit(GenJnlLine."Line No." + 10000)
    else
        exit(10000);
end;

table 50028 "Custom Table 28"
{
    DataClassification = CustomerContent;

    fields
    {
        field(1; "Entry No."; Integer)
        {
            DataClassification = CustomerContent;
            AutoIncrement = true;
        }
        field(2; "Code"; Code[20])
        {
            DataClassification = CustomerContent;
        }
        field(3; "Description"; Text[100])
        {
            DataClassification = CustomerContent;
        }
        field(4; "Amount"; Decimal)
        {
            DataClassification = CustomerContent;
        }
        field(5; "Date"; Date)
        {
            DataClassification = CustomerContent;
        }
    }

    keys
    {
        key(PK; "Entry No.")
        {
            Clustered = true;
        }
    }
}

procedure ComplexBusinessLogic47(
    DocumentNo: Code[20];
    PostingDate: Date;
    AccountNo: Code[20];
    Amount: Decimal;
    DimensionSetID: Integer
): Boolean
var
    GenJournalLine: Record "Gen. Journal Line";
    GLEntry: Record "G/L Entry";
    DimensionValue: Record "Dimension Value";
    TempDimSetEntry: Record "Dimension Set Entry" temporary;
    LastEntryNo: Integer;
    VATAmount: Decimal;
    BaseAmount: Decimal;
    TotalAmount: Decimal;
    i: Integer;
begin
    // Initialize variables
    VATAmount := 0;
    BaseAmount := Amount;
    TotalAmount := 0;

    // Validate account
    if AccountNo = '' then
        Error('Account number cannot be empty');

    // Check if document already posted
    GLEntry.SetRange("Document No.", DocumentNo);
    if not GLEntry.IsEmpty() then
        Error('Document %1 already posted', DocumentNo);

    // Calculate VAT
    if PostingDate >= DMY2Date(1, 1, 2024) then
        VATAmount := BaseAmount * 0.25
    else
        VATAmount := BaseAmount * 0.20;

    TotalAmount := BaseAmount + VATAmount;

    // Process dimension set
    if DimensionSetID <> 0 then begin
        TempDimSetEntry.SetRange("Dimension Set ID", DimensionSetID);
        if TempDimSetEntry.FindSet() then
            repeat
                if not DimensionValue.Get(
                    TempDimSetEntry."Dimension Code",
                    TempDimSetEntry."Dimension Value Code"
                ) then
                    Error(
                        'Dimension Value %1 %2 not found',
                        TempDimSetEntry."Dimension Code",
                        TempDimSetEntry."Dimension Value Code"
                    );
            until TempDimSetEntry.Next() = 0;
    end;

    // Create journal line
    GenJournalLine.Init();
    GenJournalLine."Line No." := GetNextLineNo47();
    GenJournalLine."Document No." := DocumentNo;
    GenJournalLine."Posting Date" := PostingDate;
    GenJournalLine."Account Type" := GenJournalLine."Account Type"::"G/L Account";
    GenJournalLine."Account No." := AccountNo;
    GenJournalLine.Amount := TotalAmount;
    GenJournalLine."VAT Amount" := VATAmount;
    GenJournalLine."Dimension Set ID" := DimensionSetID;
    GenJournalLine.Insert();

    // Post the entry
    GLEntry.Init();
    if GLEntry.FindLast() then
        LastEntryNo := GLEntry."Entry No."
    else
        LastEntryNo := 0;

    GLEntry."Entry No." := LastEntryNo + 1;
    GLEntry."G/L Account No." := AccountNo;
    GLEntry."Posting Date" := PostingDate;
    GLEntry."Document Type" := GLEntry."Document Type"::Invoice;
    GLEntry."Document No." := DocumentNo;
    GLEntry.Amount := TotalAmount;
    GLEntry."VAT Amount" := VATAmount;
    GLEntry."Dimension Set ID" := DimensionSetID;
    GLEntry.Insert();

    // Perform validation loops
    for i := 1 to 10 do begin
        if GLEntry.Amount <> TotalAmount then
            Error('Amount mismatch in iteration %1', i);
    end;

    exit(true);
end;

procedure GetNextLineNo47(): Integer
var
    GenJnlLine: Record "Gen. Journal Line";
begin
    if GenJnlLine.FindLast() then
        exit(GenJnlLine."Line No." + 10000)
    else
        exit(10000);
end;

page 50019 "Custom List 19"
{
    PageType = List;
    SourceTable = "Custom Table 19";
    UsageCategory = Lists;
    ApplicationArea = All;

    layout
    {
        area(Content)
        {
            repeater(Group)
            {
                field("Entry No."; "Entry No.")
                {
                    ApplicationArea = All;
                }
                field("Code"; "Code")
                {
                    ApplicationArea = All;
                }
                field("Description"; "Description")
                {
                    ApplicationArea = All;
                }
                field("Amount"; "Amount")
                {
                    ApplicationArea = All;
                }
            }
        }
    }
}

page 50020 "Custom List 20"
{
    PageType = List;
    SourceTable = "Custom Table 20";
    UsageCategory = Lists;
    ApplicationArea = All;

    layout
    {
        area(Content)
        {
            repeater(Group)
            {
                field("Entry No."; "Entry No.")
                {
                    ApplicationArea = All;
                }
                field("Code"; "Code")
                {
                    ApplicationArea = All;
                }
                field("Description"; "Description")
                {
                    ApplicationArea = All;
                }
                field("Amount"; "Amount")
                {
                    ApplicationArea = All;
                }
            }
        }
    }
}

procedure ComplexBusinessLogic48(
    DocumentNo: Code[20];
    PostingDate: Date;
    AccountNo: Code[20];
    Amount: Decimal;
    DimensionSetID: Integer
): Boolean
var
    GenJournalLine: Record "Gen. Journal Line";
    GLEntry: Record "G/L Entry";
    DimensionValue: Record "Dimension Value";
    TempDimSetEntry: Record "Dimension Set Entry" temporary;
    LastEntryNo: Integer;
    VATAmount: Decimal;
    BaseAmount: Decimal;
    TotalAmount: Decimal;
    i: Integer;
begin
    // Initialize variables
    VATAmount := 0;
    BaseAmount := Amount;
    TotalAmount := 0;

    // Validate account
    if AccountNo = '' then
        Error('Account number cannot be empty');

    // Check if document already posted
    GLEntry.SetRange("Document No.", DocumentNo);
    if not GLEntry.IsEmpty() then
        Error('Document %1 already posted', DocumentNo);

    // Calculate VAT
    if PostingDate >= DMY2Date(1, 1, 2024) then
        VATAmount := BaseAmount * 0.25
    else
        VATAmount := BaseAmount * 0.20;

    TotalAmount := BaseAmount + VATAmount;

    // Process dimension set
    if DimensionSetID <> 0 then begin
        TempDimSetEntry.SetRange("Dimension Set ID", DimensionSetID);
        if TempDimSetEntry.FindSet() then
            repeat
                if not DimensionValue.Get(
                    TempDimSetEntry."Dimension Code",
                    TempDimSetEntry."Dimension Value Code"
                ) then
                    Error(
                        'Dimension Value %1 %2 not found',
                        TempDimSetEntry."Dimension Code",
                        TempDimSetEntry."Dimension Value Code"
                    );
            until TempDimSetEntry.Next() = 0;
    end;

    // Create journal line
    GenJournalLine.Init();
    GenJournalLine."Line No." := GetNextLineNo48();
    GenJournalLine."Document No." := DocumentNo;
    GenJournalLine."Posting Date" := PostingDate;
    GenJournalLine."Account Type" := GenJournalLine."Account Type"::"G/L Account";
    GenJournalLine."Account No." := AccountNo;
    GenJournalLine.Amount := TotalAmount;
    GenJournalLine."VAT Amount" := VATAmount;
    GenJournalLine."Dimension Set ID" := DimensionSetID;
    GenJournalLine.Insert();

    // Post the entry
    GLEntry.Init();
    if GLEntry.FindLast() then
        LastEntryNo := GLEntry."Entry No."
    else
        LastEntryNo := 0;

    GLEntry."Entry No." := LastEntryNo + 1;
    GLEntry."G/L Account No." := AccountNo;
    GLEntry."Posting Date" := PostingDate;
    GLEntry."Document Type" := GLEntry."Document Type"::Invoice;
    GLEntry."Document No." := DocumentNo;
    GLEntry.Amount := TotalAmount;
    GLEntry."VAT Amount" := VATAmount;
    GLEntry."Dimension Set ID" := DimensionSetID;
    GLEntry.Insert();

    // Perform validation loops
    for i := 1 to 10 do begin
        if GLEntry.Amount <> TotalAmount then
            Error('Amount mismatch in iteration %1', i);
    end;

    exit(true);
end;

procedure GetNextLineNo48(): Integer
var
    GenJnlLine: Record "Gen. Journal Line";
begin
    if GenJnlLine.FindLast() then
        exit(GenJnlLine."Line No." + 10000)
    else
        exit(10000);
end;

procedure ComplexBusinessLogic49(
    DocumentNo: Code[20];
    PostingDate: Date;
    AccountNo: Code[20];
    Amount: Decimal;
    DimensionSetID: Integer
): Boolean
var
    GenJournalLine: Record "Gen. Journal Line";
    GLEntry: Record "G/L Entry";
    DimensionValue: Record "Dimension Value";
    TempDimSetEntry: Record "Dimension Set Entry" temporary;
    LastEntryNo: Integer;
    VATAmount: Decimal;
    BaseAmount: Decimal;
    TotalAmount: Decimal;
    i: Integer;
begin
    // Initialize variables
    VATAmount := 0;
    BaseAmount := Amount;
    TotalAmount := 0;

    // Validate account
    if AccountNo = '' then
        Error('Account number cannot be empty');

    // Check if document already posted
    GLEntry.SetRange("Document No.", DocumentNo);
    if not GLEntry.IsEmpty() then
        Error('Document %1 already posted', DocumentNo);

    // Calculate VAT
    if PostingDate >= DMY2Date(1, 1, 2024) then
        VATAmount := BaseAmount * 0.25
    else
        VATAmount := BaseAmount * 0.20;

    TotalAmount := BaseAmount + VATAmount;

    // Process dimension set
    if DimensionSetID <> 0 then begin
        TempDimSetEntry.SetRange("Dimension Set ID", DimensionSetID);
        if TempDimSetEntry.FindSet() then
            repeat
                if not DimensionValue.Get(
                    TempDimSetEntry."Dimension Code",
                    TempDimSetEntry."Dimension Value Code"
                ) then
                    Error(
                        'Dimension Value %1 %2 not found',
                        TempDimSetEntry."Dimension Code",
                        TempDimSetEntry."Dimension Value Code"
                    );
            until TempDimSetEntry.Next() = 0;
    end;

    // Create journal line
    GenJournalLine.Init();
    GenJournalLine."Line No." := GetNextLineNo49();
    GenJournalLine."Document No." := DocumentNo;
    GenJournalLine."Posting Date" := PostingDate;
    GenJournalLine."Account Type" := GenJournalLine."Account Type"::"G/L Account";
    GenJournalLine."Account No." := AccountNo;
    GenJournalLine.Amount := TotalAmount;
    GenJournalLine."VAT Amount" := VATAmount;
    GenJournalLine."Dimension Set ID" := DimensionSetID;
    GenJournalLine.Insert();

    // Post the entry
    GLEntry.Init();
    if GLEntry.FindLast() then
        LastEntryNo := GLEntry."Entry No."
    else
        LastEntryNo := 0;

    GLEntry."Entry No." := LastEntryNo + 1;
    GLEntry."G/L Account No." := AccountNo;
    GLEntry."Posting Date" := PostingDate;
    GLEntry."Document Type" := GLEntry."Document Type"::Invoice;
    GLEntry."Document No." := DocumentNo;
    GLEntry.Amount := TotalAmount;
    GLEntry."VAT Amount" := VATAmount;
    GLEntry."Dimension Set ID" := DimensionSetID;
    GLEntry.Insert();

    // Perform validation loops
    for i := 1 to 10 do begin
        if GLEntry.Amount <> TotalAmount then
            Error('Amount mismatch in iteration %1', i);
    end;

    exit(true);
end;

procedure GetNextLineNo49(): Integer
var
    GenJnlLine: Record "Gen. Journal Line";
begin
    if GenJnlLine.FindLast() then
        exit(GenJnlLine."Line No." + 10000)
    else
        exit(10000);
end;

page 50021 "Custom List 21"
{
    PageType = List;
    SourceTable = "Custom Table 21";
    UsageCategory = Lists;
    ApplicationArea = All;

    layout
    {
        area(Content)
        {
            repeater(Group)
            {
                field("Entry No."; "Entry No.")
                {
                    ApplicationArea = All;
                }
                field("Code"; "Code")
                {
                    ApplicationArea = All;
                }
                field("Description"; "Description")
                {
                    ApplicationArea = All;
                }
                field("Amount"; "Amount")
                {
                    ApplicationArea = All;
                }
            }
        }
    }
}

procedure ComplexBusinessLogic50(
    DocumentNo: Code[20];
    PostingDate: Date;
    AccountNo: Code[20];
    Amount: Decimal;
    DimensionSetID: Integer
): Boolean
var
    GenJournalLine: Record "Gen. Journal Line";
    GLEntry: Record "G/L Entry";
    DimensionValue: Record "Dimension Value";
    TempDimSetEntry: Record "Dimension Set Entry" temporary;
    LastEntryNo: Integer;
    VATAmount: Decimal;
    BaseAmount: Decimal;
    TotalAmount: Decimal;
    i: Integer;
begin
    // Initialize variables
    VATAmount := 0;
    BaseAmount := Amount;
    TotalAmount := 0;

    // Validate account
    if AccountNo = '' then
        Error('Account number cannot be empty');

    // Check if document already posted
    GLEntry.SetRange("Document No.", DocumentNo);
    if not GLEntry.IsEmpty() then
        Error('Document %1 already posted', DocumentNo);

    // Calculate VAT
    if PostingDate >= DMY2Date(1, 1, 2024) then
        VATAmount := BaseAmount * 0.25
    else
        VATAmount := BaseAmount * 0.20;

    TotalAmount := BaseAmount + VATAmount;

    // Process dimension set
    if DimensionSetID <> 0 then begin
        TempDimSetEntry.SetRange("Dimension Set ID", DimensionSetID);
        if TempDimSetEntry.FindSet() then
            repeat
                if not DimensionValue.Get(
                    TempDimSetEntry."Dimension Code",
                    TempDimSetEntry."Dimension Value Code"
                ) then
                    Error(
                        'Dimension Value %1 %2 not found',
                        TempDimSetEntry."Dimension Code",
                        TempDimSetEntry."Dimension Value Code"
                    );
            until TempDimSetEntry.Next() = 0;
    end;

    // Create journal line
    GenJournalLine.Init();
    GenJournalLine."Line No." := GetNextLineNo50();
    GenJournalLine."Document No." := DocumentNo;
    GenJournalLine."Posting Date" := PostingDate;
    GenJournalLine."Account Type" := GenJournalLine."Account Type"::"G/L Account";
    GenJournalLine."Account No." := AccountNo;
    GenJournalLine.Amount := TotalAmount;
    GenJournalLine."VAT Amount" := VATAmount;
    GenJournalLine."Dimension Set ID" := DimensionSetID;
    GenJournalLine.Insert();

    // Post the entry
    GLEntry.Init();
    if GLEntry.FindLast() then
        LastEntryNo := GLEntry."Entry No."
    else
        LastEntryNo := 0;

    GLEntry."Entry No." := LastEntryNo + 1;
    GLEntry."G/L Account No." := AccountNo;
    GLEntry."Posting Date" := PostingDate;
    GLEntry."Document Type" := GLEntry."Document Type"::Invoice;
    GLEntry."Document No." := DocumentNo;
    GLEntry.Amount := TotalAmount;
    GLEntry."VAT Amount" := VATAmount;
    GLEntry."Dimension Set ID" := DimensionSetID;
    GLEntry.Insert();

    // Perform validation loops
    for i := 1 to 10 do begin
        if GLEntry.Amount <> TotalAmount then
            Error('Amount mismatch in iteration %1', i);
    end;

    exit(true);
end;

procedure GetNextLineNo50(): Integer
var
    GenJnlLine: Record "Gen. Journal Line";
begin
    if GenJnlLine.FindLast() then
        exit(GenJnlLine."Line No." + 10000)
    else
        exit(10000);
end;

page 50022 "Custom List 22"
{
    PageType = List;
    SourceTable = "Custom Table 22";
    UsageCategory = Lists;
    ApplicationArea = All;

    layout
    {
        area(Content)
        {
            repeater(Group)
            {
                field("Entry No."; "Entry No.")
                {
                    ApplicationArea = All;
                }
                field("Code"; "Code")
                {
                    ApplicationArea = All;
                }
                field("Description"; "Description")
                {
                    ApplicationArea = All;
                }
                field("Amount"; "Amount")
                {
                    ApplicationArea = All;
                }
            }
        }
    }
}

procedure ComplexBusinessLogic51(
    DocumentNo: Code[20];
    PostingDate: Date;
    AccountNo: Code[20];
    Amount: Decimal;
    DimensionSetID: Integer
): Boolean
var
    GenJournalLine: Record "Gen. Journal Line";
    GLEntry: Record "G/L Entry";
    DimensionValue: Record "Dimension Value";
    TempDimSetEntry: Record "Dimension Set Entry" temporary;
    LastEntryNo: Integer;
    VATAmount: Decimal;
    BaseAmount: Decimal;
    TotalAmount: Decimal;
    i: Integer;
begin
    // Initialize variables
    VATAmount := 0;
    BaseAmount := Amount;
    TotalAmount := 0;

    // Validate account
    if AccountNo = '' then
        Error('Account number cannot be empty');

    // Check if document already posted
    GLEntry.SetRange("Document No.", DocumentNo);
    if not GLEntry.IsEmpty() then
        Error('Document %1 already posted', DocumentNo);

    // Calculate VAT
    if PostingDate >= DMY2Date(1, 1, 2024) then
        VATAmount := BaseAmount * 0.25
    else
        VATAmount := BaseAmount * 0.20;

    TotalAmount := BaseAmount + VATAmount;

    // Process dimension set
    if DimensionSetID <> 0 then begin
        TempDimSetEntry.SetRange("Dimension Set ID", DimensionSetID);
        if TempDimSetEntry.FindSet() then
            repeat
                if not DimensionValue.Get(
                    TempDimSetEntry."Dimension Code",
                    TempDimSetEntry."Dimension Value Code"
                ) then
                    Error(
                        'Dimension Value %1 %2 not found',
                        TempDimSetEntry."Dimension Code",
                        TempDimSetEntry."Dimension Value Code"
                    );
            until TempDimSetEntry.Next() = 0;
    end;

    // Create journal line
    GenJournalLine.Init();
    GenJournalLine."Line No." := GetNextLineNo51();
    GenJournalLine."Document No." := DocumentNo;
    GenJournalLine."Posting Date" := PostingDate;
    GenJournalLine."Account Type" := GenJournalLine."Account Type"::"G/L Account";
    GenJournalLine."Account No." := AccountNo;
    GenJournalLine.Amount := TotalAmount;
    GenJournalLine."VAT Amount" := VATAmount;
    GenJournalLine."Dimension Set ID" := DimensionSetID;
    GenJournalLine.Insert();

    // Post the entry
    GLEntry.Init();
    if GLEntry.FindLast() then
        LastEntryNo := GLEntry."Entry No."
    else
        LastEntryNo := 0;

    GLEntry."Entry No." := LastEntryNo + 1;
    GLEntry."G/L Account No." := AccountNo;
    GLEntry."Posting Date" := PostingDate;
    GLEntry."Document Type" := GLEntry."Document Type"::Invoice;
    GLEntry."Document No." := DocumentNo;
    GLEntry.Amount := TotalAmount;
    GLEntry."VAT Amount" := VATAmount;
    GLEntry."Dimension Set ID" := DimensionSetID;
    GLEntry.Insert();

    // Perform validation loops
    for i := 1 to 10 do begin
        if GLEntry.Amount <> TotalAmount then
            Error('Amount mismatch in iteration %1', i);
    end;

    exit(true);
end;

procedure GetNextLineNo51(): Integer
var
    GenJnlLine: Record "Gen. Journal Line";
begin
    if GenJnlLine.FindLast() then
        exit(GenJnlLine."Line No." + 10000)
    else
        exit(10000);
end;

table 50029 "Custom Table 29"
{
    DataClassification = CustomerContent;

    fields
    {
        field(1; "Entry No."; Integer)
        {
            DataClassification = CustomerContent;
            AutoIncrement = true;
        }
        field(2; "Code"; Code[20])
        {
            DataClassification = CustomerContent;
        }
        field(3; "Description"; Text[100])
        {
            DataClassification = CustomerContent;
        }
        field(4; "Amount"; Decimal)
        {
            DataClassification = CustomerContent;
        }
        field(5; "Date"; Date)
        {
            DataClassification = CustomerContent;
        }
    }

    keys
    {
        key(PK; "Entry No.")
        {
            Clustered = true;
        }
    }
}

table 50030 "Custom Table 30"
{
    DataClassification = CustomerContent;

    fields
    {
        field(1; "Entry No."; Integer)
        {
            DataClassification = CustomerContent;
            AutoIncrement = true;
        }
        field(2; "Code"; Code[20])
        {
            DataClassification = CustomerContent;
        }
        field(3; "Description"; Text[100])
        {
            DataClassification = CustomerContent;
        }
        field(4; "Amount"; Decimal)
        {
            DataClassification = CustomerContent;
        }
        field(5; "Date"; Date)
        {
            DataClassification = CustomerContent;
        }
    }

    keys
    {
        key(PK; "Entry No.")
        {
            Clustered = true;
        }
    }
}

procedure ComplexBusinessLogic52(
    DocumentNo: Code[20];
    PostingDate: Date;
    AccountNo: Code[20];
    Amount: Decimal;
    DimensionSetID: Integer
): Boolean
var
    GenJournalLine: Record "Gen. Journal Line";
    GLEntry: Record "G/L Entry";
    DimensionValue: Record "Dimension Value";
    TempDimSetEntry: Record "Dimension Set Entry" temporary;
    LastEntryNo: Integer;
    VATAmount: Decimal;
    BaseAmount: Decimal;
    TotalAmount: Decimal;
    i: Integer;
begin
    // Initialize variables
    VATAmount := 0;
    BaseAmount := Amount;
    TotalAmount := 0;

    // Validate account
    if AccountNo = '' then
        Error('Account number cannot be empty');

    // Check if document already posted
    GLEntry.SetRange("Document No.", DocumentNo);
    if not GLEntry.IsEmpty() then
        Error('Document %1 already posted', DocumentNo);

    // Calculate VAT
    if PostingDate >= DMY2Date(1, 1, 2024) then
        VATAmount := BaseAmount * 0.25
    else
        VATAmount := BaseAmount * 0.20;

    TotalAmount := BaseAmount + VATAmount;

    // Process dimension set
    if DimensionSetID <> 0 then begin
        TempDimSetEntry.SetRange("Dimension Set ID", DimensionSetID);
        if TempDimSetEntry.FindSet() then
            repeat
                if not DimensionValue.Get(
                    TempDimSetEntry."Dimension Code",
                    TempDimSetEntry."Dimension Value Code"
                ) then
                    Error(
                        'Dimension Value %1 %2 not found',
                        TempDimSetEntry."Dimension Code",
                        TempDimSetEntry."Dimension Value Code"
                    );
            until TempDimSetEntry.Next() = 0;
    end;

    // Create journal line
    GenJournalLine.Init();
    GenJournalLine."Line No." := GetNextLineNo52();
    GenJournalLine."Document No." := DocumentNo;
    GenJournalLine."Posting Date" := PostingDate;
    GenJournalLine."Account Type" := GenJournalLine."Account Type"::"G/L Account";
    GenJournalLine."Account No." := AccountNo;
    GenJournalLine.Amount := TotalAmount;
    GenJournalLine."VAT Amount" := VATAmount;
    GenJournalLine."Dimension Set ID" := DimensionSetID;
    GenJournalLine.Insert();

    // Post the entry
    GLEntry.Init();
    if GLEntry.FindLast() then
        LastEntryNo := GLEntry."Entry No."
    else
        LastEntryNo := 0;

    GLEntry."Entry No." := LastEntryNo + 1;
    GLEntry."G/L Account No." := AccountNo;
    GLEntry."Posting Date" := PostingDate;
    GLEntry."Document Type" := GLEntry."Document Type"::Invoice;
    GLEntry."Document No." := DocumentNo;
    GLEntry.Amount := TotalAmount;
    GLEntry."VAT Amount" := VATAmount;
    GLEntry."Dimension Set ID" := DimensionSetID;
    GLEntry.Insert();

    // Perform validation loops
    for i := 1 to 10 do begin
        if GLEntry.Amount <> TotalAmount then
            Error('Amount mismatch in iteration %1', i);
    end;

    exit(true);
end;

procedure GetNextLineNo52(): Integer
var
    GenJnlLine: Record "Gen. Journal Line";
begin
    if GenJnlLine.FindLast() then
        exit(GenJnlLine."Line No." + 10000)
    else
        exit(10000);
end;

procedure ComplexBusinessLogic53(
    DocumentNo: Code[20];
    PostingDate: Date;
    AccountNo: Code[20];
    Amount: Decimal;
    DimensionSetID: Integer
): Boolean
var
    GenJournalLine: Record "Gen. Journal Line";
    GLEntry: Record "G/L Entry";
    DimensionValue: Record "Dimension Value";
    TempDimSetEntry: Record "Dimension Set Entry" temporary;
    LastEntryNo: Integer;
    VATAmount: Decimal;
    BaseAmount: Decimal;
    TotalAmount: Decimal;
    i: Integer;
begin
    // Initialize variables
    VATAmount := 0;
    BaseAmount := Amount;
    TotalAmount := 0;

    // Validate account
    if AccountNo = '' then
        Error('Account number cannot be empty');

    // Check if document already posted
    GLEntry.SetRange("Document No.", DocumentNo);
    if not GLEntry.IsEmpty() then
        Error('Document %1 already posted', DocumentNo);

    // Calculate VAT
    if PostingDate >= DMY2Date(1, 1, 2024) then
        VATAmount := BaseAmount * 0.25
    else
        VATAmount := BaseAmount * 0.20;

    TotalAmount := BaseAmount + VATAmount;

    // Process dimension set
    if DimensionSetID <> 0 then begin
        TempDimSetEntry.SetRange("Dimension Set ID", DimensionSetID);
        if TempDimSetEntry.FindSet() then
            repeat
                if not DimensionValue.Get(
                    TempDimSetEntry."Dimension Code",
                    TempDimSetEntry."Dimension Value Code"
                ) then
                    Error(
                        'Dimension Value %1 %2 not found',
                        TempDimSetEntry."Dimension Code",
                        TempDimSetEntry."Dimension Value Code"
                    );
            until TempDimSetEntry.Next() = 0;
    end;

    // Create journal line
    GenJournalLine.Init();
    GenJournalLine."Line No." := GetNextLineNo53();
    GenJournalLine."Document No." := DocumentNo;
    GenJournalLine."Posting Date" := PostingDate;
    GenJournalLine."Account Type" := GenJournalLine."Account Type"::"G/L Account";
    GenJournalLine."Account No." := AccountNo;
    GenJournalLine.Amount := TotalAmount;
    GenJournalLine."VAT Amount" := VATAmount;
    GenJournalLine."Dimension Set ID" := DimensionSetID;
    GenJournalLine.Insert();

    // Post the entry
    GLEntry.Init();
    if GLEntry.FindLast() then
        LastEntryNo := GLEntry."Entry No."
    else
        LastEntryNo := 0;

    GLEntry."Entry No." := LastEntryNo + 1;
    GLEntry."G/L Account No." := AccountNo;
    GLEntry."Posting Date" := PostingDate;
    GLEntry."Document Type" := GLEntry."Document Type"::Invoice;
    GLEntry."Document No." := DocumentNo;
    GLEntry.Amount := TotalAmount;
    GLEntry."VAT Amount" := VATAmount;
    GLEntry."Dimension Set ID" := DimensionSetID;
    GLEntry.Insert();

    // Perform validation loops
    for i := 1 to 10 do begin
        if GLEntry.Amount <> TotalAmount then
            Error('Amount mismatch in iteration %1', i);
    end;

    exit(true);
end;

procedure GetNextLineNo53(): Integer
var
    GenJnlLine: Record "Gen. Journal Line";
begin
    if GenJnlLine.FindLast() then
        exit(GenJnlLine."Line No." + 10000)
    else
        exit(10000);
end;

procedure ComplexBusinessLogic54(
    DocumentNo: Code[20];
    PostingDate: Date;
    AccountNo: Code[20];
    Amount: Decimal;
    DimensionSetID: Integer
): Boolean
var
    GenJournalLine: Record "Gen. Journal Line";
    GLEntry: Record "G/L Entry";
    DimensionValue: Record "Dimension Value";
    TempDimSetEntry: Record "Dimension Set Entry" temporary;
    LastEntryNo: Integer;
    VATAmount: Decimal;
    BaseAmount: Decimal;
    TotalAmount: Decimal;
    i: Integer;
begin
    // Initialize variables
    VATAmount := 0;
    BaseAmount := Amount;
    TotalAmount := 0;

    // Validate account
    if AccountNo = '' then
        Error('Account number cannot be empty');

    // Check if document already posted
    GLEntry.SetRange("Document No.", DocumentNo);
    if not GLEntry.IsEmpty() then
        Error('Document %1 already posted', DocumentNo);

    // Calculate VAT
    if PostingDate >= DMY2Date(1, 1, 2024) then
        VATAmount := BaseAmount * 0.25
    else
        VATAmount := BaseAmount * 0.20;

    TotalAmount := BaseAmount + VATAmount;

    // Process dimension set
    if DimensionSetID <> 0 then begin
        TempDimSetEntry.SetRange("Dimension Set ID", DimensionSetID);
        if TempDimSetEntry.FindSet() then
            repeat
                if not DimensionValue.Get(
                    TempDimSetEntry."Dimension Code",
                    TempDimSetEntry."Dimension Value Code"
                ) then
                    Error(
                        'Dimension Value %1 %2 not found',
                        TempDimSetEntry."Dimension Code",
                        TempDimSetEntry."Dimension Value Code"
                    );
            until TempDimSetEntry.Next() = 0;
    end;

    // Create journal line
    GenJournalLine.Init();
    GenJournalLine."Line No." := GetNextLineNo54();
    GenJournalLine."Document No." := DocumentNo;
    GenJournalLine."Posting Date" := PostingDate;
    GenJournalLine."Account Type" := GenJournalLine."Account Type"::"G/L Account";
    GenJournalLine."Account No." := AccountNo;
    GenJournalLine.Amount := TotalAmount;
    GenJournalLine."VAT Amount" := VATAmount;
    GenJournalLine."Dimension Set ID" := DimensionSetID;
    GenJournalLine.Insert();

    // Post the entry
    GLEntry.Init();
    if GLEntry.FindLast() then
        LastEntryNo := GLEntry."Entry No."
    else
        LastEntryNo := 0;

    GLEntry."Entry No." := LastEntryNo + 1;
    GLEntry."G/L Account No." := AccountNo;
    GLEntry."Posting Date" := PostingDate;
    GLEntry."Document Type" := GLEntry."Document Type"::Invoice;
    GLEntry."Document No." := DocumentNo;
    GLEntry.Amount := TotalAmount;
    GLEntry."VAT Amount" := VATAmount;
    GLEntry."Dimension Set ID" := DimensionSetID;
    GLEntry.Insert();

    // Perform validation loops
    for i := 1 to 10 do begin
        if GLEntry.Amount <> TotalAmount then
            Error('Amount mismatch in iteration %1', i);
    end;

    exit(true);
end;

procedure GetNextLineNo54(): Integer
var
    GenJnlLine: Record "Gen. Journal Line";
begin
    if GenJnlLine.FindLast() then
        exit(GenJnlLine."Line No." + 10000)
    else
        exit(10000);
end;

table 50031 "Custom Table 31"
{
    DataClassification = CustomerContent;

    fields
    {
        field(1; "Entry No."; Integer)
        {
            DataClassification = CustomerContent;
            AutoIncrement = true;
        }
        field(2; "Code"; Code[20])
        {
            DataClassification = CustomerContent;
        }
        field(3; "Description"; Text[100])
        {
            DataClassification = CustomerContent;
        }
        field(4; "Amount"; Decimal)
        {
            DataClassification = CustomerContent;
        }
        field(5; "Date"; Date)
        {
            DataClassification = CustomerContent;
        }
    }

    keys
    {
        key(PK; "Entry No.")
        {
            Clustered = true;
        }
    }
}

procedure ComplexBusinessLogic55(
    DocumentNo: Code[20];
    PostingDate: Date;
    AccountNo: Code[20];
    Amount: Decimal;
    DimensionSetID: Integer
): Boolean
var
    GenJournalLine: Record "Gen. Journal Line";
    GLEntry: Record "G/L Entry";
    DimensionValue: Record "Dimension Value";
    TempDimSetEntry: Record "Dimension Set Entry" temporary;
    LastEntryNo: Integer;
    VATAmount: Decimal;
    BaseAmount: Decimal;
    TotalAmount: Decimal;
    i: Integer;
begin
    // Initialize variables
    VATAmount := 0;
    BaseAmount := Amount;
    TotalAmount := 0;

    // Validate account
    if AccountNo = '' then
        Error('Account number cannot be empty');

    // Check if document already posted
    GLEntry.SetRange("Document No.", DocumentNo);
    if not GLEntry.IsEmpty() then
        Error('Document %1 already posted', DocumentNo);

    // Calculate VAT
    if PostingDate >= DMY2Date(1, 1, 2024) then
        VATAmount := BaseAmount * 0.25
    else
        VATAmount := BaseAmount * 0.20;

    TotalAmount := BaseAmount + VATAmount;

    // Process dimension set
    if DimensionSetID <> 0 then begin
        TempDimSetEntry.SetRange("Dimension Set ID", DimensionSetID);
        if TempDimSetEntry.FindSet() then
            repeat
                if not DimensionValue.Get(
                    TempDimSetEntry."Dimension Code",
                    TempDimSetEntry."Dimension Value Code"
                ) then
                    Error(
                        'Dimension Value %1 %2 not found',
                        TempDimSetEntry."Dimension Code",
                        TempDimSetEntry."Dimension Value Code"
                    );
            until TempDimSetEntry.Next() = 0;
    end;

    // Create journal line
    GenJournalLine.Init();
    GenJournalLine."Line No." := GetNextLineNo55();
    GenJournalLine."Document No." := DocumentNo;
    GenJournalLine."Posting Date" := PostingDate;
    GenJournalLine."Account Type" := GenJournalLine."Account Type"::"G/L Account";
    GenJournalLine."Account No." := AccountNo;
    GenJournalLine.Amount := TotalAmount;
    GenJournalLine."VAT Amount" := VATAmount;
    GenJournalLine."Dimension Set ID" := DimensionSetID;
    GenJournalLine.Insert();

    // Post the entry
    GLEntry.Init();
    if GLEntry.FindLast() then
        LastEntryNo := GLEntry."Entry No."
    else
        LastEntryNo := 0;

    GLEntry."Entry No." := LastEntryNo + 1;
    GLEntry."G/L Account No." := AccountNo;
    GLEntry."Posting Date" := PostingDate;
    GLEntry."Document Type" := GLEntry."Document Type"::Invoice;
    GLEntry."Document No." := DocumentNo;
    GLEntry.Amount := TotalAmount;
    GLEntry."VAT Amount" := VATAmount;
    GLEntry."Dimension Set ID" := DimensionSetID;
    GLEntry.Insert();

    // Perform validation loops
    for i := 1 to 10 do begin
        if GLEntry.Amount <> TotalAmount then
            Error('Amount mismatch in iteration %1', i);
    end;

    exit(true);
end;

procedure GetNextLineNo55(): Integer
var
    GenJnlLine: Record "Gen. Journal Line";
begin
    if GenJnlLine.FindLast() then
        exit(GenJnlLine."Line No." + 10000)
    else
        exit(10000);
end;

procedure ComplexBusinessLogic56(
    DocumentNo: Code[20];
    PostingDate: Date;
    AccountNo: Code[20];
    Amount: Decimal;
    DimensionSetID: Integer
): Boolean
var
    GenJournalLine: Record "Gen. Journal Line";
    GLEntry: Record "G/L Entry";
    DimensionValue: Record "Dimension Value";
    TempDimSetEntry: Record "Dimension Set Entry" temporary;
    LastEntryNo: Integer;
    VATAmount: Decimal;
    BaseAmount: Decimal;
    TotalAmount: Decimal;
    i: Integer;
begin
    // Initialize variables
    VATAmount := 0;
    BaseAmount := Amount;
    TotalAmount := 0;

    // Validate account
    if AccountNo = '' then
        Error('Account number cannot be empty');

    // Check if document already posted
    GLEntry.SetRange("Document No.", DocumentNo);
    if not GLEntry.IsEmpty() then
        Error('Document %1 already posted', DocumentNo);

    // Calculate VAT
    if PostingDate >= DMY2Date(1, 1, 2024) then
        VATAmount := BaseAmount * 0.25
    else
        VATAmount := BaseAmount * 0.20;

    TotalAmount := BaseAmount + VATAmount;

    // Process dimension set
    if DimensionSetID <> 0 then begin
        TempDimSetEntry.SetRange("Dimension Set ID", DimensionSetID);
        if TempDimSetEntry.FindSet() then
            repeat
                if not DimensionValue.Get(
                    TempDimSetEntry."Dimension Code",
                    TempDimSetEntry."Dimension Value Code"
                ) then
                    Error(
                        'Dimension Value %1 %2 not found',
                        TempDimSetEntry."Dimension Code",
                        TempDimSetEntry."Dimension Value Code"
                    );
            until TempDimSetEntry.Next() = 0;
    end;

    // Create journal line
    GenJournalLine.Init();
    GenJournalLine."Line No." := GetNextLineNo56();
    GenJournalLine."Document No." := DocumentNo;
    GenJournalLine."Posting Date" := PostingDate;
    GenJournalLine."Account Type" := GenJournalLine."Account Type"::"G/L Account";
    GenJournalLine."Account No." := AccountNo;
    GenJournalLine.Amount := TotalAmount;
    GenJournalLine."VAT Amount" := VATAmount;
    GenJournalLine."Dimension Set ID" := DimensionSetID;
    GenJournalLine.Insert();

    // Post the entry
    GLEntry.Init();
    if GLEntry.FindLast() then
        LastEntryNo := GLEntry."Entry No."
    else
        LastEntryNo := 0;

    GLEntry."Entry No." := LastEntryNo + 1;
    GLEntry."G/L Account No." := AccountNo;
    GLEntry."Posting Date" := PostingDate;
    GLEntry."Document Type" := GLEntry."Document Type"::Invoice;
    GLEntry."Document No." := DocumentNo;
    GLEntry.Amount := TotalAmount;
    GLEntry."VAT Amount" := VATAmount;
    GLEntry."Dimension Set ID" := DimensionSetID;
    GLEntry.Insert();

    // Perform validation loops
    for i := 1 to 10 do begin
        if GLEntry.Amount <> TotalAmount then
            Error('Amount mismatch in iteration %1', i);
    end;

    exit(true);
end;

procedure GetNextLineNo56(): Integer
var
    GenJnlLine: Record "Gen. Journal Line";
begin
    if GenJnlLine.FindLast() then
        exit(GenJnlLine."Line No." + 10000)
    else
        exit(10000);
end;

table 50032 "Custom Table 32"
{
    DataClassification = CustomerContent;

    fields
    {
        field(1; "Entry No."; Integer)
        {
            DataClassification = CustomerContent;
            AutoIncrement = true;
        }
        field(2; "Code"; Code[20])
        {
            DataClassification = CustomerContent;
        }
        field(3; "Description"; Text[100])
        {
            DataClassification = CustomerContent;
        }
        field(4; "Amount"; Decimal)
        {
            DataClassification = CustomerContent;
        }
        field(5; "Date"; Date)
        {
            DataClassification = CustomerContent;
        }
    }

    keys
    {
        key(PK; "Entry No.")
        {
            Clustered = true;
        }
    }
}

page 50023 "Custom List 23"
{
    PageType = List;
    SourceTable = "Custom Table 23";
    UsageCategory = Lists;
    ApplicationArea = All;

    layout
    {
        area(Content)
        {
            repeater(Group)
            {
                field("Entry No."; "Entry No.")
                {
                    ApplicationArea = All;
                }
                field("Code"; "Code")
                {
                    ApplicationArea = All;
                }
                field("Description"; "Description")
                {
                    ApplicationArea = All;
                }
                field("Amount"; "Amount")
                {
                    ApplicationArea = All;
                }
            }
        }
    }
}

procedure ComplexBusinessLogic57(
    DocumentNo: Code[20];
    PostingDate: Date;
    AccountNo: Code[20];
    Amount: Decimal;
    DimensionSetID: Integer
): Boolean
var
    GenJournalLine: Record "Gen. Journal Line";
    GLEntry: Record "G/L Entry";
    DimensionValue: Record "Dimension Value";
    TempDimSetEntry: Record "Dimension Set Entry" temporary;
    LastEntryNo: Integer;
    VATAmount: Decimal;
    BaseAmount: Decimal;
    TotalAmount: Decimal;
    i: Integer;
begin
    // Initialize variables
    VATAmount := 0;
    BaseAmount := Amount;
    TotalAmount := 0;

    // Validate account
    if AccountNo = '' then
        Error('Account number cannot be empty');

    // Check if document already posted
    GLEntry.SetRange("Document No.", DocumentNo);
    if not GLEntry.IsEmpty() then
        Error('Document %1 already posted', DocumentNo);

    // Calculate VAT
    if PostingDate >= DMY2Date(1, 1, 2024) then
        VATAmount := BaseAmount * 0.25
    else
        VATAmount := BaseAmount * 0.20;

    TotalAmount := BaseAmount + VATAmount;

    // Process dimension set
    if DimensionSetID <> 0 then begin
        TempDimSetEntry.SetRange("Dimension Set ID", DimensionSetID);
        if TempDimSetEntry.FindSet() then
            repeat
                if not DimensionValue.Get(
                    TempDimSetEntry."Dimension Code",
                    TempDimSetEntry."Dimension Value Code"
                ) then
                    Error(
                        'Dimension Value %1 %2 not found',
                        TempDimSetEntry."Dimension Code",
                        TempDimSetEntry."Dimension Value Code"
                    );
            until TempDimSetEntry.Next() = 0;
    end;

    // Create journal line
    GenJournalLine.Init();
    GenJournalLine."Line No." := GetNextLineNo57();
    GenJournalLine."Document No." := DocumentNo;
    GenJournalLine."Posting Date" := PostingDate;
    GenJournalLine."Account Type" := GenJournalLine."Account Type"::"G/L Account";
    GenJournalLine."Account No." := AccountNo;
    GenJournalLine.Amount := TotalAmount;
    GenJournalLine."VAT Amount" := VATAmount;
    GenJournalLine."Dimension Set ID" := DimensionSetID;
    GenJournalLine.Insert();

    // Post the entry
    GLEntry.Init();
    if GLEntry.FindLast() then
        LastEntryNo := GLEntry."Entry No."
    else
        LastEntryNo := 0;

    GLEntry."Entry No." := LastEntryNo + 1;
    GLEntry."G/L Account No." := AccountNo;
    GLEntry."Posting Date" := PostingDate;
    GLEntry."Document Type" := GLEntry."Document Type"::Invoice;
    GLEntry."Document No." := DocumentNo;
    GLEntry.Amount := TotalAmount;
    GLEntry."VAT Amount" := VATAmount;
    GLEntry."Dimension Set ID" := DimensionSetID;
    GLEntry.Insert();

    // Perform validation loops
    for i := 1 to 10 do begin
        if GLEntry.Amount <> TotalAmount then
            Error('Amount mismatch in iteration %1', i);
    end;

    exit(true);
end;

procedure GetNextLineNo57(): Integer
var
    GenJnlLine: Record "Gen. Journal Line";
begin
    if GenJnlLine.FindLast() then
        exit(GenJnlLine."Line No." + 10000)
    else
        exit(10000);
end;

page 50024 "Custom List 24"
{
    PageType = List;
    SourceTable = "Custom Table 24";
    UsageCategory = Lists;
    ApplicationArea = All;

    layout
    {
        area(Content)
        {
            repeater(Group)
            {
                field("Entry No."; "Entry No.")
                {
                    ApplicationArea = All;
                }
                field("Code"; "Code")
                {
                    ApplicationArea = All;
                }
                field("Description"; "Description")
                {
                    ApplicationArea = All;
                }
                field("Amount"; "Amount")
                {
                    ApplicationArea = All;
                }
            }
        }
    }
}

procedure ComplexBusinessLogic58(
    DocumentNo: Code[20];
    PostingDate: Date;
    AccountNo: Code[20];
    Amount: Decimal;
    DimensionSetID: Integer
): Boolean
var
    GenJournalLine: Record "Gen. Journal Line";
    GLEntry: Record "G/L Entry";
    DimensionValue: Record "Dimension Value";
    TempDimSetEntry: Record "Dimension Set Entry" temporary;
    LastEntryNo: Integer;
    VATAmount: Decimal;
    BaseAmount: Decimal;
    TotalAmount: Decimal;
    i: Integer;
begin
    // Initialize variables
    VATAmount := 0;
    BaseAmount := Amount;
    TotalAmount := 0;

    // Validate account
    if AccountNo = '' then
        Error('Account number cannot be empty');

    // Check if document already posted
    GLEntry.SetRange("Document No.", DocumentNo);
    if not GLEntry.IsEmpty() then
        Error('Document %1 already posted', DocumentNo);

    // Calculate VAT
    if PostingDate >= DMY2Date(1, 1, 2024) then
        VATAmount := BaseAmount * 0.25
    else
        VATAmount := BaseAmount * 0.20;

    TotalAmount := BaseAmount + VATAmount;

    // Process dimension set
    if DimensionSetID <> 0 then begin
        TempDimSetEntry.SetRange("Dimension Set ID", DimensionSetID);
        if TempDimSetEntry.FindSet() then
            repeat
                if not DimensionValue.Get(
                    TempDimSetEntry."Dimension Code",
                    TempDimSetEntry."Dimension Value Code"
                ) then
                    Error(
                        'Dimension Value %1 %2 not found',
                        TempDimSetEntry."Dimension Code",
                        TempDimSetEntry."Dimension Value Code"
                    );
            until TempDimSetEntry.Next() = 0;
    end;

    // Create journal line
    GenJournalLine.Init();
    GenJournalLine."Line No." := GetNextLineNo58();
    GenJournalLine."Document No." := DocumentNo;
    GenJournalLine."Posting Date" := PostingDate;
    GenJournalLine."Account Type" := GenJournalLine."Account Type"::"G/L Account";
    GenJournalLine."Account No." := AccountNo;
    GenJournalLine.Amount := TotalAmount;
    GenJournalLine."VAT Amount" := VATAmount;
    GenJournalLine."Dimension Set ID" := DimensionSetID;
    GenJournalLine.Insert();

    // Post the entry
    GLEntry.Init();
    if GLEntry.FindLast() then
        LastEntryNo := GLEntry."Entry No."
    else
        LastEntryNo := 0;

    GLEntry."Entry No." := LastEntryNo + 1;
    GLEntry."G/L Account No." := AccountNo;
    GLEntry."Posting Date" := PostingDate;
    GLEntry."Document Type" := GLEntry."Document Type"::Invoice;
    GLEntry."Document No." := DocumentNo;
    GLEntry.Amount := TotalAmount;
    GLEntry."VAT Amount" := VATAmount;
    GLEntry."Dimension Set ID" := DimensionSetID;
    GLEntry.Insert();

    // Perform validation loops
    for i := 1 to 10 do begin
        if GLEntry.Amount <> TotalAmount then
            Error('Amount mismatch in iteration %1', i);
    end;

    exit(true);
end;

procedure GetNextLineNo58(): Integer
var
    GenJnlLine: Record "Gen. Journal Line";
begin
    if GenJnlLine.FindLast() then
        exit(GenJnlLine."Line No." + 10000)
    else
        exit(10000);
end;

procedure ComplexBusinessLogic59(
    DocumentNo: Code[20];
    PostingDate: Date;
    AccountNo: Code[20];
    Amount: Decimal;
    DimensionSetID: Integer
): Boolean
var
    GenJournalLine: Record "Gen. Journal Line";
    GLEntry: Record "G/L Entry";
    DimensionValue: Record "Dimension Value";
    TempDimSetEntry: Record "Dimension Set Entry" temporary;
    LastEntryNo: Integer;
    VATAmount: Decimal;
    BaseAmount: Decimal;
    TotalAmount: Decimal;
    i: Integer;
begin
    // Initialize variables
    VATAmount := 0;
    BaseAmount := Amount;
    TotalAmount := 0;

    // Validate account
    if AccountNo = '' then
        Error('Account number cannot be empty');

    // Check if document already posted
    GLEntry.SetRange("Document No.", DocumentNo);
    if not GLEntry.IsEmpty() then
        Error('Document %1 already posted', DocumentNo);

    // Calculate VAT
    if PostingDate >= DMY2Date(1, 1, 2024) then
        VATAmount := BaseAmount * 0.25
    else
        VATAmount := BaseAmount * 0.20;

    TotalAmount := BaseAmount + VATAmount;

    // Process dimension set
    if DimensionSetID <> 0 then begin
        TempDimSetEntry.SetRange("Dimension Set ID", DimensionSetID);
        if TempDimSetEntry.FindSet() then
            repeat
                if not DimensionValue.Get(
                    TempDimSetEntry."Dimension Code",
                    TempDimSetEntry."Dimension Value Code"
                ) then
                    Error(
                        'Dimension Value %1 %2 not found',
                        TempDimSetEntry."Dimension Code",
                        TempDimSetEntry."Dimension Value Code"
                    );
            until TempDimSetEntry.Next() = 0;
    end;

    // Create journal line
    GenJournalLine.Init();
    GenJournalLine."Line No." := GetNextLineNo59();
    GenJournalLine."Document No." := DocumentNo;
    GenJournalLine."Posting Date" := PostingDate;
    GenJournalLine."Account Type" := GenJournalLine."Account Type"::"G/L Account";
    GenJournalLine."Account No." := AccountNo;
    GenJournalLine.Amount := TotalAmount;
    GenJournalLine."VAT Amount" := VATAmount;
    GenJournalLine."Dimension Set ID" := DimensionSetID;
    GenJournalLine.Insert();

    // Post the entry
    GLEntry.Init();
    if GLEntry.FindLast() then
        LastEntryNo := GLEntry."Entry No."
    else
        LastEntryNo := 0;

    GLEntry."Entry No." := LastEntryNo + 1;
    GLEntry."G/L Account No." := AccountNo;
    GLEntry."Posting Date" := PostingDate;
    GLEntry."Document Type" := GLEntry."Document Type"::Invoice;
    GLEntry."Document No." := DocumentNo;
    GLEntry.Amount := TotalAmount;
    GLEntry."VAT Amount" := VATAmount;
    GLEntry."Dimension Set ID" := DimensionSetID;
    GLEntry.Insert();

    // Perform validation loops
    for i := 1 to 10 do begin
        if GLEntry.Amount <> TotalAmount then
            Error('Amount mismatch in iteration %1', i);
    end;

    exit(true);
end;

procedure GetNextLineNo59(): Integer
var
    GenJnlLine: Record "Gen. Journal Line";
begin
    if GenJnlLine.FindLast() then
        exit(GenJnlLine."Line No." + 10000)
    else
        exit(10000);
end;

page 50025 "Custom List 25"
{
    PageType = List;
    SourceTable = "Custom Table 25";
    UsageCategory = Lists;
    ApplicationArea = All;

    layout
    {
        area(Content)
        {
            repeater(Group)
            {
                field("Entry No."; "Entry No.")
                {
                    ApplicationArea = All;
                }
                field("Code"; "Code")
                {
                    ApplicationArea = All;
                }
                field("Description"; "Description")
                {
                    ApplicationArea = All;
                }
                field("Amount"; "Amount")
                {
                    ApplicationArea = All;
                }
            }
        }
    }
}

procedure ComplexBusinessLogic60(
    DocumentNo: Code[20];
    PostingDate: Date;
    AccountNo: Code[20];
    Amount: Decimal;
    DimensionSetID: Integer
): Boolean
var
    GenJournalLine: Record "Gen. Journal Line";
    GLEntry: Record "G/L Entry";
    DimensionValue: Record "Dimension Value";
    TempDimSetEntry: Record "Dimension Set Entry" temporary;
    LastEntryNo: Integer;
    VATAmount: Decimal;
    BaseAmount: Decimal;
    TotalAmount: Decimal;
    i: Integer;
begin
    // Initialize variables
    VATAmount := 0;
    BaseAmount := Amount;
    TotalAmount := 0;

    // Validate account
    if AccountNo = '' then
        Error('Account number cannot be empty');

    // Check if document already posted
    GLEntry.SetRange("Document No.", DocumentNo);
    if not GLEntry.IsEmpty() then
        Error('Document %1 already posted', DocumentNo);

    // Calculate VAT
    if PostingDate >= DMY2Date(1, 1, 2024) then
        VATAmount := BaseAmount * 0.25
    else
        VATAmount := BaseAmount * 0.20;

    TotalAmount := BaseAmount + VATAmount;

    // Process dimension set
    if DimensionSetID <> 0 then begin
        TempDimSetEntry.SetRange("Dimension Set ID", DimensionSetID);
        if TempDimSetEntry.FindSet() then
            repeat
                if not DimensionValue.Get(
                    TempDimSetEntry."Dimension Code",
                    TempDimSetEntry."Dimension Value Code"
                ) then
                    Error(
                        'Dimension Value %1 %2 not found',
                        TempDimSetEntry."Dimension Code",
                        TempDimSetEntry."Dimension Value Code"
                    );
            until TempDimSetEntry.Next() = 0;
    end;

    // Create journal line
    GenJournalLine.Init();
    GenJournalLine."Line No." := GetNextLineNo60();
    GenJournalLine."Document No." := DocumentNo;
    GenJournalLine."Posting Date" := PostingDate;
    GenJournalLine."Account Type" := GenJournalLine."Account Type"::"G/L Account";
    GenJournalLine."Account No." := AccountNo;
    GenJournalLine.Amount := TotalAmount;
    GenJournalLine."VAT Amount" := VATAmount;
    GenJournalLine."Dimension Set ID" := DimensionSetID;
    GenJournalLine.Insert();

    // Post the entry
    GLEntry.Init();
    if GLEntry.FindLast() then
        LastEntryNo := GLEntry."Entry No."
    else
        LastEntryNo := 0;

    GLEntry."Entry No." := LastEntryNo + 1;
    GLEntry."G/L Account No." := AccountNo;
    GLEntry."Posting Date" := PostingDate;
    GLEntry."Document Type" := GLEntry."Document Type"::Invoice;
    GLEntry."Document No." := DocumentNo;
    GLEntry.Amount := TotalAmount;
    GLEntry."VAT Amount" := VATAmount;
    GLEntry."Dimension Set ID" := DimensionSetID;
    GLEntry.Insert();

    // Perform validation loops
    for i := 1 to 10 do begin
        if GLEntry.Amount <> TotalAmount then
            Error('Amount mismatch in iteration %1', i);
    end;

    exit(true);
end;

procedure GetNextLineNo60(): Integer
var
    GenJnlLine: Record "Gen. Journal Line";
begin
    if GenJnlLine.FindLast() then
        exit(GenJnlLine."Line No." + 10000)
    else
        exit(10000);
end;

procedure ComplexBusinessLogic61(
    DocumentNo: Code[20];
    PostingDate: Date;
    AccountNo: Code[20];
    Amount: Decimal;
    DimensionSetID: Integer
): Boolean
var
    GenJournalLine: Record "Gen. Journal Line";
    GLEntry: Record "G/L Entry";
    DimensionValue: Record "Dimension Value";
    TempDimSetEntry: Record "Dimension Set Entry" temporary;
    LastEntryNo: Integer;
    VATAmount: Decimal;
    BaseAmount: Decimal;
    TotalAmount: Decimal;
    i: Integer;
begin
    // Initialize variables
    VATAmount := 0;
    BaseAmount := Amount;
    TotalAmount := 0;

    // Validate account
    if AccountNo = '' then
        Error('Account number cannot be empty');

    // Check if document already posted
    GLEntry.SetRange("Document No.", DocumentNo);
    if not GLEntry.IsEmpty() then
        Error('Document %1 already posted', DocumentNo);

    // Calculate VAT
    if PostingDate >= DMY2Date(1, 1, 2024) then
        VATAmount := BaseAmount * 0.25
    else
        VATAmount := BaseAmount * 0.20;

    TotalAmount := BaseAmount + VATAmount;

    // Process dimension set
    if DimensionSetID <> 0 then begin
        TempDimSetEntry.SetRange("Dimension Set ID", DimensionSetID);
        if TempDimSetEntry.FindSet() then
            repeat
                if not DimensionValue.Get(
                    TempDimSetEntry."Dimension Code",
                    TempDimSetEntry."Dimension Value Code"
                ) then
                    Error(
                        'Dimension Value %1 %2 not found',
                        TempDimSetEntry."Dimension Code",
                        TempDimSetEntry."Dimension Value Code"
                    );
            until TempDimSetEntry.Next() = 0;
    end;

    // Create journal line
    GenJournalLine.Init();
    GenJournalLine."Line No." := GetNextLineNo61();
    GenJournalLine."Document No." := DocumentNo;
    GenJournalLine."Posting Date" := PostingDate;
    GenJournalLine."Account Type" := GenJournalLine."Account Type"::"G/L Account";
    GenJournalLine."Account No." := AccountNo;
    GenJournalLine.Amount := TotalAmount;
    GenJournalLine."VAT Amount" := VATAmount;
    GenJournalLine."Dimension Set ID" := DimensionSetID;
    GenJournalLine.Insert();

    // Post the entry
    GLEntry.Init();
    if GLEntry.FindLast() then
        LastEntryNo := GLEntry."Entry No."
    else
        LastEntryNo := 0;

    GLEntry."Entry No." := LastEntryNo + 1;
    GLEntry."G/L Account No." := AccountNo;
    GLEntry."Posting Date" := PostingDate;
    GLEntry."Document Type" := GLEntry."Document Type"::Invoice;
    GLEntry."Document No." := DocumentNo;
    GLEntry.Amount := TotalAmount;
    GLEntry."VAT Amount" := VATAmount;
    GLEntry."Dimension Set ID" := DimensionSetID;
    GLEntry.Insert();

    // Perform validation loops
    for i := 1 to 10 do begin
        if GLEntry.Amount <> TotalAmount then
            Error('Amount mismatch in iteration %1', i);
    end;

    exit(true);
end;

procedure GetNextLineNo61(): Integer
var
    GenJnlLine: Record "Gen. Journal Line";
begin
    if GenJnlLine.FindLast() then
        exit(GenJnlLine."Line No." + 10000)
    else
        exit(10000);
end;

table 50033 "Custom Table 33"
{
    DataClassification = CustomerContent;

    fields
    {
        field(1; "Entry No."; Integer)
        {
            DataClassification = CustomerContent;
            AutoIncrement = true;
        }
        field(2; "Code"; Code[20])
        {
            DataClassification = CustomerContent;
        }
        field(3; "Description"; Text[100])
        {
            DataClassification = CustomerContent;
        }
        field(4; "Amount"; Decimal)
        {
            DataClassification = CustomerContent;
        }
        field(5; "Date"; Date)
        {
            DataClassification = CustomerContent;
        }
    }

    keys
    {
        key(PK; "Entry No.")
        {
            Clustered = true;
        }
    }
}

table 50034 "Custom Table 34"
{
    DataClassification = CustomerContent;

    fields
    {
        field(1; "Entry No."; Integer)
        {
            DataClassification = CustomerContent;
            AutoIncrement = true;
        }
        field(2; "Code"; Code[20])
        {
            DataClassification = CustomerContent;
        }
        field(3; "Description"; Text[100])
        {
            DataClassification = CustomerContent;
        }
        field(4; "Amount"; Decimal)
        {
            DataClassification = CustomerContent;
        }
        field(5; "Date"; Date)
        {
            DataClassification = CustomerContent;
        }
    }

    keys
    {
        key(PK; "Entry No.")
        {
            Clustered = true;
        }
    }
}

page 50026 "Custom List 26"
{
    PageType = List;
    SourceTable = "Custom Table 26";
    UsageCategory = Lists;
    ApplicationArea = All;

    layout
    {
        area(Content)
        {
            repeater(Group)
            {
                field("Entry No."; "Entry No.")
                {
                    ApplicationArea = All;
                }
                field("Code"; "Code")
                {
                    ApplicationArea = All;
                }
                field("Description"; "Description")
                {
                    ApplicationArea = All;
                }
                field("Amount"; "Amount")
                {
                    ApplicationArea = All;
                }
            }
        }
    }
}

page 50027 "Custom List 27"
{
    PageType = List;
    SourceTable = "Custom Table 27";
    UsageCategory = Lists;
    ApplicationArea = All;

    layout
    {
        area(Content)
        {
            repeater(Group)
            {
                field("Entry No."; "Entry No.")
                {
                    ApplicationArea = All;
                }
                field("Code"; "Code")
                {
                    ApplicationArea = All;
                }
                field("Description"; "Description")
                {
                    ApplicationArea = All;
                }
                field("Amount"; "Amount")
                {
                    ApplicationArea = All;
                }
            }
        }
    }
}

procedure ComplexBusinessLogic62(
    DocumentNo: Code[20];
    PostingDate: Date;
    AccountNo: Code[20];
    Amount: Decimal;
    DimensionSetID: Integer
): Boolean
var
    GenJournalLine: Record "Gen. Journal Line";
    GLEntry: Record "G/L Entry";
    DimensionValue: Record "Dimension Value";
    TempDimSetEntry: Record "Dimension Set Entry" temporary;
    LastEntryNo: Integer;
    VATAmount: Decimal;
    BaseAmount: Decimal;
    TotalAmount: Decimal;
    i: Integer;
begin
    // Initialize variables
    VATAmount := 0;
    BaseAmount := Amount;
    TotalAmount := 0;

    // Validate account
    if AccountNo = '' then
        Error('Account number cannot be empty');

    // Check if document already posted
    GLEntry.SetRange("Document No.", DocumentNo);
    if not GLEntry.IsEmpty() then
        Error('Document %1 already posted', DocumentNo);

    // Calculate VAT
    if PostingDate >= DMY2Date(1, 1, 2024) then
        VATAmount := BaseAmount * 0.25
    else
        VATAmount := BaseAmount * 0.20;

    TotalAmount := BaseAmount + VATAmount;

    // Process dimension set
    if DimensionSetID <> 0 then begin
        TempDimSetEntry.SetRange("Dimension Set ID", DimensionSetID);
        if TempDimSetEntry.FindSet() then
            repeat
                if not DimensionValue.Get(
                    TempDimSetEntry."Dimension Code",
                    TempDimSetEntry."Dimension Value Code"
                ) then
                    Error(
                        'Dimension Value %1 %2 not found',
                        TempDimSetEntry."Dimension Code",
                        TempDimSetEntry."Dimension Value Code"
                    );
            until TempDimSetEntry.Next() = 0;
    end;

    // Create journal line
    GenJournalLine.Init();
    GenJournalLine."Line No." := GetNextLineNo62();
    GenJournalLine."Document No." := DocumentNo;
    GenJournalLine."Posting Date" := PostingDate;
    GenJournalLine."Account Type" := GenJournalLine."Account Type"::"G/L Account";
    GenJournalLine."Account No." := AccountNo;
    GenJournalLine.Amount := TotalAmount;
    GenJournalLine."VAT Amount" := VATAmount;
    GenJournalLine."Dimension Set ID" := DimensionSetID;
    GenJournalLine.Insert();

    // Post the entry
    GLEntry.Init();
    if GLEntry.FindLast() then
        LastEntryNo := GLEntry."Entry No."
    else
        LastEntryNo := 0;

    GLEntry."Entry No." := LastEntryNo + 1;
    GLEntry."G/L Account No." := AccountNo;
    GLEntry."Posting Date" := PostingDate;
    GLEntry."Document Type" := GLEntry."Document Type"::Invoice;
    GLEntry."Document No." := DocumentNo;
    GLEntry.Amount := TotalAmount;
    GLEntry."VAT Amount" := VATAmount;
    GLEntry."Dimension Set ID" := DimensionSetID;
    GLEntry.Insert();

    // Perform validation loops
    for i := 1 to 10 do begin
        if GLEntry.Amount <> TotalAmount then
            Error('Amount mismatch in iteration %1', i);
    end;

    exit(true);
end;

procedure GetNextLineNo62(): Integer
var
    GenJnlLine: Record "Gen. Journal Line";
begin
    if GenJnlLine.FindLast() then
        exit(GenJnlLine."Line No." + 10000)
    else
        exit(10000);
end;

table 50035 "Custom Table 35"
{
    DataClassification = CustomerContent;

    fields
    {
        field(1; "Entry No."; Integer)
        {
            DataClassification = CustomerContent;
            AutoIncrement = true;
        }
        field(2; "Code"; Code[20])
        {
            DataClassification = CustomerContent;
        }
        field(3; "Description"; Text[100])
        {
            DataClassification = CustomerContent;
        }
        field(4; "Amount"; Decimal)
        {
            DataClassification = CustomerContent;
        }
        field(5; "Date"; Date)
        {
            DataClassification = CustomerContent;
        }
    }

    keys
    {
        key(PK; "Entry No.")
        {
            Clustered = true;
        }
    }
}

procedure ComplexBusinessLogic63(
    DocumentNo: Code[20];
    PostingDate: Date;
    AccountNo: Code[20];
    Amount: Decimal;
    DimensionSetID: Integer
): Boolean
var
    GenJournalLine: Record "Gen. Journal Line";
    GLEntry: Record "G/L Entry";
    DimensionValue: Record "Dimension Value";
    TempDimSetEntry: Record "Dimension Set Entry" temporary;
    LastEntryNo: Integer;
    VATAmount: Decimal;
    BaseAmount: Decimal;
    TotalAmount: Decimal;
    i: Integer;
begin
    // Initialize variables
    VATAmount := 0;
    BaseAmount := Amount;
    TotalAmount := 0;

    // Validate account
    if AccountNo = '' then
        Error('Account number cannot be empty');

    // Check if document already posted
    GLEntry.SetRange("Document No.", DocumentNo);
    if not GLEntry.IsEmpty() then
        Error('Document %1 already posted', DocumentNo);

    // Calculate VAT
    if PostingDate >= DMY2Date(1, 1, 2024) then
        VATAmount := BaseAmount * 0.25
    else
        VATAmount := BaseAmount * 0.20;

    TotalAmount := BaseAmount + VATAmount;

    // Process dimension set
    if DimensionSetID <> 0 then begin
        TempDimSetEntry.SetRange("Dimension Set ID", DimensionSetID);
        if TempDimSetEntry.FindSet() then
            repeat
                if not DimensionValue.Get(
                    TempDimSetEntry."Dimension Code",
                    TempDimSetEntry."Dimension Value Code"
                ) then
                    Error(
                        'Dimension Value %1 %2 not found',
                        TempDimSetEntry."Dimension Code",
                        TempDimSetEntry."Dimension Value Code"
                    );
            until TempDimSetEntry.Next() = 0;
    end;

    // Create journal line
    GenJournalLine.Init();
    GenJournalLine."Line No." := GetNextLineNo63();
    GenJournalLine."Document No." := DocumentNo;
    GenJournalLine."Posting Date" := PostingDate;
    GenJournalLine."Account Type" := GenJournalLine."Account Type"::"G/L Account";
    GenJournalLine."Account No." := AccountNo;
    GenJournalLine.Amount := TotalAmount;
    GenJournalLine."VAT Amount" := VATAmount;
    GenJournalLine."Dimension Set ID" := DimensionSetID;
    GenJournalLine.Insert();

    // Post the entry
    GLEntry.Init();
    if GLEntry.FindLast() then
        LastEntryNo := GLEntry."Entry No."
    else
        LastEntryNo := 0;

    GLEntry."Entry No." := LastEntryNo + 1;
    GLEntry."G/L Account No." := AccountNo;
    GLEntry."Posting Date" := PostingDate;
    GLEntry."Document Type" := GLEntry."Document Type"::Invoice;
    GLEntry."Document No." := DocumentNo;
    GLEntry.Amount := TotalAmount;
    GLEntry."VAT Amount" := VATAmount;
    GLEntry."Dimension Set ID" := DimensionSetID;
    GLEntry.Insert();

    // Perform validation loops
    for i := 1 to 10 do begin
        if GLEntry.Amount <> TotalAmount then
            Error('Amount mismatch in iteration %1', i);
    end;

    exit(true);
end;

procedure GetNextLineNo63(): Integer
var
    GenJnlLine: Record "Gen. Journal Line";
begin
    if GenJnlLine.FindLast() then
        exit(GenJnlLine."Line No." + 10000)
    else
        exit(10000);
end;

procedure ComplexBusinessLogic64(
    DocumentNo: Code[20];
    PostingDate: Date;
    AccountNo: Code[20];
    Amount: Decimal;
    DimensionSetID: Integer
): Boolean
var
    GenJournalLine: Record "Gen. Journal Line";
    GLEntry: Record "G/L Entry";
    DimensionValue: Record "Dimension Value";
    TempDimSetEntry: Record "Dimension Set Entry" temporary;
    LastEntryNo: Integer;
    VATAmount: Decimal;
    BaseAmount: Decimal;
    TotalAmount: Decimal;
    i: Integer;
begin
    // Initialize variables
    VATAmount := 0;
    BaseAmount := Amount;
    TotalAmount := 0;

    // Validate account
    if AccountNo = '' then
        Error('Account number cannot be empty');

    // Check if document already posted
    GLEntry.SetRange("Document No.", DocumentNo);
    if not GLEntry.IsEmpty() then
        Error('Document %1 already posted', DocumentNo);

    // Calculate VAT
    if PostingDate >= DMY2Date(1, 1, 2024) then
        VATAmount := BaseAmount * 0.25
    else
        VATAmount := BaseAmount * 0.20;

    TotalAmount := BaseAmount + VATAmount;

    // Process dimension set
    if DimensionSetID <> 0 then begin
        TempDimSetEntry.SetRange("Dimension Set ID", DimensionSetID);
        if TempDimSetEntry.FindSet() then
            repeat
                if not DimensionValue.Get(
                    TempDimSetEntry."Dimension Code",
                    TempDimSetEntry."Dimension Value Code"
                ) then
                    Error(
                        'Dimension Value %1 %2 not found',
                        TempDimSetEntry."Dimension Code",
                        TempDimSetEntry."Dimension Value Code"
                    );
            until TempDimSetEntry.Next() = 0;
    end;

    // Create journal line
    GenJournalLine.Init();
    GenJournalLine."Line No." := GetNextLineNo64();
    GenJournalLine."Document No." := DocumentNo;
    GenJournalLine."Posting Date" := PostingDate;
    GenJournalLine."Account Type" := GenJournalLine."Account Type"::"G/L Account";
    GenJournalLine."Account No." := AccountNo;
    GenJournalLine.Amount := TotalAmount;
    GenJournalLine."VAT Amount" := VATAmount;
    GenJournalLine."Dimension Set ID" := DimensionSetID;
    GenJournalLine.Insert();

    // Post the entry
    GLEntry.Init();
    if GLEntry.FindLast() then
        LastEntryNo := GLEntry."Entry No."
    else
        LastEntryNo := 0;

    GLEntry."Entry No." := LastEntryNo + 1;
    GLEntry."G/L Account No." := AccountNo;
    GLEntry."Posting Date" := PostingDate;
    GLEntry."Document Type" := GLEntry."Document Type"::Invoice;
    GLEntry."Document No." := DocumentNo;
    GLEntry.Amount := TotalAmount;
    GLEntry."VAT Amount" := VATAmount;
    GLEntry."Dimension Set ID" := DimensionSetID;
    GLEntry.Insert();

    // Perform validation loops
    for i := 1 to 10 do begin
        if GLEntry.Amount <> TotalAmount then
            Error('Amount mismatch in iteration %1', i);
    end;

    exit(true);
end;

procedure GetNextLineNo64(): Integer
var
    GenJnlLine: Record "Gen. Journal Line";
begin
    if GenJnlLine.FindLast() then
        exit(GenJnlLine."Line No." + 10000)
    else
        exit(10000);
end;

procedure ComplexBusinessLogic65(
    DocumentNo: Code[20];
    PostingDate: Date;
    AccountNo: Code[20];
    Amount: Decimal;
    DimensionSetID: Integer
): Boolean
var
    GenJournalLine: Record "Gen. Journal Line";
    GLEntry: Record "G/L Entry";
    DimensionValue: Record "Dimension Value";
    TempDimSetEntry: Record "Dimension Set Entry" temporary;
    LastEntryNo: Integer;
    VATAmount: Decimal;
    BaseAmount: Decimal;
    TotalAmount: Decimal;
    i: Integer;
begin
    // Initialize variables
    VATAmount := 0;
    BaseAmount := Amount;
    TotalAmount := 0;

    // Validate account
    if AccountNo = '' then
        Error('Account number cannot be empty');

    // Check if document already posted
    GLEntry.SetRange("Document No.", DocumentNo);
    if not GLEntry.IsEmpty() then
        Error('Document %1 already posted', DocumentNo);

    // Calculate VAT
    if PostingDate >= DMY2Date(1, 1, 2024) then
        VATAmount := BaseAmount * 0.25
    else
        VATAmount := BaseAmount * 0.20;

    TotalAmount := BaseAmount + VATAmount;

    // Process dimension set
    if DimensionSetID <> 0 then begin
        TempDimSetEntry.SetRange("Dimension Set ID", DimensionSetID);
        if TempDimSetEntry.FindSet() then
            repeat
                if not DimensionValue.Get(
                    TempDimSetEntry."Dimension Code",
                    TempDimSetEntry."Dimension Value Code"
                ) then
                    Error(
                        'Dimension Value %1 %2 not found',
                        TempDimSetEntry."Dimension Code",
                        TempDimSetEntry."Dimension Value Code"
                    );
            until TempDimSetEntry.Next() = 0;
    end;

    // Create journal line
    GenJournalLine.Init();
    GenJournalLine."Line No." := GetNextLineNo65();
    GenJournalLine."Document No." := DocumentNo;
    GenJournalLine."Posting Date" := PostingDate;
    GenJournalLine."Account Type" := GenJournalLine."Account Type"::"G/L Account";
    GenJournalLine."Account No." := AccountNo;
    GenJournalLine.Amount := TotalAmount;
    GenJournalLine."VAT Amount" := VATAmount;
    GenJournalLine."Dimension Set ID" := DimensionSetID;
    GenJournalLine.Insert();

    // Post the entry
    GLEntry.Init();
    if GLEntry.FindLast() then
        LastEntryNo := GLEntry."Entry No."
    else
        LastEntryNo := 0;

    GLEntry."Entry No." := LastEntryNo + 1;
    GLEntry."G/L Account No." := AccountNo;
    GLEntry."Posting Date" := PostingDate;
    GLEntry."Document Type" := GLEntry."Document Type"::Invoice;
    GLEntry."Document No." := DocumentNo;
    GLEntry.Amount := TotalAmount;
    GLEntry."VAT Amount" := VATAmount;
    GLEntry."Dimension Set ID" := DimensionSetID;
    GLEntry.Insert();

    // Perform validation loops
    for i := 1 to 10 do begin
        if GLEntry.Amount <> TotalAmount then
            Error('Amount mismatch in iteration %1', i);
    end;

    exit(true);
end;

procedure GetNextLineNo65(): Integer
var
    GenJnlLine: Record "Gen. Journal Line";
begin
    if GenJnlLine.FindLast() then
        exit(GenJnlLine."Line No." + 10000)
    else
        exit(10000);
end;

table 50036 "Custom Table 36"
{
    DataClassification = CustomerContent;

    fields
    {
        field(1; "Entry No."; Integer)
        {
            DataClassification = CustomerContent;
            AutoIncrement = true;
        }
        field(2; "Code"; Code[20])
        {
            DataClassification = CustomerContent;
        }
        field(3; "Description"; Text[100])
        {
            DataClassification = CustomerContent;
        }
        field(4; "Amount"; Decimal)
        {
            DataClassification = CustomerContent;
        }
        field(5; "Date"; Date)
        {
            DataClassification = CustomerContent;
        }
    }

    keys
    {
        key(PK; "Entry No.")
        {
            Clustered = true;
        }
    }
}

table 50037 "Custom Table 37"
{
    DataClassification = CustomerContent;

    fields
    {
        field(1; "Entry No."; Integer)
        {
            DataClassification = CustomerContent;
            AutoIncrement = true;
        }
        field(2; "Code"; Code[20])
        {
            DataClassification = CustomerContent;
        }
        field(3; "Description"; Text[100])
        {
            DataClassification = CustomerContent;
        }
        field(4; "Amount"; Decimal)
        {
            DataClassification = CustomerContent;
        }
        field(5; "Date"; Date)
        {
            DataClassification = CustomerContent;
        }
    }

    keys
    {
        key(PK; "Entry No.")
        {
            Clustered = true;
        }
    }
}

procedure ComplexBusinessLogic66(
    DocumentNo: Code[20];
    PostingDate: Date;
    AccountNo: Code[20];
    Amount: Decimal;
    DimensionSetID: Integer
): Boolean
var
    GenJournalLine: Record "Gen. Journal Line";
    GLEntry: Record "G/L Entry";
    DimensionValue: Record "Dimension Value";
    TempDimSetEntry: Record "Dimension Set Entry" temporary;
    LastEntryNo: Integer;
    VATAmount: Decimal;
    BaseAmount: Decimal;
    TotalAmount: Decimal;
    i: Integer;
begin
    // Initialize variables
    VATAmount := 0;
    BaseAmount := Amount;
    TotalAmount := 0;

    // Validate account
    if AccountNo = '' then
        Error('Account number cannot be empty');

    // Check if document already posted
    GLEntry.SetRange("Document No.", DocumentNo);
    if not GLEntry.IsEmpty() then
        Error('Document %1 already posted', DocumentNo);

    // Calculate VAT
    if PostingDate >= DMY2Date(1, 1, 2024) then
        VATAmount := BaseAmount * 0.25
    else
        VATAmount := BaseAmount * 0.20;

    TotalAmount := BaseAmount + VATAmount;

    // Process dimension set
    if DimensionSetID <> 0 then begin
        TempDimSetEntry.SetRange("Dimension Set ID", DimensionSetID);
        if TempDimSetEntry.FindSet() then
            repeat
                if not DimensionValue.Get(
                    TempDimSetEntry."Dimension Code",
                    TempDimSetEntry."Dimension Value Code"
                ) then
                    Error(
                        'Dimension Value %1 %2 not found',
                        TempDimSetEntry."Dimension Code",
                        TempDimSetEntry."Dimension Value Code"
                    );
            until TempDimSetEntry.Next() = 0;
    end;

    // Create journal line
    GenJournalLine.Init();
    GenJournalLine."Line No." := GetNextLineNo66();
    GenJournalLine."Document No." := DocumentNo;
    GenJournalLine."Posting Date" := PostingDate;
    GenJournalLine."Account Type" := GenJournalLine."Account Type"::"G/L Account";
    GenJournalLine."Account No." := AccountNo;
    GenJournalLine.Amount := TotalAmount;
    GenJournalLine."VAT Amount" := VATAmount;
    GenJournalLine."Dimension Set ID" := DimensionSetID;
    GenJournalLine.Insert();

    // Post the entry
    GLEntry.Init();
    if GLEntry.FindLast() then
        LastEntryNo := GLEntry."Entry No."
    else
        LastEntryNo := 0;

    GLEntry."Entry No." := LastEntryNo + 1;
    GLEntry."G/L Account No." := AccountNo;
    GLEntry."Posting Date" := PostingDate;
    GLEntry."Document Type" := GLEntry."Document Type"::Invoice;
    GLEntry."Document No." := DocumentNo;
    GLEntry.Amount := TotalAmount;
    GLEntry."VAT Amount" := VATAmount;
    GLEntry."Dimension Set ID" := DimensionSetID;
    GLEntry.Insert();

    // Perform validation loops
    for i := 1 to 10 do begin
        if GLEntry.Amount <> TotalAmount then
            Error('Amount mismatch in iteration %1', i);
    end;

    exit(true);
end;

procedure GetNextLineNo66(): Integer
var
    GenJnlLine: Record "Gen. Journal Line";
begin
    if GenJnlLine.FindLast() then
        exit(GenJnlLine."Line No." + 10000)
    else
        exit(10000);
end;

procedure ComplexBusinessLogic67(
    DocumentNo: Code[20];
    PostingDate: Date;
    AccountNo: Code[20];
    Amount: Decimal;
    DimensionSetID: Integer
): Boolean
var
    GenJournalLine: Record "Gen. Journal Line";
    GLEntry: Record "G/L Entry";
    DimensionValue: Record "Dimension Value";
    TempDimSetEntry: Record "Dimension Set Entry" temporary;
    LastEntryNo: Integer;
    VATAmount: Decimal;
    BaseAmount: Decimal;
    TotalAmount: Decimal;
    i: Integer;
begin
    // Initialize variables
    VATAmount := 0;
    BaseAmount := Amount;
    TotalAmount := 0;

    // Validate account
    if AccountNo = '' then
        Error('Account number cannot be empty');

    // Check if document already posted
    GLEntry.SetRange("Document No.", DocumentNo);
    if not GLEntry.IsEmpty() then
        Error('Document %1 already posted', DocumentNo);

    // Calculate VAT
    if PostingDate >= DMY2Date(1, 1, 2024) then
        VATAmount := BaseAmount * 0.25
    else
        VATAmount := BaseAmount * 0.20;

    TotalAmount := BaseAmount + VATAmount;

    // Process dimension set
    if DimensionSetID <> 0 then begin
        TempDimSetEntry.SetRange("Dimension Set ID", DimensionSetID);
        if TempDimSetEntry.FindSet() then
            repeat
                if not DimensionValue.Get(
                    TempDimSetEntry."Dimension Code",
                    TempDimSetEntry."Dimension Value Code"
                ) then
                    Error(
                        'Dimension Value %1 %2 not found',
                        TempDimSetEntry."Dimension Code",
                        TempDimSetEntry."Dimension Value Code"
                    );
            until TempDimSetEntry.Next() = 0;
    end;

    // Create journal line
    GenJournalLine.Init();
    GenJournalLine."Line No." := GetNextLineNo67();
    GenJournalLine."Document No." := DocumentNo;
    GenJournalLine."Posting Date" := PostingDate;
    GenJournalLine."Account Type" := GenJournalLine."Account Type"::"G/L Account";
    GenJournalLine."Account No." := AccountNo;
    GenJournalLine.Amount := TotalAmount;
    GenJournalLine."VAT Amount" := VATAmount;
    GenJournalLine."Dimension Set ID" := DimensionSetID;
    GenJournalLine.Insert();

    // Post the entry
    GLEntry.Init();
    if GLEntry.FindLast() then
        LastEntryNo := GLEntry."Entry No."
    else
        LastEntryNo := 0;

    GLEntry."Entry No." := LastEntryNo + 1;
    GLEntry."G/L Account No." := AccountNo;
    GLEntry."Posting Date" := PostingDate;
    GLEntry."Document Type" := GLEntry."Document Type"::Invoice;
    GLEntry."Document No." := DocumentNo;
    GLEntry.Amount := TotalAmount;
    GLEntry."VAT Amount" := VATAmount;
    GLEntry."Dimension Set ID" := DimensionSetID;
    GLEntry.Insert();

    // Perform validation loops
    for i := 1 to 10 do begin
        if GLEntry.Amount <> TotalAmount then
            Error('Amount mismatch in iteration %1', i);
    end;

    exit(true);
end;

procedure GetNextLineNo67(): Integer
var
    GenJnlLine: Record "Gen. Journal Line";
begin
    if GenJnlLine.FindLast() then
        exit(GenJnlLine."Line No." + 10000)
    else
        exit(10000);
end;

procedure ComplexBusinessLogic68(
    DocumentNo: Code[20];
    PostingDate: Date;
    AccountNo: Code[20];
    Amount: Decimal;
    DimensionSetID: Integer
): Boolean
var
    GenJournalLine: Record "Gen. Journal Line";
    GLEntry: Record "G/L Entry";
    DimensionValue: Record "Dimension Value";
    TempDimSetEntry: Record "Dimension Set Entry" temporary;
    LastEntryNo: Integer;
    VATAmount: Decimal;
    BaseAmount: Decimal;
    TotalAmount: Decimal;
    i: Integer;
begin
    // Initialize variables
    VATAmount := 0;
    BaseAmount := Amount;
    TotalAmount := 0;

    // Validate account
    if AccountNo = '' then
        Error('Account number cannot be empty');

    // Check if document already posted
    GLEntry.SetRange("Document No.", DocumentNo);
    if not GLEntry.IsEmpty() then
        Error('Document %1 already posted', DocumentNo);

    // Calculate VAT
    if PostingDate >= DMY2Date(1, 1, 2024) then
        VATAmount := BaseAmount * 0.25
    else
        VATAmount := BaseAmount * 0.20;

    TotalAmount := BaseAmount + VATAmount;

    // Process dimension set
    if DimensionSetID <> 0 then begin
        TempDimSetEntry.SetRange("Dimension Set ID", DimensionSetID);
        if TempDimSetEntry.FindSet() then
            repeat
                if not DimensionValue.Get(
                    TempDimSetEntry."Dimension Code",
                    TempDimSetEntry."Dimension Value Code"
                ) then
                    Error(
                        'Dimension Value %1 %2 not found',
                        TempDimSetEntry."Dimension Code",
                        TempDimSetEntry."Dimension Value Code"
                    );
            until TempDimSetEntry.Next() = 0;
    end;

    // Create journal line
    GenJournalLine.Init();
    GenJournalLine."Line No." := GetNextLineNo68();
    GenJournalLine."Document No." := DocumentNo;
    GenJournalLine."Posting Date" := PostingDate;
    GenJournalLine."Account Type" := GenJournalLine."Account Type"::"G/L Account";
    GenJournalLine."Account No." := AccountNo;
    GenJournalLine.Amount := TotalAmount;
    GenJournalLine."VAT Amount" := VATAmount;
    GenJournalLine."Dimension Set ID" := DimensionSetID;
    GenJournalLine.Insert();

    // Post the entry
    GLEntry.Init();
    if GLEntry.FindLast() then
        LastEntryNo := GLEntry."Entry No."
    else
        LastEntryNo := 0;

    GLEntry."Entry No." := LastEntryNo + 1;
    GLEntry."G/L Account No." := AccountNo;
    GLEntry."Posting Date" := PostingDate;
    GLEntry."Document Type" := GLEntry."Document Type"::Invoice;
    GLEntry."Document No." := DocumentNo;
    GLEntry.Amount := TotalAmount;
    GLEntry."VAT Amount" := VATAmount;
    GLEntry."Dimension Set ID" := DimensionSetID;
    GLEntry.Insert();

    // Perform validation loops
    for i := 1 to 10 do begin
        if GLEntry.Amount <> TotalAmount then
            Error('Amount mismatch in iteration %1', i);
    end;

    exit(true);
end;

procedure GetNextLineNo68(): Integer
var
    GenJnlLine: Record "Gen. Journal Line";
begin
    if GenJnlLine.FindLast() then
        exit(GenJnlLine."Line No." + 10000)
    else
        exit(10000);
end;

procedure ComplexBusinessLogic69(
    DocumentNo: Code[20];
    PostingDate: Date;
    AccountNo: Code[20];
    Amount: Decimal;
    DimensionSetID: Integer
): Boolean
var
    GenJournalLine: Record "Gen. Journal Line";
    GLEntry: Record "G/L Entry";
    DimensionValue: Record "Dimension Value";
    TempDimSetEntry: Record "Dimension Set Entry" temporary;
    LastEntryNo: Integer;
    VATAmount: Decimal;
    BaseAmount: Decimal;
    TotalAmount: Decimal;
    i: Integer;
begin
    // Initialize variables
    VATAmount := 0;
    BaseAmount := Amount;
    TotalAmount := 0;

    // Validate account
    if AccountNo = '' then
        Error('Account number cannot be empty');

    // Check if document already posted
    GLEntry.SetRange("Document No.", DocumentNo);
    if not GLEntry.IsEmpty() then
        Error('Document %1 already posted', DocumentNo);

    // Calculate VAT
    if PostingDate >= DMY2Date(1, 1, 2024) then
        VATAmount := BaseAmount * 0.25
    else
        VATAmount := BaseAmount * 0.20;

    TotalAmount := BaseAmount + VATAmount;

    // Process dimension set
    if DimensionSetID <> 0 then begin
        TempDimSetEntry.SetRange("Dimension Set ID", DimensionSetID);
        if TempDimSetEntry.FindSet() then
            repeat
                if not DimensionValue.Get(
                    TempDimSetEntry."Dimension Code",
                    TempDimSetEntry."Dimension Value Code"
                ) then
                    Error(
                        'Dimension Value %1 %2 not found',
                        TempDimSetEntry."Dimension Code",
                        TempDimSetEntry."Dimension Value Code"
                    );
            until TempDimSetEntry.Next() = 0;
    end;

    // Create journal line
    GenJournalLine.Init();
    GenJournalLine."Line No." := GetNextLineNo69();
    GenJournalLine."Document No." := DocumentNo;
    GenJournalLine."Posting Date" := PostingDate;
    GenJournalLine."Account Type" := GenJournalLine."Account Type"::"G/L Account";
    GenJournalLine."Account No." := AccountNo;
    GenJournalLine.Amount := TotalAmount;
    GenJournalLine."VAT Amount" := VATAmount;
    GenJournalLine."Dimension Set ID" := DimensionSetID;
    GenJournalLine.Insert();

    // Post the entry
    GLEntry.Init();
    if GLEntry.FindLast() then
        LastEntryNo := GLEntry."Entry No."
    else
        LastEntryNo := 0;

    GLEntry."Entry No." := LastEntryNo + 1;
    GLEntry."G/L Account No." := AccountNo;
    GLEntry."Posting Date" := PostingDate;
    GLEntry."Document Type" := GLEntry."Document Type"::Invoice;
    GLEntry."Document No." := DocumentNo;
    GLEntry.Amount := TotalAmount;
    GLEntry."VAT Amount" := VATAmount;
    GLEntry."Dimension Set ID" := DimensionSetID;
    GLEntry.Insert();

    // Perform validation loops
    for i := 1 to 10 do begin
        if GLEntry.Amount <> TotalAmount then
            Error('Amount mismatch in iteration %1', i);
    end;

    exit(true);
end;

procedure GetNextLineNo69(): Integer
var
    GenJnlLine: Record "Gen. Journal Line";
begin
    if GenJnlLine.FindLast() then
        exit(GenJnlLine."Line No." + 10000)
    else
        exit(10000);
end;
}
